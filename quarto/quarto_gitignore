# Quarto render artifacts (regenerated on each render)
/.quarto/
*_cache/
*_files/

# OS junk
.DS_Store

# R session files
.Rhistory
.RData
.Rproj.user/

# Do NOT ignore _output/ or _data/ — these contain expensive
# simulation results (HTML reports and .rds bundles) that must
# be shared across machines. They are excluded from the R
# package tarball via .Rbuildignore (^quarto$).

# Machine-local analyses — do not propagate via git.
# These directories live on one machine only (large data, private
# inputs, in-progress work that should not yet be shared).
applications/private_analysis/

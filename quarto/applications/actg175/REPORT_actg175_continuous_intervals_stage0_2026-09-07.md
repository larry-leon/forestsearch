# REPORT — ACTG175 continuous intervals, Stage 0 (locate and anchor)

Date: 2026-09-07. Mac-Studio-3, `~/Documents/GitHub/forestsearch`, branch `feature/glm-extension-mac`, HEAD `f18a478f` at the start of this stage (task document committed as `4be1f9f9`; the carried-over wording edits as `f18a478f`), forestsearch 0.3.5 installed. Task: `dev/tasks/TASK_actg175_continuous_intervals_2026-09-07.md`; decisions N-1–N-4 at defaults. No R/ change; no edit to any off-limits path.

## 2.1 Candidates: every `.qmd` under `quarto/applications/actg175/` calling `forestsearch()` on a continuous outcome

Enumerated from source (`grep 'forestsearch('`, `outcome_type = "continuous"`), non-archive documents only:

| document | `forestsearch()` on a continuous outcome | MR gate today | committed payload |
|---|---|---|---|
| `analysis_actg175_continuous_oc.qmd` (1,061 lines) | yes — chunk `anchor` (lines 88–137): `outcome.name = "y_decline"`, `outcome_type = "continuous"`, `effect_measure = "MD"`, `adverse_outcome = TRUE`, `seedit = params$seed` (8316951), `sg_focus = "maxeffCons"`, `selection_rule = "neighborhood"`, `consistency_method = "resample"`, `effect.threshold = 10`, `consistency.threshold = 5`, `pconsistency.threshold = 0.90`, `use_twostage = TRUE`, `conf.cont_jcuts = list(age, preanti, wtkg, karnof, cd40, cd80 = 10)`, `cut_type = "default"`, `maxk = 2`, `n.min = 60`, `d0.min = d1.min = 10`, `fs.splits = 500`, `use_lasso = use_grf = use_dina = FALSE`, `is.RCT = TRUE`, `parallel_args = list(plan = "sequential")`, `details = FALSE`, `quiet = TRUE`; covariates `age preanti wtkg karnof cd40 cd80 hemo homo drugs race gender symptom` (data-prep chunk, lines 54–75) | **no** (`mr_inference` not set) | `_payloads/analysis_actg175_continuous_oc/analysis_actg175_continuous_oc_payload.rds` (tracked; 12,267 bytes; built 2026-08-31 21:06 under 0.3.2): `table` (the calibration ladder), `labels$sg_harm = "{age <= 37} & !{cd40 <= 507}"`, `labels$sg_focus = "maxeffCons"`, `meta` (N = 1,083, outcome `y_decline`, treat `treat`, covariates, c1 = 10, c2 = 8, `args_call`: draws 20,000, block 5e4, seed 8316951, M = 4,508, n_workers 14, `t_loop_secs` 2,056.4), `extras` (`type1`, `declared`, `comparator_c2_5`, `calibration`, **`anchor` = {def `{age <= 37} & !{cd40 <= 507}`, `n_H = 66`, `T_obs = 87.9166667`, `p_cons = 0.95`, `beta_treat = −27.0`}**, `purity`, `c2_policy`, `q_variants`, `interpretation` with `itt` = −27.591 (SE 7.889)) |
| `analysis_actg175_continuous_compare_all.qmd` (765 lines) | yes — `compare_selection_rules()` over eight (focus, rule) arms (chunk `run-comparison`, line 291) and the `maxeffcons-mr-anchor` chunk (line 404): the same configuration as above with `parallel_args = multisession`, `quiet = FALSE` | yes on the anchor arm only: `mr_inference = TRUE, mr_inference_args = list(ci_method = "ij", draws = fs_mr_draws (5000), include_complement = TRUE)` | **none tracked**: `_payloads/analysis_actg175_continuous_compare_all/` (`comparison_continuous.rds` 199 MB, `selected_subgroups_continuous.rds`) is untracked pre-existing dirt; only a 2026-09-01 HTML is tracked under `_payloads_2026-09-01/html/` |
| `template_actg175_continuous.qmd` (277 lines) | yes — chunk `fs-run` (line 101): `sg_focus = "minSG"`, `hr.threshold = c1`, `hr.consistency = c2`, `pconsistency 0.80`, `cont.cutoff = 4`, front ends off | no | `_payloads/template_actg175_continuous/…_payload.rds` (tracked): `labels$sg_harm = !{cd40 <= 421} & !{wtkg <= 82}` — a different subgroup |
| `analysis_actg175_continuous_oc_evaluation.qmd` | **no** `forestsearch()` call (0); out of scope by §5 | — | tracked payload carries the anchor label and `n_H = 66` (inherited from the OC run) |

**GATE 2.1: exactly one document reproduces the anchor from its committed payload with a `forestsearch()` call: `analysis_actg175_continuous_oc.qmd`.** The compare-all document reaches the same Ĥ but its payload is not committed; the template finds a different subgroup; the OC-evaluation document calls no search. **N-1 = `analysis_actg175_continuous_oc.qmd`.**

## 2.2 Identity anchors (numbers the document reports today that must be unchanged)

From the committed payload and the document's own gate (chunk `anchor`, lines 132–133: `stopifnot(setequal(fs_anchor$sg.harm, c("{age <= 37}", "!{cd40 <= 507}")), n_H == 66L, abs(T_obs - (87 + 11/12)) < 5e-7)`):

- the found rule `{age <= 37} & !{cd40 <= 507}`; n(Ĥ) = 66 of N = 1,083; the fitted mean difference T̂_obs = 87.9166667 CD4 units on the decline scale (the naive estimate); consistency p = 0.95; `beta_treat = −27.0` (adjusted GLM); unadjusted ITT −27.591 (SE 7.889) on `y_decline`;
- there is **no committed MR (IJ) interval and no FB result** for this document (the gate is not called today), so Table 1 carries naive / MR (IJ) / MR (field) only (convention 6);
- everything else in the payload (`table`, `extras$type1`, `declared`, `comparator_c2_5`, `calibration`, `purity`, `c2_policy`, `q_variants`, `interpretation`) — the operating-characteristic results, which the new flags cannot touch (the gate runs after the search; the OC loop reads `fs_anchor$sg.harm`, `T_obs`, the family). Gate 1 compares them element by element; `built_at` and `forestsearch_version` (0.3.2 → 0.3.5) are the expected provenance differences.

**Scales.** `y_decline = cd40 − cd420` (data-prep chunk, line 62): higher = worse, and with `adverse_outcome = TRUE` the gate works on `y_decline` itself, so the gate's columns are **oriented, positive = harm** (T̂_obs = +87.9 is harm in Ĥ; the ITT −27.6 on this scale is an overall benefit of 27.6 CD4 cells/mm³). The raw `cd4_change = cd420 − cd40` scale is the sign flip (N-3).

## 2.3 The gate call and the pass-throughs

The call as it stands is quoted in 2.1. It does not set `mr_inference`, so the three flags of §3.1 require `mr_inference = TRUE` with an `mr_inference_args` list to reach the gate at all; the list will carry exactly `ci_method = "field"`, `field_complement = TRUE`, `return_reselection = TRUE`, plus `draws = 5000L` (the compare-all anchor's and the `mdf1` campaign's value; the package default is 2,000) and `include_complement = TRUE` (already `forestsearch()`'s default, stated for the record). Nothing else changes on the call. **Deviation from "nothing else on the call" stated:** `mr_inference = TRUE` and `draws` are the enabling additions, since the document has never called the gate.

Entry point: `subgroup_method = "consistency"` on a GLM outcome takes the single consistency-engine MR site `R/forestsearch_main.R:3388–3421` (the `.mr_glm_ok` branch), which forwards `ci_method` (:3406), `return_reselection` (:3412), `field_complement` (:3416), `ij_residual` (:3420, default `"two_term"`), `draws`, `include_complement` and `seed = seedit`; `man/forestsearch.Rd` (lines 858–880) documents `ci_method`, `return_reselection` ("the gate's return gains the `reselection` element (per-draw winners and the re-selection frequencies `p_hat`)") and `field_complement` ("attached as `field$complement` with the one-sided 95% upper bound as its primary product"). The GLM `gspec` carries `adverse_outcome = TRUE`, so no flip is applied (`R/consistency_resample.R:255–257`); `log_scale = FALSE` for MD, `to_eff` is the identity (`R/fs_mr_inference.R:543`). The returned object carries `fs$mr_inference$naive`, `$debiased` (with `lower_1s`, `se_ij`, `se_wald`), `$complement$naive` / `$debiased`, `$field` (`est2`, `lower_1s`, `lower_2s`, `upper_2s`, `lambda_sd`, `lambda_mean`, draw counts, `timing_seconds`), `$field$complement` (`est2`, `upper_1s`, `lower_1s`, `lower_2s`, `upper_2s`, `se_field`), `$field$joint` (`gamma`, `lower_H`, `upper_Hc`, `bonf_*`, `joint_prob`, `corr`, `n_joint_draws`) and `$reselection$p_hat` (named by family label) — verified on the one-off run below (`names(g)` printed).

## 2.4 Render cost

The committed HTML records the document's own clock (chunk `doc-clock`, lines 978–980): **"document compute wall-clock so far: 51.9 min"** immediately before the payload export, on this machine (`n_workers = 14` in the payload equals this Mac's physical cores; the OC loop alone `t_loop_secs = 2,056 s` = 34.3 min). One-off timing (task §2.4's alternative; `scripts_mdf1`-style script, the document's `data-prep` and `anchor` chunks evaluated verbatim, then the same call with the gate on): search 0.7 s; search + gate 15.2 s, i.e. the MR increment is **14.5 s** (multiplier stage 0.5 s, field 9.9 s, complement 0.5 s; family 4,935 candidates). Ĥ, n(Ĥ) and T_obs reproduce exactly on both calls.

**Projection: ≈ 52.5 min per render** (51.9 + 0.25 min, plus pandoc), under the 1 h per-render limit with ≈ 7 min headroom; two renders would fit the 2 h cumulative ceiling. **GATE 2.4: PASS** (marginal headroom noted; the render runs under `scripts_mdf1/tmo.sh 3600`).

Preview from the one-off run (oriented decline scale; to be reproduced by the render, not typed into the document): naive 87.917 (4.31, 171.53), SE 42.66; MR (IJ) β̃ 33.615 (−72.04, 139.27), SE_IJ 53.91, one-sided lower −55.05; MR (field) est₂ 27.26, one-sided lower −43.80, two-sided (−56.44, 94.20), λ-SD 38.79; complement β̃ᶜ −33.62, field one-sided upper −18.55; joint γ = 0.025 (= Bonferroni), pair (−56.44, −16.35), corr −0.019; p̂(Ĥ) = 0.087.

## Gate 0

2.1 one document (N-1 = `analysis_actg175_continuous_oc.qmd`); 2.2 anchors listed; 2.3 pass-throughs confirmed from source and the one-off run; 2.4 projection 52.5 min ≤ 1 h. **Gate 0: PASS.** Proceeding to Stage 1.

# Launcher Preamble — prepend this to any hand-off brief

*Paste this block FIRST, then paste the topic hand-off (FDR tool, or companion
paper) immediately after it, in the same message. This tells the new chat to
adopt the standing session rules before starting the topic work.*

---

## Standing rules first

Before doing anything else, read and adopt the working rules in
**`claude_session_starter.md`** (the "Session Directive"). It is in this
project's knowledge base.

- **If you can access it** (this chat is inside the `forestsearch` project):
  retrieve it, then in your first response list its nine rules back in one short
  sentence each, and attempt the R install it specifies in §7 (by default). Then
  proceed to the topic brief below.
- **If you CANNOT access it** (this chat is outside the project, so the KB is not
  attached): say so plainly in one line and ask me to either attach the project
  or paste `claude_session_starter.md`. Do NOT proceed with the topic work until
  the standing rules are in hand — they govern how you handle files, scope, the
  sandbox, and venue selection, and skipping them has repeatedly cost rework.

Those rules take precedence over anything in the topic brief that conflicts with
them. In particular note from the starter: (§1) the most recent in-chat version
of a file is the source of truth, not the stale KB snapshot; (§4) ask for a file
rather than guessing its contents; (§7) install R by default and reinstall
silently after any sandbox reset; (§9) if a task needs the live compiled
`forestsearch` package or a real multi-core run, STOP and recommend a Claude Code
session in RStudio (or my 127-core machine) rather than grinding in the chat
sandbox.

## Then the topic

Everything below this line is the topic-specific hand-off for this chat. Adopt
the standing rules above, acknowledge them, then begin the topic work.

---

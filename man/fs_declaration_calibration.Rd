% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_declaration_calibration.R
\name{fs_declaration_calibration}
\alias{fs_declaration_calibration}
\alias{print.fs_declaration_calibration}
\title{Calibrated declaration threshold and family-wise size of the p-star screen}
\usage{
fs_declaration_calibration(
  fit,
  alpha = 0.05,
  family = c("prereduction", "reduced"),
  ...,
  c0 = NULL
)

\method{print}{fs_declaration_calibration}(x, ...)
}
\arguments{
\item{fit}{A \code{forestsearch} object fitted with the declaration field
retained, or the list returned by \code{fs_mr_inference()} with
\code{keep_declaration_field = TRUE}.}

\item{alpha}{Target family-wise declaration rate, in (0, 1). Default \code{0.05}.}

\item{family}{\code{"prereduction"} (default) or \code{"reduced"}; see the Family
section.}

\item{...}{Only \code{p_star} and \code{c_cons} are read, and only when the fit's
resolved admission set carries no consistency floor (for example
\code{sg_focus = "maxeff"}); they then name the hypothetical screen whose size
is evaluated.  \code{c_cons} is on the comparison scale (log for ratio
measures).}

\item{c0}{\code{NULL} (default) or a numeric vector of clinically specified,
pre-specified protected null levels; see the Protected null level
section.  Named only (it follows \code{...}).}

\item{x}{An \code{fs_declaration_calibration} object.}
}
\value{
An object of class \code{fs_declaration_calibration}: a list with
\code{kappa_hat}, \code{fw_size}, \code{alpha}, \code{p_star}, \code{z_pstar}, \code{c_cons},
\code{c_screen}, \code{B}, \code{multiplier_law}, \code{quantile_type}, \code{family_source},
\code{family_label}, \code{n_family_prereduction}, \code{n_family_reduced},
\code{admitted_current} (the candidates the executed screen admitted; \code{NULL}
when the fit is a bare \code{fs_mr_inference()} result), \code{admitted_calibrated},
\code{admitted_pstar} (the relabelled current rule over the same family),
\code{screened} (the candidates the consistency screen evaluated, when known),
\code{Mstar}, \code{beta_hat}, \code{sigma_D}, \code{T_hat}, \code{column_sd}, \code{zstar_mean},
\code{field_cor} (families of at most 8 candidates, else \code{NULL}), and a
\code{reduction} list recording the replay of the near-duplicate reduction.
When \code{c0} is given it also carries \code{c0}, a list with \code{table} (one row per
\code{c0}: \code{c0}, \code{c0_cmp}, \code{kappa_hat}, \code{fw_size}, \code{pstar_implied},
\code{n_admitted_calibrated}, the 0.90 / 0.95 / 0.99 quantiles of \code{Mstar_c0},
and \code{is_c2}), \code{admitted_calibrated} (a list indexed by \code{c0}), \code{Mstar_c0}
and \code{source} (\code{"capture"} or \code{"field_matrix"}).  Every other element is
unchanged by \code{c0}.
}
\description{
Post-hoc, opt-in, reported diagnostics of the consistency screen, computed
from multiplier draws the package already produces.  The screen admits a
candidate \code{g} when its standardized statistic
\code{T(g) = (beta_hat(g) - c_cons) / sigma_D(g)} clears \code{qnorm((1 + p_star) / 2)};
that admission is exactly the closed-form consistency rule, relabelled.
This function reports how often the maximum of that statistic over the
candidate family would clear the conventional cutoff under the null
perturbation law (\code{fw_size}), and the cutoff that would hold the
family-wise declaration rate at \code{alpha} (\code{kappa_hat}).
}
\details{
It reads a fitted object and returns a new one.  It does not modify its
input, does not re-run the search, does not call the consistency engine,
and does not change what the search admitted: \code{admitted_calibrated} is the
set the calibrated rule \emph{would} admit, reported beside \code{admitted_current}.
}
\section{Definitions}{

With shared multipliers \code{xi[b, i]} (one vector per draw \code{b}, reused across
every candidate) and the dfbeta influence \code{db[g, i]}:
\itemize{
\item \verb{Zstar[b, g] = sum_i xi[b, i] * db[g, i] / sigma_D(g)}, with
\verb{sigma_D(g)^2 = sum_i db[g, i]^2} -- the robust scale the screen itself
uses, never a model-based standard error.
\item \verb{Mstar[b] = max_g Zstar[b, g]} over the family: one-sided, in the harm
direction.
\item \code{kappa_hat(alpha)} is the empirical (\code{type = 1}) \code{1 - alpha} quantile of
\code{Mstar}.
\item \code{fw_size = mean(Mstar > qnorm((1 + p_star) / 2))}.  Its threshold is the
fit's own \code{p_star}, not \code{alpha}: it is the family-wise size of the screen
as run, not of the calibrated rule.
\item The calibrated admission rule is
\code{beta_hat(g) >= max(c_screen, c_cons + kappa_hat * sigma_D(g))}, the
current rule with \code{qnorm((1 + p_star) / 2)} replaced by \code{kappa_hat}.
}
}

\section{Family}{

\code{family = "prereduction"} (the default, and the only value meant for the
calibrated rule) takes the maximum over the multiplier-resampling family:
every enumerated candidate of at most \code{maxk} factors meeting the size
minimum, before any reduction keyed on fitted quantities.  The
near-duplicate reduction keys on sample-fitted summaries, so a family
reduced by it is outcome-dependent; the pre-reduction family is
covariate-measurable, and is also the conservative choice (a larger family
raises the maximum).  The per-arm event minima are not replayed in that
family, which makes it a superset of the one the search chose among.

\code{family = "reduced"} removes the candidates the near-duplicate reduction
removed and is a diagnostic only, so the gap is measurable.  Its result is
conditional on the realized family and is labelled so; it is never meant
for admission.  It needs a \code{forestsearch} fit (the reduction lives in the
identifier) and the full field matrix (\code{keep_field_matrix = TRUE}).
}

\section{Protected null level}{

\code{c1} and \code{c2} state the claim; \code{c0} states what the claim is protected
against: a clinically specified, pre-specified benefit level (for example
HR 0.75).  The family-wise declaration rate is then controlled at \code{alpha}
whenever every candidate's true effect is at least as good as \code{c0}, rather
than only when every candidate sits at \code{c2}.  With
\code{delta_g = (c_cons - c0_cmp) / sigma_D(g)}:
\itemize{
\item \verb{Mstar_c0[b] = max_g \{ Zstar[b, g] - delta_g \}};
\item \code{kappa_hat(c0)} is its empirical (\code{type = 1}) \code{1 - alpha} quantile;
\item \code{fw_size(c0) = mean(Mstar_c0 > qnorm((1 + p_star) / 2))};
\item \code{pstar_implied = 2 * pnorm(kappa_hat(c0)) - 1}, the \code{p_star} whose
cutoff equals \code{kappa_hat(c0)};
\item admission is unchanged in form, \code{T(g) >= kappa_hat(c0)}.
}

If every candidate's true effect is \code{c0_cmp}, \code{T(g)} is centred at
\code{-delta_g}, so the null law of \verb{max_g T(g)} is that of the shifted maximum.
At \code{c0 = c2}, \code{delta_g = 0} and every quantity is the unshifted one.

\code{c0} is on the natural scale of the consistency threshold \code{c2} (the HR on
the survival path; the ratio for OR / RR / IRR; the difference for RD /
MD) and is mapped exactly as \code{c2} is: \code{log()} for ratio measures, identity
otherwise.  \code{c0 <= c2} is required.  A non-positive \code{c0} on a ratio path
(a log supplied by mistake) errors; on an identity path a wrong-scale
\code{c0} cannot be detected.

The shifted maxima are read from the capture when the fit carries them
for every requested \code{c0} (\code{declaration_c0} at fit time); otherwise they are
computed from the stored field matrix (\code{keep_field_matrix = TRUE});
otherwise the call errors.  It never falls back to the unshifted maximum.
\code{family = "reduced"} always computes from the field matrix.
}

\section{Inputs}{

The field is retained only on request.  Fit with
\code{mr_inference = TRUE} and
\code{mr_inference_args = list(keep_declaration_field = TRUE)} (add
\code{keep_field_matrix = TRUE} for the reduced diagnostic), or call
\code{fs_mr_inference(..., keep_declaration_field = TRUE)} and pass its result.
The multiplier law and \code{B} are those of the MR draws and are recorded in
the result.
}

\examples{
\dontrun{
fit <- forestsearch(df, ..., mr_inference = TRUE,
                    mr_inference_args = list(keep_declaration_field = TRUE))
fs_declaration_calibration(fit, alpha = 0.05)
}
}
\seealso{
\code{\link[=fs_mr_inference]{fs_mr_inference()}}, \code{\link[=fs_family_report]{fs_family_report()}}, \code{\link[=fs_fdr_report]{fs_fdr_report()}}.
}

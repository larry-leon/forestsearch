% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_identification_summary.R
\name{fs_rule_covariate_pairs}
\alias{fs_rule_covariate_pairs}
\title{Covariate signatures of the realized rules}
\usage{
fs_rule_covariate_pairs(x, true_covariates = NULL, detected_only = TRUE)
}
\arguments{
\item{x}{A results data frame carrying \code{sg_def}, a bundle with \code{results}, a
path to an \code{.rds}, or a bare character vector of rules.}

\item{true_covariates}{Character vector. The true-region covariates, used to
classify each signature as \code{"exact"} (the signature is exactly this set),
\code{"partial"} (overlaps it) or \code{"none"}. \code{NULL} skips the classification.}

\item{detected_only}{Logical. Restrict to rows with \code{detected == 1} when a
results frame is supplied.}
}
\value{
A data frame ordered by frequency: \code{covariates}, \code{k}, \code{n}, \code{share},
\code{n_rules}, and \code{match} when \code{true_covariates} is given.
}
\description{
Collapses realized rules to the \strong{set of covariates} they name, discarding
the cut values. A rules-by-frequency table lists every distinct \code{sg_def}
string and so is dominated by cut noise -- a 500-replicate survival run
produced 583 rows, in which \code{{age <= 46} & {nodes <= 5}} and
\code{{nodes <= 3} & {age <= 46}} are separate entries despite being the same
pairing. This reports how often each covariate \emph{combination} is selected,
which is the structural question.
}
\section{What a signature is}{


The covariate names in the rule, de-duplicated and sorted, joined by \code{" & "}.
Sorting is what makes the two orderings above collapse together. Two
consequences worth knowing:
\itemize{
\item A rule naming one covariate twice --- a band such as
\code{!{nodes <= 3} & {nodes <= 5}} --- has signature \code{"nodes"}, of size 1. The
\code{k} column gives the number of distinct covariates, and \code{n_rules} the
number of distinct rule strings that collapsed into the row, so bands are
visible rather than hidden.
\item Logically equivalent spellings of the same condition (\code{!{meno}} versus
\code{{meno == 0}}) collapse together, which a raw rule table also splits.
}
}


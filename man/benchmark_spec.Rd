% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_subgroup_sims.R
\name{benchmark_spec}
\alias{benchmark_spec}
\title{Random-benchmark subgroup specification}
\usage{
benchmark_spec(sizes = c(60L, 40L, 20L, 15L), nested = TRUE, prefix = "random")
}
\arguments{
\item{sizes}{Integer vector of benchmark sizes. Order determines the
column-creation order only; the draw uses \code{max(sizes)}.}

\item{nested}{Logical. \code{TRUE} (default): one draw, prefix nesting --
the vignette scheme. \code{FALSE}: an independent draw per size, in the
order given (consumes more RNG; not the vignette scheme).}

\item{prefix}{Column-name prefix, default \code{"random"}.}
}
\value{
An object of class \code{"benchmark_spec"}.
}
\description{
Describes the size-matched random benchmark subgroups regenerated in
every simulated trial. With the default \code{nested = TRUE}, a single
index draw of \code{max(sizes)} patients is taken per trial and the smaller
benchmarks are its prefixes (\code{random15} \eqn{\subset} \code{random20}
\eqn{\subset} \code{random40} \eqn{\subset} \code{random60}), exactly as in the
extreme-subgroups vignettes. Membership columns are named
\code{paste0(prefix, sizes)} and take values 0/1.
}
\examples{
benchmark_spec()
benchmark_spec(sizes = c(100, 50), prefix = "rnd")
}
\seealso{
\code{\link[=run_subgroup_sims]{run_subgroup_sims()}}
}

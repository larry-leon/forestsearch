% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaHhat_truth.R
\name{fs_build_eval_frame}
\alias{fs_build_eval_frame}
\title{The frame \code{beta(Hhat)} is scored on}
\usage{
fs_build_eval_frame(
  dgm,
  outcome_type = c("survival", "binary", "continuous", "count"),
  eval_seed = 20260628L,
  analysis_time = 84,
  cens_adjust = log(1.5),
  n_eval = NULL
)
}
\arguments{
\item{dgm}{A DGM object carrying \code{df_super}.}

\item{outcome_type}{Character. One of \code{"survival"}, \code{"binary"},
\code{"continuous"}, \code{"count"}.}

\item{eval_seed}{Integer. Fixes the single realization on the full pool.
Ignored for \code{"continuous"} and \code{"count"}.}

\item{analysis_time, cens_adjust}{Survival only.}

\item{n_eval}{Defunct. Non-\code{NULL} is a hard error: this always evaluates the
full super-population, so both engines score the identical target.}
}
\value{
A data frame: the evaluation population.
}
\description{
One entry point per outcome family, dispatching on \code{outcome_type} rather
than exposing a separate function per family. Defaults reproduce the
simulation modules this replaces exactly.
}
\section{What each family returns}{

\itemize{
\item \strong{survival} -- the entire fixed super-population, every subject exactly
once (\code{replace = FALSE}), under the same randomized/censored analysis the
trials run.
\item \strong{binary} -- the same construction on the GLM simulator: every subject
once, under one fixed treatment/outcome realization.
\item \strong{continuous / count} -- \code{dgm$df_super} \strong{unchanged}. No simulation
occurs. The mean difference is collapsible, so the target is an exact
finite mean over the super-population: the scoring frame \emph{is} the
population. \code{eval_seed} is accepted and ignored here so that generic
harness code need not branch, and the target carries \strong{zero Monte Carlo
error} -- uniformity of the call surface is not sameness of the object.
}
}

\section{Rejected arguments}{


\code{analysis_time}, \code{cens_adjust} and \code{n_eval} are survival-only. Supplying any
of them on a non-survival path is an error rather than a silent no-op:
quietly ignoring an argument that means something on another path is how
conventions drift apart.
}

\keyword{internal}

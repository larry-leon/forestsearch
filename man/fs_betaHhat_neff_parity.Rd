% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaHhat_truth.R
\name{fs_betaHhat_neff_parity}
\alias{fs_betaHhat_neff_parity}
\title{Refuse to print coverage targets computed on different denominators}
\usage{
fs_betaHhat_neff_parity(
  cov_df,
  strict = TRUE,
  target_beta = "C_betaHhat",
  target_ref = "C_dagger"
)
}
\arguments{
\item{cov_df}{Coverage table with \code{target}, \code{n_eff}, \code{block} and
\code{estimator} columns.}

\item{strict}{Logical. \code{TRUE} stops; \code{FALSE} warns, for a run whose cause is
known.}

\item{target_beta, target_ref}{Character. The target to check and the
reference to check it against.}
}
\value{
\code{cov_df}, invisibly.
}
\description{
\code{.coverage_meta()}-style assembly computes
\code{ok <- is.finite(target) & is.finite(lo) & is.finite(hi)} and
\code{n_eff <- sum(ok)}, so a non-finite target is dropped with no error and no
warning. Coverage is then computed over the replicates whose targets
happened to score, which is not a random subset: whatever made a target
non-finite is a property of the realized rule.
}
\details{
Because a reference target that is a finite scalar is dropped only when the
interval itself is non-finite, \code{n_eff} parity between the two is the exact
check for a target-specific loss, and it is free -- \code{n_eff} is already in
the table.
}
\section{Two causes, deliberately not distinguished}{


A parity break can mean an unresolvable rule, or a genuinely degenerate
region whose per-family guard returned \code{NA} by design. Both make the two
coverage figures incomparable as printed, so both stop. Use
\code{\link[=fs_betaHhat_neff_report]{fs_betaHhat_neff_report()}} on the bundle to tell them apart before
deciding.
}

\keyword{internal}

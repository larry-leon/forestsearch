% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_subgroup_sims.R
\name{validate_subgroups}
\alias{validate_subgroups}
\title{Validate subgroup definitions against a data frame}
\usage{
validate_subgroups(
  subgroups,
  data,
  cutpoints = list(),
  benchmarks = benchmark_spec()
)
}
\arguments{
\item{subgroups}{List of subgroup definitions; each element a list
with character scalars \code{id} (an R expression over columns), \code{name},
and \code{grp}.}

\item{data}{A data frame carrying the covariates the \code{id} expressions
reference (e.g. \code{dgm$df_source} or \code{dgm$df_super}).}

\item{cutpoints}{Named list of scalars exposed as columns (e.g.
\code{list(age_med = 53)}), matching the runner's \code{cutpoints} argument.}

\item{benchmarks}{A \code{\link[=benchmark_spec]{benchmark_spec()}} (or \code{NULL}) whose membership
columns are stubbed as \code{0L} during validation.}
}
\value{
Invisibly \code{TRUE}; errors otherwise.
}
\description{
Evaluates every subgroup \code{id} expression against \code{data} (augmented
with \code{flag_itt = 1L}, the scalar \code{cutpoints} as columns, and 0-valued
benchmark membership columns) and fails with a named, collected error
if any expression errors, is non-logical, or has the wrong length.
Run automatically at the top of \code{\link[=run_subgroup_sims]{run_subgroup_sims()}} so that a typo
in a subgroup definition stops the study in milliseconds instead of
surfacing as a silent all-\code{NA} column after the simulation loop.
}

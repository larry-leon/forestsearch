% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dina.R
\name{dina_fit}
\alias{dina_fit}
\title{Estimate heterogeneous treatment effects via DINA}
\usage{
dina_fit(
  X,
  Y,
  W,
  family = c("gaussian", "binomial", "poisson", "cox"),
  propensity_method = "logistic",
  baseline_method = NULL,
  cross_fitting = TRUE,
  n_folds = 2L,
  cens_type = NULL,
  cens_params = list(),
  n_grid = 500L,
  eps = 1e-06,
  seed = NULL
)
}
\arguments{
\item{X}{a numeric covariate matrix or data frame with \code{n} rows and \code{d}
columns.  All columns are treated as numeric predictors.  Column names,
when present, are propagated to the coefficient names of the fitted
object.}

\item{Y}{the response.  For \code{family = "cox"}, a data frame (or matrix
coercible to one) with columns \code{time} (event/censoring time) and
\code{status} (1 = event, 0 = censored).  For all other families, a numeric
vector of length \code{n}.}

\item{W}{binary treatment indicator (0 = control, 1 = treatment), length
\code{n}.}

\item{family}{character; one of \code{"gaussian"}, \code{"binomial"}, \code{"poisson"},
\code{"cox"}.}

\item{propensity_method}{how to estimate the propensity score
\eqn{e(x) = P(W = 1 \mid X = x)}.  One of:
\itemize{
\item the string \code{"logistic"} (the default): a logistic GLM on \code{X};
\item a numeric scalar in \verb{(0, 1)}: a known constant propensity (e.g.,
\code{0.5} for a balanced randomized trial);
\item a function taking a covariate matrix and returning a numeric vector
of fitted propensities, allowing the user to plug in random forests,
boosting, etc.
}}

\item{baseline_method}{how to estimate the natural-parameter functions
\eqn{\eta_0(x)}, \eqn{\eta_1(x)} (or the marginal mean for the Gaussian
family).  One of:
\itemize{
\item \code{NULL} (the default): use \code{"glm"} for non-Cox families and \code{"cox"} for
the Cox family;
\item \code{"glm"}: a GLM with the natural link for the family;
\item \code{"cox"}: \code{\link[survival:coxph]{survival::coxph()}} (Cox family only);
\item a list with components \code{eta0} and \code{eta1}, each a function taking a
covariate matrix and returning a vector of linear predictors.  For
Gaussian, a list with a single component \code{nu} may be supplied
instead.
}}

\item{cross_fitting}{logical; if \code{TRUE} (the default) use \code{n_folds}-fold
cross-fitting to separate nuisance estimation from target-parameter
estimation.  If \code{FALSE}, the same data are used for both steps.}

\item{n_folds}{positive integer giving the number of cross-fitting folds
(default \code{2}, matching the paper).}

\item{cens_type}{for \code{family = "cox"}, the censoring mechanism.  One of
\code{NULL} (no censoring), \code{"fixed"}, \code{"uniform"}, \code{"exponential"}, or
\code{"unknown"} (estimate the probability of \emph{not} being censored from \code{X}
by logistic regression).  Ignored for non-Cox families.}

\item{cens_params}{named list of censoring parameters.  Required entries
depend on \code{cens_type}:
\itemize{
\item \code{"fixed"}, \code{"uniform"}: \code{T0} (the administrative censoring time).
\item \code{"exponential"}: \code{rate_c} (the exponential rate).
\item \code{"unknown"}, \code{NULL}: ignored.
}}

\item{n_grid}{positive integer giving the number of time-grid points used
for numerical integration of the not-censoring probability for
\code{cens_type \%in\% c("uniform", "exponential")} (default \code{500}).}

\item{eps}{small positive constant added to denominators for numerical
stability (default \code{1e-6}).}

\item{seed}{optional integer seed for the random cross-fitting fold
assignment.  When supplied, the global RNG state is restored on exit.}
}
\value{
An object of class \code{"dina"}, a list with components:
\describe{
\item{coefficients}{named numeric vector of estimated DINA coefficients
\eqn{\hat\beta}, with \code{(Intercept)} first followed by one entry per
covariate.}
\item{vcov}{estimated variance-covariance matrix of \eqn{\hat\beta}.}
\item{family}{the response family, as supplied.}
\item{n, d}{sample size and number of covariates.}
\item{cross_fitting, n_folds}{cross-fitting settings.}
\item{call}{the matched call.}
}
}
\description{
\code{dina_fit()} implements the two-step DINA (DIfference in NAtural parameters)
estimator of Gao and Hastie (2025) for heterogeneous treatment effects
(HTE) with general response types: Gaussian (mean difference), binomial
(log odds ratio), Poisson (log mean ratio), and Cox (log hazard ratio).
The treatment effect is assumed to be linear in the covariates,
\deqn{\tau(x) = \beta_0 + x^\top \beta_{1:d},}
following Proposition 1 of the paper.  The procedure has two steps:
(1) estimate nuisance functions \eqn{a(x)} and \eqn{\nu(x)} on a training
fold, and (2) plug those estimators into a GLM (or Cox) likelihood on the
held-out fold to estimate \eqn{\beta}.  Cross-fitting averages the two
estimators obtained by swapping the folds.
}
\details{
The Gaussian family uses the simplification of Section 3.2: \eqn{a(x) =
e(x)} and \eqn{\nu(x) = E[Y \mid X = x]}, so the conditional outcome means
\eqn{\eta_0(x)}, \eqn{\eta_1(x)} are not required separately.  For the
binomial and Poisson families, separate within-arm fits to \eqn{\eta_0(x)}
and \eqn{\eta_1(x)} are used to construct \eqn{a(x)} via the modified
propensity in Eq. (12) of the paper.  For the Cox family, \eqn{a(x)} uses
the not-censoring probabilities of Eq. (18); how those are estimated is
controlled by \code{cens_type}.

Variance estimation follows the standard sandwich form:
\eqn{\widehat{V}(\hat\beta) = \widehat\Sigma_1^{-1} \widehat\Sigma_2
\widehat\Sigma_1^{-1} / n}, where \eqn{\widehat\Sigma_1} and
\eqn{\widehat\Sigma_2} are the bread (negative score derivative) and meat
(outer product of scores) matrices.  Under cross-fitting these are
averaged across folds before the sandwich is formed.  For the Cox step 2
we use the partial-likelihood information-matrix variance returned by
\code{\link[survival:coxph]{survival::coxph()}}.
}
\examples{
set.seed(1)
n <- 400
d <- 3
X <- matrix(stats::runif(n * d, -1, 1), n, d)
colnames(X) <- paste0("x", seq_len(d))
W <- stats::rbinom(n, 1, stats::plogis(0.5 * X[, 1]))
tau_x <- 0.5 + 0.8 * X[, 1] - 0.3 * X[, 2]
Y <- 1 + X[, 1] + W * tau_x + stats::rnorm(n)
fit <- dina_fit(X, Y, W, family = "gaussian", seed = 1)
coef(fit)
confint(fit)

}
\references{
Gao, Z. and Hastie, T. (2025).  Estimating heterogeneous treatment
effects for general responses.  \emph{Biometrics} 81(4): ujaf162.
\doi{10.1093/biomtc/ujaf162}.

Robinson, P. M. (1988).  Root-n-consistent semiparametric regression.
\emph{Econometrica} 56(4): 931--954.

Nie, X. and Wager, S. (2021).  Quasi-oracle estimation of heterogeneous
treatment effects.  \emph{Biometrika} 108(2): 299--319.

Dandl, S., Bender, A. and Hothorn, T. (2024).  Heterogeneous treatment
effect estimation for observational data using model-based forests.
\emph{Statistical Methods in Medical Research} 33(3): 392--413.
}

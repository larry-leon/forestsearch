% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaHhat_truth.R
\name{fs_betaHhat_table}
\alias{fs_betaHhat_table}
\title{Deduplicated \code{beta(Hhat)} targets over distinct realized rules}
\usage{
fs_betaHhat_table(
  sg_defs,
  frame,
  focus,
  outcome_type = c("survival", "binary", "continuous", "count"),
  effect_measure = NULL,
  outcome.name = "y_sim",
  event.name = "event_sim",
  treat.name = "treat_sim",
  sg_def_structs = NULL
)
}
\arguments{
\item{sg_defs}{Character vector of realized rules, one entry per replicate.
\code{NA} or \code{""} marks a replicate where no subgroup was identified.}

\item{frame}{Data frame. The fixed evaluation population.}

\item{focus, outcome_type, effect_measure, outcome.name, event.name, treat.name}{Passed to \code{\link[=fs_betaHhat_one]{fs_betaHhat_one()}}.}

\item{sg_def_structs}{Optional named list of structured GRF definitions,
keyed by the rule string, used in preference to string parsing.}
}
\value{
A data frame keyed by \code{sg_def}, one row per distinct rule, with the
\code{\link[=fs_betaHhat_one]{fs_betaHhat_one()}} schema. Resolution counters are attached as
attributes and retrievable with \code{\link[=fs_betaHhat_counts]{fs_betaHhat_counts()}}.
}
\description{
Scores each \strong{distinct} realized rule once. Deduplication is by rule, not
by replicate: two replicates landing on the same rule score the same target,
and the target does not depend on the trial.
}
\section{Resolution accounting}{


Six counters are attached to the result and are the fix for targets being
dropped silently. \code{n_eff} reported beside a target is what makes a reduced
denominator visible; a coverage figure computed on an unknown fraction of
replicates is not interpretable, and the fraction has to travel with it.

A seventh, \code{n_reps_undetected}, counts replicates with no rule at all, so
that \code{n_reps_resolved + n_reps_unresolved + n_reps_undetected} closes
against \code{n_reps_total}.
}

\keyword{internal}

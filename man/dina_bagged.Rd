% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dina_df.R
\name{dina_bagged}
\alias{dina_bagged}
\title{Bagged DINA with IJ variance (data-frame interface)}
\usage{
dina_bagged(
  df,
  outcome,
  treatment,
  covariates,
  family = c("gaussian", "binomial", "poisson", "cox"),
  status = NULL,
  ...
)
}
\arguments{
\item{df}{data frame (or tibble / \code{data.table}) containing the
outcome, treatment, and covariate columns.}

\item{outcome}{character(1); the name of the response column in
\code{df}.  For \code{family = "cox"} this is the event/censoring time
column.}

\item{treatment}{character(1); the name of the binary treatment
indicator column in \code{df}.  Values must be 0/1 or logical.}

\item{covariates}{character vector of length \verb{>= 1}; the names of
the covariate columns in \code{df}.  All must be numeric.}

\item{family}{one of \code{"gaussian"}, \code{"binomial"}, \code{"poisson"}, \code{"cox"}.}

\item{status}{character(1) or \code{NULL}; for \code{family = "cox"} only, the
name of the event indicator column in \code{df} (\code{1} = event, \code{0} =
censored).  Ignored for non-Cox families.}

\item{...}{further arguments forwarded to \code{\link[=dina_fit_bagged]{dina_fit_bagged()}}.}
}
\value{
An object of class \code{c("dina_bagged", "dina")}, as returned
by \code{\link[=dina_fit_bagged]{dina_fit_bagged()}}.  The \verb{$call} component is overwritten with
the original \code{dina_bagged()} call.
}
\description{
\code{dina_bagged()} is the high-level user-facing wrapper around
\code{\link[=dina_fit_bagged]{dina_fit_bagged()}}.  It accepts a data frame plus column-name
arguments and dispatches to \code{\link[=dina_fit_bagged]{dina_fit_bagged()}}, which fits the
DINA estimator on \code{n_bags} bootstrap replicates and returns the
infinitesimal-jackknife variance per Wager, Hastie and Efron (2014).
}
\details{
All bagging-related arguments (\code{n_bags}, \code{cross_fitting_per_bag},
\code{n_folds_per_bag}, \code{parallel}, \code{ij_finite_sample_correction},
\code{project_psd}, \code{verbose}, ...) are forwarded via \code{...}.  See
\code{\link[=dina_fit_bagged]{dina_fit_bagged()}} for full documentation.

For \code{parallel = "bags"}, set \code{future::plan()} to a parallel backend
\emph{before} calling \code{dina_bagged()}.  Restore with
\verb{future::plan("sequential"); gc()} afterwards.
}
\examples{
set.seed(1)
n <- 400
df_demo <- data.frame(
  y         = NA,
  w         = stats::rbinom(n, 1, 0.5),
  x1        = stats::runif(n, -1, 1),
  x2        = stats::runif(n, -1, 1),
  x3        = stats::runif(n, -1, 1)
)
tau_x      <- 0.5 + 0.8 * df_demo$x1 - 0.3 * df_demo$x2
df_demo$y  <- 1 + df_demo$x1 + df_demo$w * tau_x + stats::rnorm(n)

fit <- dina_bagged(
  df         = df_demo,
  outcome    = "y",
  treatment  = "w",
  covariates = c("x1", "x2", "x3"),
  family     = "gaussian",
  n_bags     = 30L,
  seed       = 1L
)
coef(fit)
sqrt(diag(vcov(fit)))

}
\seealso{
\code{\link[=dina_fit_bagged]{dina_fit_bagged()}} for the underlying matrix-based bagged
estimator; \code{\link[=dina]{dina()}} for the single-pass DINA with sandwich variance.
}

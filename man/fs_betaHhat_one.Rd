% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaHhat_truth.R
\name{fs_betaHhat_one}
\alias{fs_betaHhat_one}
\title{Conditional estimand \code{beta(Hhat)} for one realized rule}
\usage{
fs_betaHhat_one(
  rule,
  frame,
  focus,
  outcome_type = c("survival", "binary", "continuous", "count"),
  effect_measure = NULL,
  outcome.name = "y_sim",
  event.name = "event_sim",
  treat.name = "treat_sim",
  sg_def_struct = NULL
)
}
\arguments{
\item{rule}{Character. The realized rule: a named \code{sg.harm} vector, the
\code{" & "}-joined \code{sg_def} string, or a GRF disjunction
\code{"(a & b) | (c & d)"}. \code{NULL} or \code{NA} means no subgroup was identified.}

\item{frame}{Data frame. The fixed evaluation population.}

\item{focus}{Character, required, no default. \code{"harm"} reads
\code{treat.recommend == 0} as in-region; \code{"benefit"} inverts.}

\item{outcome_type}{Character. One of \code{"survival"}, \code{"binary"},
\code{"continuous"}, \code{"count"}.}

\item{effect_measure}{Character. Passed to \code{\link[=compute_aor]{compute_aor()}} for the
continuous and count families; ignored for survival and binary, which keep
their fitted targets.}

\item{outcome.name, event.name, treat.name}{Character. Column names in
\code{frame}. \code{event.name} is used only by the survival family.}

\item{sg_def_struct}{Optional structured GRF subgroup definition. When
supplied it takes precedence over \code{rule} and membership is computed by
\code{.grf_evaluate_subgroup()}, which is the package's own answer for GRF
membership.}
}
\value{
A one-row data frame with columns \code{betaHhat_H}, \code{betaHhat_Hc},
\code{nH_eval}, \code{nHc_eval}, \code{status} and \code{missing_cols}. The column names are
scale-agnostic and identical across families so downstream
\code{paste0("betaHhat_", suffix)} scoring is shared verbatim.
}
\description{
Computes the population value of the standard within-subgroup analysis on a
realized selection region and its complement, on a fixed evaluation frame.
This is the estimand Theorem 2 certifies coverage for, evaluated at the
rule a detector actually returned -- not the marginal true-subgroup target.
}
\details{
Membership is resolved once, by \code{\link[=.fs_resolve_membership]{.fs_resolve_membership()}}, and the same
resolution serves every outcome family: membership is a property of the rule
and the frame, not of the effect measure.
}
\section{The partition invariant}{


\code{nH_eval + nHc_eval} must equal \code{nrow(frame)} on every call where the rule
resolves. Hhat and its complement partition the frame by construction, so a
sum below \code{nrow(frame)} means some rows fell into neither side -- which
happens when a membership vector carries \code{NA}. That is a hard error, not a
warning: if membership is incoherent, every number downstream of it is
meaningless.
}

\section{Unresolvable rules}{


A rule naming a column the frame lacks has no membership on that frame. The
record is all-\code{NA} with \code{status = "unresolved"} and \code{missing_cols} naming
the columns, rather than a partially-resolved region. Silently dropping the
offending clause returns a finite value for the wrong region.
}

\keyword{internal}

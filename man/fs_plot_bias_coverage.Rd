% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_bias_coverage.R
\name{fs_plot_bias_coverage}
\alias{fs_plot_bias_coverage}
\title{Bias-vs-coverage display (three panels)}
\usage{
fs_plot_bias_coverage(
  tbl,
  labels = TRUE,
  curves = c(1, 1.25, 1.5, 2),
  level = 0.95
)
}
\arguments{
\item{tbl}{Stacked output of \code{\link[=fs_sim_bias_coverage]{fs_sim_bias_coverage()}} with an added
\code{cell} column (one factor level per simulation cell).}

\item{labels}{Draw cell labels next to the points (default \code{TRUE}).}

\item{curves}{Gaussian-reference \code{r} values for panels 1-2
(default \code{c(1, 1.25, 1.5, 2)}).}

\item{level}{Nominal level for the reference lines (default 0.95).}
}
\value{
A patchwork object of the three ggplot panels.
}
\description{
Draws the standard bias-vs-coverage display from a stacked
\code{\link[=fs_sim_bias_coverage]{fs_sim_bias_coverage()}} table carrying a \code{cell} column: (1) one-sided
coverage against \code{b} with Gaussian-reference curves \code{Phi(z1 * r - b)} for
the given \code{curves} values of \code{r}; (2) two-sided coverage against \code{b} with
curves \code{Phi(z2 * r - b) - Phi(-z2 * r - b)}; (3) observed coverage against
the Gaussian reference at each point's own \verb{(b, r)}, with the diagonal.
Estimators are distinguished by marker shape, the nominal level by a
dashed line, and cells by text labels.
}

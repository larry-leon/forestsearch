% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consistency_resample.R
\name{consistency_resample_compare}
\alias{consistency_resample_compare}
\title{Validate the consistency approximation against literal splitting (Cox)}
\usage{
consistency_resample_compare(
  df,
  hr.consistency = 1,
  R_true = 400L,
  draws = 1000L,
  multiplier = "rademacher",
  tte.name = "Y",
  event.name = "Event",
  treat.name = "Treat",
  cox_init = 0,
  adjust_covariates = NULL,
  seed = NULL
)
}
\arguments{
\item{df}{data.frame/data.table with the relevant outcome columns.}

\item{hr.consistency}{Numeric; survival HR consistency threshold (also the
default GLM threshold when \code{consistency_threshold} is \code{NULL}). Default 1.0.}

\item{R_true}{Integer; number of literal random splits.}

\item{multiplier}{Character; \code{"rademacher"} (default), \code{"normal"},
\code{"poisson"}.}

\item{tte.name, event.name, treat.name}{Character; survival/treatment column
names.}

\item{cox_init}{Numeric; Cox warm-start.}

\item{adjust_covariates}{Character vector or \code{NULL}; adjustment terms.}

\item{seed}{Integer or \code{NULL}; RNG seed for the Monte Carlo draws.}

\item{R_true, draws}{Integers controlling the truth loop and approximation.}
}
\value{
One-row data.frame with \code{n}, \code{d}, \code{beta_hat}, \code{sigma_D},
\code{rate_true}, \code{valid_true}, \code{rate_closed}, \code{rate_mc}, \code{err_closed},
\code{err_mc}.
}
\description{
Calls the package's \code{\link[=run_single_consistency_split]{run_single_consistency_split()}} \code{R_true} times and
compares the Monte Carlo rate to the closed-form and multiplier
approximations from \code{\link[=consistency_resample]{consistency_resample()}}. Survival path only; for the
GLM paths use the standalone GLM validator.
}
\seealso{
\code{\link[=consistency_resample]{consistency_resample()}}, \code{\link[=run_single_consistency_split]{run_single_consistency_split()}}
}

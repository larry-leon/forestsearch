% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_utility_functions.R
\name{analyze_subgroup_glm}
\alias{analyze_subgroup_glm}
\title{Analyze subgroup for GLM outcomes}
\usage{
analyze_subgroup_glm(
  df_sub,
  outcome.name,
  treat.name,
  subgroup_name,
  effect_a = NA,
  estimator_fn = NULL,
  effect_measure = "RD",
  outcome_type = "binary",
  N
)
}
\arguments{
\item{df_sub}{Data frame for the subgroup.}

\item{outcome.name}{Character. Name of outcome variable.}

\item{treat.name}{Character. Name of treatment variable.}

\item{subgroup_name}{Character. Label for this subgroup.}

\item{effect_a}{Character. Adjusted effect estimate string (optional).}

\item{estimator_fn}{Closure from \code{make_effect_estimator()}, or NULL.}

\item{effect_measure}{Character. Effect measure label (e.g., "RD", "OR").}

\item{outcome_type}{Character. One of \code{"binary"}, \code{"continuous"},
or \code{"count"}.  Controls formatting: binary displays per-arm means
as percentages (\code{"Rate"}); continuous and count display raw means
(\code{"Mean"}).  Default \code{"binary"}.}

\item{N}{Integer. Total sample size (for percentage calculation).}
}
\value{
One-row data frame of formatted subgroup results.  Column names
are placeholders (\code{Subgroup}, \code{n}, \code{n1}, \code{rate0},
\code{rate1}, \code{Diff}, \code{effect}, optionally \code{effect_a})
and are overwritten by the calling function
(\code{\link{SG_tab_estimates_glm}}) with the user-facing labels
(e.g., \code{"Rate(C)"} vs \code{"Mean(C)"}, the effect-measure
header).  Returning a data frame -- rather than a named character
vector via \code{c(...)} -- ensures type stability across the ITT
and subgroup call paths and prevents downstream rendering failures
in consumers that go through \code{as.data.frame()} (e.g., gt).
}
\description{
GLM counterpart to \code{\link[=analyze_subgroup]{analyze_subgroup()}}.  Computes per-arm outcome
rates, sample sizes, and the treatment effect estimate (RD, OR, etc.)
using the estimator closure.
}
\keyword{internal}

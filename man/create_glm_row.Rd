% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grf.subg.harm.glm.R, R/grf_subg_harm_glm.R
\name{create_glm_row}
\alias{create_glm_row}
\title{Compute GLM Effect Estimate for a Single Subgroup}
\usage{
create_glm_row(
  df,
  outcome.name,
  treat.name = "treat",
  effect_measure = "log_OR",
  offset.name = NULL,
  overdispersion = "none",
  conf.level = 0.95,
  min_arm_n = 5L,
  min_arm_events = 3L,
  verbose = FALSE
)

create_glm_row(
  df,
  outcome.name,
  treat.name = "treat",
  effect_measure = "log_OR",
  offset.name = NULL,
  overdispersion = "none",
  conf.level = 0.95,
  min_arm_n = 5L,
  min_arm_events = 3L,
  verbose = FALSE
)
}
\arguments{
\item{df}{Data frame for the subgroup (already subset).}

\item{outcome.name}{Character. Name of the outcome column.}

\item{treat.name}{Character. Name of the treatment indicator.}

\item{effect_measure}{Character. Effect measure; see
\code{\link{grf.subg.harm.glm}} for options.}

\item{offset.name}{Character or \code{NULL}. Name of the person-time column.
When supplied, \code{log(offset)} is added as an offset term.
Default: \code{NULL}.}

\item{overdispersion}{Character. One of \code{"none"}, \code{"quasi"},
\code{"negbin"}. Default: \code{"none"}.}

\item{conf.level}{Numeric. Confidence level. Default: \code{0.95}.}

\item{min_arm_n}{Integer. Minimum observations per arm; returns \code{NA}
row if either arm is below this threshold. Default: \code{5L}.}

\item{min_arm_events}{Integer. For binary/count outcomes, minimum events
per arm; returns \code{NA} row if check fails. Default: \code{3L}.}

\item{verbose}{Logical. Default: \code{FALSE}.}
}
\value{
A one-row data frame with columns:
\code{N}, \code{n_treat}, \code{n_control},
\code{events_treat}, \code{events_control} (for binary/count),
\code{est}, \code{lower}, \code{upper}, \code{se}, \code{effect_measure}.

A one-row data frame with columns:
\code{N}, \code{n_treat}, \code{n_control},
\code{events_treat}, \code{events_control} (for binary/count),
\code{est}, \code{lower}, \code{upper}, \code{se}, \code{effect_measure}.
}
\description{
Fits a GLM within the specified subgroup and returns a one-row data frame
containing the effect estimate, confidence interval, sample size, and event
count. Supports binary, continuous, and count (with offset) outcomes.

Fits a GLM within the specified subgroup and returns a one-row data frame
containing the effect estimate, confidence interval, sample size, and event
count. Supports binary, continuous, and count (with offset) outcomes.
}
\seealso{
\code{\link{grf.subg.harm.glm}}
}
\keyword{internal}

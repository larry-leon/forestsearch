% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sg_weighted_km.R
\name{figure_note}
\alias{figure_note}
\title{Generate Figure Note for Quarto/RMarkdown}
\usage{
figure_note(
  x,
  prefix = "*Note*: ",
  include_definition = TRUE,
  include_hr_explanation = TRUE,
  custom_text = NULL
)
}
\arguments{
\item{x}{Output from plot_sg_weighted_km()}

\item{prefix}{Character. Prefix for the note. Default uses italic Note.}

\item{include_definition}{Logical. Include subgroup definition. Default: TRUE}

\item{include_hr_explanation}{Logical. Include HR(bc) explanation. Default: TRUE}

\item{custom_text}{Character. Additional custom text to append. Default: NULL}
}
\value{
Character string formatted as a figure note, or NULL if no content
}
\description{
Formats the figure note from plot_sg_weighted_km() output for use in
Quarto or RMarkdown documents.
}
\examples{
\donttest{
# Setup: run ForestSearch on GBSG breast cancer data
df <- survival::gbsg
df$grade3 <- as.integer(df$grade == "3")
df$id <- seq_len(nrow(df))
fs <- forestsearch(df,
  confounders.name = c("age", "meno", "size", "grade3", "nodes", "pgr", "er"),
  outcome.name = "rfstime", event.name = "status", treat.name = "hormon",
  fs.splits = 100, max_subgroups_search = 5,
  use_twostage = TRUE, details = FALSE, plot.sg = FALSE)

km_result <- plot_sg_weighted_km(fs.est = fs,
  outcome.name = "rfstime", event.name = "status", treat.name = "hormon")
cat(figure_note(km_result))
}

}

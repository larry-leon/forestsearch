% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaHhat_truth.R
\name{fs_attach_betaHhat}
\alias{fs_attach_betaHhat}
\title{Attach \code{beta(Hhat)} targets to a results frame}
\usage{
fs_attach_betaHhat(
  results,
  frame,
  focus,
  outcome_type = c("survival", "binary", "continuous", "count"),
  effect_measure = NULL,
  outcome.name = "y_sim",
  event.name = "event_sim",
  treat.name = "treat_sim",
  sg_def_structs = NULL
)
}
\arguments{
\item{results}{Data frame carrying an \code{sg_def} column.}

\item{frame}{Data frame. The fixed evaluation population.}

\item{focus, outcome_type, effect_measure, outcome.name, event.name, treat.name}{Passed to \code{\link[=fs_betaHhat_one]{fs_betaHhat_one()}}.}

\item{sg_def_structs}{Optional named list of structured GRF definitions.}
}
\value{
\code{results} with \code{betaHhat_H}, \code{betaHhat_Hc}, \code{betaHhat_status} and
\code{nH_eval} / \code{nHc_eval} columns added, and counters attached.
}
\description{
The one call each engine adds to its \code{run_cell()}, immediately before the
bundle is assembled. Runs in the main process, after the replicate loop, so
the evaluation frame stays in one place.
}
\details{
Replicates with no realized rule receive \code{NA} targets, matching the
behaviour of the modules this replaces. Their count is reported as
\code{n_reps_undetected} rather than being folded into the resolved or
unresolved tallies.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_subgroup_sims.R
\name{run_subgroup_sims}
\alias{run_subgroup_sims}
\title{Run the extreme-subgroups simulation study}
\usage{
run_subgroup_sims(
  dgm,
  subgroups,
  n_sims,
  fit = subgroup_cox(survival::Surv(y_sim, event_sim) ~ treat_sim),
  baseline = c("resample", "fixed"),
  n = NULL,
  analysis_time,
  max_entry,
  cens_adjust,
  cutpoints = list(),
  benchmarks = benchmark_spec(),
  min_n = 5L,
  workers = NULL,
  seed_base = 0L,
  rand_seed_offset = 1000000L,
  hr_true = NULL,
  k_treat = NULL,
  future_packages = c("forestsearch", "survival"),
  validate = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{dgm}{A DGM from \code{\link[=generate_aft_dgm_flex]{generate_aft_dgm_flex()}}. For
\code{baseline = "fixed"} it must carry \code{df_source}.}

\item{subgroups}{List of subgroup definitions, each a list with
character scalars \code{id} (an expression over trial columns, exposed
cut-points, \code{flag_itt}, and benchmark columns), \code{name}, and \code{grp}.}

\item{n_sims}{Number of simulated trials.}

\item{fit}{Per-subgroup analysis function \code{data -> c(estimate, upper)};
see \code{\link[=subgroup_cox]{subgroup_cox()}}. Default is an unstratified Cox fit on the
simulated columns; the vignettes pass a grade-stratified formula.}

\item{baseline}{\code{"resample"} (random-X: draw \code{n} patients from the
super-population per trial) or \code{"fixed"} (conditional-on-X: every
trial is the \code{df_source} panel).}

\item{n}{Patients per trial. Required for \code{"resample"}; must be
\code{NULL} for \code{"fixed"}, where N is \code{nrow(dgm$df_source)} by
construction.}

\item{analysis_time, max_entry, cens_adjust}{Passed to
\code{\link[=simulate_from_dgm]{simulate_from_dgm()}} (administrative censoring time, recruitment
window, calibrated censoring shift).}

\item{cutpoints}{Named list of scalars exposed as columns of each
simulated trial so \code{id} expressions like \code{"age <= age_med"}
resolve.}

\item{benchmarks}{A \code{\link[=benchmark_spec]{benchmark_spec()}}, or \code{NULL} to skip benchmark
generation entirely (no RNG is consumed for it).}

\item{min_n}{Subgroups with fewer rows are skipped for that trial
(row stays \code{NA}), default \code{5L} as in the vignettes.}

\item{workers}{Parallel workers. \code{NULL} (default) reuses a
caller-established non-sequential \code{\link[future:plan]{future::plan()}} when one exists
-- no worker-pool spawn or teardown, the mode the vignettes use
after their \code{parallel-setup} chunk -- and otherwise creates a
multisession plan on \code{ceiling(0.90 * availableCores())}, restored
on exit. A numeric value forces a plan for this call (restored on
exit): a fraction in (0, 1) of \code{availableCores()}, an integer
count, or \code{1} for sequential. Results are identical under every
setting (seeding is per-iteration); only wall time differs.
\code{sim_config} records \code{workers}, \code{plan_reused}, and \code{t_plan_secs}.}

\item{seed_base, rand_seed_offset}{Seed scheme; defaults (\code{0L},
\code{1e6L}) reproduce the vignettes.}

\item{hr_true, k_treat}{Optional provenance values stored verbatim in
the result (the vignettes store \code{0.70} and the calibrated
\code{k_treat}).}

\item{future_packages}{Packages loaded on workers, default
\code{c("forestsearch", "survival")} as in the vignettes.}

\item{validate}{Run \code{\link[=validate_subgroups]{validate_subgroups()}} first (default \code{TRUE}).}

\item{verbose}{Print progress lines.}
}
\value{
An object of class \code{"subgroup_sims"}: a named list with
\code{design}, \code{n_sims}, matrices \code{sim_hrs} / \code{sim_ubs} / \code{sim_ns}
(\code{n_sims} x subgroups, columns named by subgroup \code{name}),
\code{subgroups}, \code{sim_config}, \code{cens_adjust}, \code{k_treat}, \code{hr_true},
\code{created}, \code{r_version}, and \code{forestsearch_version}.
}
\description{
Draws \code{n_sims} synthetic trials from a calibrated DGM via
\code{\link[=simulate_from_dgm]{simulate_from_dgm()}}, fits \code{fit} in every pre-defined subgroup of
every trial, and returns the raw result matrices plus full
provenance. With the default \code{fit}, \code{benchmarks}, seeds, and skip
rule, the matrices are bit-identical to those produced by the
\code{extreme_subgroups} vignettes for the matching \code{baseline}, and the
returned object is byte-compatible with their RDS payload:
\code{saveRDS(sims, path)} yields a file the design-comparison memo loads
unchanged.
}
\details{
Per-iteration seeding is explicit (trial \code{ss} uses
\code{seed_base + ss} for the DGM draw and
\code{seed_base + ss + rand_seed_offset} for benchmark membership), so
results are independent of the parallel schedule: any \code{workers}
setting, including sequential, gives identical matrices.
}
\examples{
\dontrun{
sims <- run_subgroup_sims(
  dgm = dgm_uniform, subgroups = subgroups, n_sims = 1000,
  fit = subgroup_cox(survival::Surv(y_sim, event_sim) ~ treat_sim +
                       survival::strata(grade)),
  baseline = "fixed",
  analysis_time = 84, max_entry = 24,
  cens_adjust = cal_uniform$cens_adjust,
  cutpoints = list(age_med = median(gbsg$age)),
  hr_true = 0.70, k_treat = k_treat_uniform
)
saveRDS(sims, "results/extreme_sims_fixed_1000_payload.rds")
}
}
\seealso{
\code{\link[=summary.subgroup_sims]{summary.subgroup_sims()}}, \code{\link[=benchmark_spec]{benchmark_spec()}},
\code{\link[=subgroup_cox]{subgroup_cox()}}, \code{\link[=validate_subgroups]{validate_subgroups()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_mr_inference.R
\name{fs_mr_inference}
\alias{fs_mr_inference}
\title{Multiplier resampling (MR) for a selected forestsearch subgroup}
\usage{
fs_mr_inference(
  df,
  candidates,
  spec,
  selected_members,
  admission,
  t_confirm = NULL,
  confirm_rule = c("point", "ci"),
  reselection = c("maxcons", "maxeff", "maxSG", "minSG", "effMaxSG", "effMinSG"),
  effect_neighborhood = 0.1,
  selection_rule = c("neighborhood", "pareto", "both"),
  draws = 2000L,
  multiplier = c("poisson", "gaussian", "rademacher"),
  include_complement = FALSE,
  ci_method = c("field", "ij", "wald"),
  seed = NULL,
  return_reselection = TRUE,
  field_R_out = 1000L,
  field_R_in = 500L,
  field_uniform = FALSE,
  field_M_cap = NULL,
  field_complement = TRUE,
  field_decompose = FALSE,
  field_scale_complement = c("selected", "none"),
  ij_residual = c("two_term", "winner", "winner_floor")
)
}
\arguments{
\item{df}{Analysis data frame (the standardized \code{df.fs} inside
\code{forestsearch()}); must contain the columns named in \code{spec}.}

\item{candidates}{Named list of integer row-index vectors, one per screened
candidate subgroup (the family the selection rule chose among).}

\item{spec}{List with \code{outcome_type}, \code{effect_measure}, \code{treat.name},
\code{outcome.name}, \code{event.name}, \code{offset.name}, \code{adjust_covariates},
\code{adverse_outcome}.}

\item{selected_members}{Integer row indices of the observed selected subgroup
(\code{which(grp.consistency$sg.harm.id == 1)}).}

\item{admission}{The resolved admission set, as returned by
\code{.fs_resolve_admission()}: a list with \code{effect_floor} (numeric on the
\strong{comparison scale} -- log for ratio measures -- or \code{NULL}) and
\code{consistency} (a list with \code{c_cons} and \code{p_star}, or \code{NULL}).

This replaces the former \code{c_screen} / \code{c_consistency} / \code{p_star}
arguments, from which MR used to rebuild
\code{t_g <- pmax(c_screen, c_consistency + z * sigma_D)} at this site. That
reconstruction was the defect: the identifier resolved its floors in one
place and MR re-derived them in another, so the two could disagree about
which candidates were ever in contention -- and under
\code{sg_focus = "maxeff"} they did. MR is a linearization of the selection
\emph{map}, which is a ranking and a domain; passing the resolved domain makes
the two equal by construction.

\code{NULL} means the floor does not apply, and is handled by a separate code
path rather than by sentinel arithmetic. With neither floor present there
is no admission filter at all.}

\item{t_confirm}{Harm-confirmation threshold on the \strong{effect scale} (HR/OR/
RR/IRR, or RD/MD for differences -- not the working log scale). \code{NULL} uses
the near-null default: \code{1} for ratio measures, \code{0} for differences. Set it
near the null rather than at the screening threshold, because the
de-biasing correction over-shrinks true effects.}

\item{confirm_rule}{Which harm-confirmation rule to apply to the de-biased
estimate: \code{"point"} requires the de-biased point estimate to clear
\code{t_confirm}; \code{"ci"} requires the one-sided 95\% selection-adjusted lower
bound to clear it, and is therefore the stricter of the two.}

\item{reselection}{Bootstrap re-selection rule for the bias term; default
\code{"maxcons"} (validated). \code{"effMaxSG"} etc. are available but approximate.}

\item{effect_neighborhood}{Band for the \code{eff*SG} re-selection rules.}

\item{draws, multiplier, seed}{Multiplier-bootstrap controls. \code{multiplier}
defaults to \code{"poisson"} (mimics the nonparametric bootstrap).}

\item{include_complement}{Logical.  When \code{TRUE}, also de-bias the complement
subgroup (everyone not in the selected subgroup).  The complement is not
selected independently -- its bias is induced by selecting the subgroup --
so on each multiplier draw the complement of the re-selected winner is
tracked and de-biased with the complement's own influence.  Complements are
fit only for candidates that win across draws (plus the selected one), so
the extra cost is small.  Default \code{FALSE}.}

\item{ci_method}{\code{"field"} (default, the recommendation) computes everything
the \code{"ij"} path computes -- the \code{debiased} element, its IJ SE and its IJ
intervals are identical, and are returned in every call regardless of this
argument -- and \strong{additionally} runs the field-calibrated interval, whose
one-sided products are the certified ones (the lower bound on the harm
subgroup, the studentized complement's upper bound, and the Bonferroni
joint pair).  Nothing is removed by choosing it; it adds a per-fit Monte
Carlo cost (\code{field_R_out} x \code{field_R_in}, defaults 1000 / 500, plus the
complement's own block under \code{field_complement = TRUE}).  \code{"ij"} restores
the previous default: the \strong{de-biased} CI from the infinitesimal-jackknife
variance (Leon et al. 2024, Eq. VInfJ_bc), computed from the same
multiplier draws -- the leading-order analogue of the FB interval -- with
the field block omitted.  \code{"wald"} uses the subgroup robust SE
(\code{sigma_D}) for the de-biased CI and also omits the field block.  The
naive CI always uses the robust SE.  The field block (method proposal,
\verb{dev/tasks/TASK_mr_field_vs_guohe_2026-09-05.md}) is returned as a \code{field}
element: Gaussian-multiplier perturbations of the shrunk candidate-effect
field (\code{w = beta_hat} with the winner's entry replaced by the de-biased
estimate) are pushed through the configured re-selection map, with an
inner Monte Carlo estimating the selection drift, and the resulting
\verb{Lambda*} distribution is inverted around the de-biased estimate
(basic-bootstrap form).  Drawn under \code{seed + 900000L} when \code{seed} is
given, after -- never inside -- the main multiplier stream, so \code{"ij"} and
\code{"wald"} output is unaffected byte-for-byte.}

\item{return_reselection}{Logical. When \code{TRUE}, the return list gains a
\code{reselection} element exposing the per-draw re-selection already computed
for the bias term: \code{winner} (integer vector of length \code{draws}, the index
of the re-selected candidate in the kept family on each draw, \code{NA} where
no draw winner exists) and \code{p_hat} (named numeric over the kept family,
\code{tabulate(winner) / draws} -- so \code{sum(p_hat) == selection_rate}, and the
frequencies sum to 1 exactly when every draw produced a winner). Default
\code{TRUE}: the re-selection diagnostics are part of the recommended output
(they carry the stable-pick frequency the coverage caveat is read by).
Nothing in the arithmetic depends on this switch, so \code{FALSE} reproduces
the previous, smaller return object exactly.}

\item{field_R_out, field_R_in}{Outer and inner Monte Carlo sizes for
\code{ci_method = "field"} (defaults 1000 / 500); ignored otherwise.  The
inner draws are shared across outer draws.}

\item{field_uniform}{Logical (default \code{FALSE}); only consulted under
\code{ci_method = "field"}.  When \code{TRUE}, after the field block completes the
gate additionally runs the uniform (kappa) calibration
(\code{fs_mr_field_uniform()}, method proposal
\verb{dev/tasks/TASK_mr_field_uniform_2026-09-05.md}) and attaches its result
as \code{field$uniform}: the smallest widening factor \code{kappa} in \verb{[1, 2]}
such that the widened two-sided interval attains 95\% coverage uniformly
over the \strong{winner-profile protection family} -- hypothetical true
fields equal to the shrunk field \code{w} with the winner's entry set to the
runner-up level plus \code{delta} winner-SEs, \code{delta} on a 0-4 grid --
computed from the trial's own influence structure (\code{Sigma_hat} via
\code{B_eff}), restricted to the \strong{mass-carrying candidate set} (smallest
\code{M <= 12} candidates holding >= 99\% of the re-selection mass, winner
always included).  Guarantee as documented there: the one-sided field
bound is uniformly valid; the two-sided interval widened by the
reported \code{kappa} is uniformly valid over the winner-profile family; the
plain quantile interval is approximate.  The sweep draws under
\code{seed + 910000L} after -- never inside -- the field's own stream, so
every other output, the field block included, is byte-identical whether
or not it runs.}

\item{field_M_cap}{Optional override of the uniform sweep's mass-carrying
cap (\code{fs_mr_field_uniform()}'s \code{M_cap}); \code{NULL} (default) uses that
function's default.  Pass a value at least the family size to lift the
cap to the full family (the GBSG frozen-intervals illustration does).
Consulted only when \code{field_uniform = TRUE}; add-only, default-inert.}

\item{field_complement}{Logical (default \code{TRUE}, the recommended
construction); consulted only under
\code{ci_method = "field"} with \code{include_complement = TRUE}.  When \code{TRUE},
after the harm field block completes, the same field procedure is
evaluated for the complement subgroup (method proposal
\verb{dev/tasks/TASK_mr_field_complement_2026-09-06.md}) and attached as
\code{field$complement}.  \strong{Orientation:} selection acts on the harm-subgroup
effects; the complement is its by-product, and its naive estimate is
pushed toward \emph{benefit}.  For a benefit claim ("the effect in the
complement is at most U") the exposed limit is therefore the \strong{upper}
bound on the complement's working-scale effect, and the primary product
here is the one-sided 95\% upper bound \code{upper_1s}.  The guarantee is the
mirror of the harm side's: an upper bound is vulnerable to
under-correction of the beneficial optimism (U too low over-claims) and
immune to over-correction.  Selection is the harm field's own -- every
outer and inner draw's winner is re-read, never re-selected -- and the
complement's perturbations use the harm field's own multipliers projected
through the complement's influence, so its noise carries its correct
correlation with every candidate's noise; complements of draw winners the
multiplier stage did not fit are fit lazily.  The block draws nothing, so
every other output -- the harm \code{field} block and its \code{uniform} sub-block
included -- is byte-identical whether or not it runs, so \code{FALSE}
reproduces the pre-complement output exactly.}

\item{field_decompose}{Logical (default \code{FALSE}); consulted only when the
complement field block runs.  When \code{TRUE}, \code{field$complement} gains a
\code{decomp_fields} list of add-only scale diagnostics (the selected
complement's influence-norm scale, the draw-weighted mean and CV of the
outer winners' scales, their ratio \code{scale_ratio_c}, and the variance /
covariance of the two pieces of \code{Lambda*c}; PROPOSAL_complement_field_scale
2026-09-08 v2, Stage 1); it reads existing objects only, so every
pre-existing output is unchanged whether or not it runs.}

\item{field_scale_complement}{\code{"selected"} (default, the recommended
construction: the studentized complement field, "field-s") or \code{"none"};
consulted only when the complement field block runs.  Under \code{"selected"} the
complement's field readings are studentized to the selected complement's
own scale before differencing -- each outer reading \verb{zeta^c_\{r, G_r\}} is
multiplied by \verb{s_sel / s_\{G_r\}} and each inner reading by \verb{s_sel / s_\{G(v_r + zeta'_j)\}}, with \code{s_g = sqrt(sum(Bc[, g]^2))} the candidate's
complement influence-norm scale (the percentile-t root; PROPOSAL_complement_
field_scale_2026-09-08_v2 s5, variant R1) -- and \code{field$complement} gains
the \verb{_s} companions of its bounds (\code{est2_s}, \code{upper_1s_s}, \code{lower_1s_s},
\code{lower_2s_s}, \code{upper_2s_s}, \code{lower_se_s}, \code{upper_se_s}, \code{se_field_s},
\code{lambda_mean_s}) with \code{field$joint_s} beside \code{field$joint}.  Add-beside:
no existing field changes under either setting; the default reproduces
prior output exactly.}

\item{ij_residual}{Which IJ residual populates the \strong{reported} de-biased
SE and interval (\code{debiased$se_ij}/\code{lower}/\code{upper}/\code{lower_1s}, and the
complement's), method proposal
\verb{dev/tasks/TASK_complement_refinements_2026-09-06.md}.  \code{"two_term"}
(default) is the paper's residual
\eqn{r_b = (bias_{sel} + bias_{fix}) - D_{H^*_b}(b) - D_{\hat H}(b)},
whose limit variance is \eqn{(p + e_{\hat H})' \Sigma (p + e_{\hat H})} --
\eqn{4\sigma^2} when one candidate dominates, a doubled SE exactly where
there is least to correct (the complement is effectively always in that
regime).  \code{"winner"} uses the winner-only (total-derivative) residual
\eqn{r^w_b = bias_{sel} - D_{H^*_b}(b)}, exact when one candidate
dominates but under-covering at ties on the harm side (the delta method
does not hold in the local regime); \code{"winner_floor"} floors it at the
naive robust variance, \eqn{\max(V^w, \hat\sigma^2_D)}, which repairs the
tie regime at zero cost.  All three SEs are computed from the same draws
and returned side by side in every call (\code{se_ij_two_term},
\code{se_ij_winner}, \code{se_ij_winner_floor} with their bounds \verb{*_w} / \verb{*_wf});
the argument only selects the reported one, and the point estimates
never change.  Add-only: the default reproduces prior output exactly
(the new elements aside).}
}
\value{
List with the selected index/label, \code{naive} and \code{debiased} estimates
(effect scale, with approximate 95\% CIs), \code{selection_bias}, \code{fixed_bias},
\code{selection_rate}, \code{mean_r}, \code{mean_r_c}, the \code{settings} actually used (\code{t_confirm},
\code{confirm_rule}, \code{reselection}, \code{selection_rule}, \code{multiplier}, \code{draws}),
\code{harm_flag}, family/subgroup sizes, and \code{timing_seconds}. The \code{debiased}
element carries \code{se_ij}, \code{se_wald}, \code{var_ij}, and \code{ij_source}; its CI uses
the IJ SE under \code{ci_method = "field"} (the default) and \code{"ij"} (the FB
analogue in both cases) and the robust SE under \code{"wald"}.
When \code{include_complement = TRUE}, a \code{complement} element carries the
complement subgroup's \code{naive}/\code{debiased} estimates and bias terms in the
same form, including its own IJ variance.  The complement's de-biased CI
follows the same SE convention as the winner's: the IJ SE under
\code{ci_method = "ij"} and \code{"field"} (under \code{"field"} the complement, like the
\code{debiased} element, is identical to the \code{"ij"} output), the robust SE
under \code{"wald"}.

\code{mean_r} is the mean of the IJ residual \eqn{r_b} over \code{ok_H} -- exactly
the draws entering the variance, not all \code{draws}, since on an excluded
draw \eqn{r_b} is not a meaningful quantity. \strong{The invariant is that it is
zero by construction whenever both bias terms share a denominator}, which
is the convention the package implements: \code{selection_bias} and
\code{fixed_bias} both average over the draws that produced a winner, and the
IJ runs on that same set. A non-zero value therefore means the two terms
are being normalised differently somewhere -- the defect corrected in
\code{dad0415}, whose signature was precisely a non-zero residual mean.
\code{mean_r_c} is the same quantity for the complement over \code{use_c}, and is
\code{NA_real_} when no complement was fit. Both are diagnostics of the
correction's internal consistency rather than properties of the estimate,
which is why they sit beside \code{selection_rate} and not inside \code{debiased}.

Note the mixed scales in the flat \code{debiased} list: \code{est}/\code{lower}/\code{upper}/
\code{lower_1s} are on the \strong{effect} scale, while \code{se}/\code{se_ij}/\code{se_wald}/
\code{var_ij} and the two bias terms are on the \strong{working} (log, for ratio
measures) scale.

When the selected subgroup cannot be fit in the reconstructed family the
return is a short variant carrying only \code{selected_index} (\code{NA}),
\code{selected_label}, \code{harm_flag} (\code{NA}), \code{settings}, \code{note} and \code{n_family};
consumers must tolerate that shape.

Under \code{return_reselection = TRUE} the full return additionally carries a
\code{reselection} element (see that argument); the short variant never does.

Under \code{ci_method = "field"} the full return additionally carries a
\code{field} element: \code{lambda_mean}, \code{lambda_sd}/\code{se_field} and the
\verb{Lambda*} quantiles \code{q05/q25/q50/q75/q95/q025/q975} on the \strong{working}
scale; \code{n_out_used}, \code{n_in_used_mean}, \code{R_out}, \code{R_in}, \code{seed_offset},
\code{timing_seconds}; and, on the \strong{effect} scale (the \code{debiased}
convention), the second-order point estimate \code{est2} (de-biased estimate
minus \code{lambda_mean}), the primary one-sided 95\% bound \code{lower_1s}
(de-biased minus \code{q95}), the two-sided quantile interval
\code{lower_2s}/\code{upper_2s}, and the supplementary SE-type interval
\code{lower_se}/\code{upper_se} around \code{est2}.

Under \code{field_complement = TRUE} (with \code{include_complement = TRUE}) the
\code{field} element additionally carries \code{complement}, the complement's own
field block in the same form and scales, inverted around the complement's
two-term de-biased estimate: \code{lambda_mean}, \code{lambda_sd}/\code{se_field}, the
seven \code{Lambda*c} quantiles; on the effect scale \code{est2}, the \strong{primary}
one-sided 95\% upper bound \code{upper_1s} (de-biased minus \code{q05}), the
one-sided lower bound \code{lower_1s}, the two-sided \code{lower_2s}/\code{upper_2s}
and the SE-type \code{lower_se}/\code{upper_se}; the draw accounting
\code{n_out_used}, \code{n_in_used_mean}, \code{n_out_dropped_unfit}; the fit accounting
\code{n_complement_fits} (distinct complement fits, the multiplier stage's
included), \code{n_new_fits}, \code{share_draws_new_fit} (share of outer + inner
draw-winner readings whose candidate the multiplier stage had not fit);
\code{R_out}, \code{R_in}, \code{timing_seconds}.  A \code{note} replaces the numbers when
the selected complement is unfit or fewer than 2 outer draws survive.

With the complement field on, \code{field} also carries \code{joint}: the
simultaneous (harm lower, complement upper) pair from the aligned outer
draws \eqn{(\Lambda^*_r, \Lambda^{*c}_r)} -- same multipliers, same
winners, no new draws.  \code{gamma} is the equal-tail level (grid from
\code{alpha} down to \code{alpha / 2}, step 0.001; the largest value whose joint
probability \eqn{P^*(\Lambda^* \le q_{1-\gamma}, \Lambda^{*c} \ge
  q_\gamma) \ge 1 - \alpha}), \code{joint_prob} the achieved probability,
\code{lower_H}/\code{upper_Hc} the calibrated pair (effect scale),
\code{bonf_lower_H}/\code{bonf_upper_Hc} the Bonferroni pair at \code{gamma = alpha/2}
with its \code{bonf_joint_prob}, \code{corr} the draws' correlation,
\code{n_joint_draws}, and the \code{grid_gamma}/\code{grid_joint_prob} profile.  The
marginal one-sided bounds are unchanged.  The top-level \code{ij_residual}
records the residual that populated the reported IJ SEs.
}
\description{
Computes a multiplier-bootstrap approximation of the bootstrap
bias-corrected treatment effect for the selected subgroup and flags whether
it is still consistent with harm.  Reuses the resample consistency engine's
per-candidate treatment \code{dfbeta}; see the file header for the closed form.
}
\details{
This is \strong{post-selection inference on a completed analysis}: it runs after
\code{\link[=forestsearch]{forestsearch()}} has chosen the subgroup and cannot change that choice.  It
is the fast, refit-free counterpart of the full bootstrap (FB,
\code{\link[=forestsearch_bootstrap_dofuture]{forestsearch_bootstrap_dofuture()}}), which re-runs the entire search in
every replicate; MR perturbs the influence contributions of a single fit
instead.  MR approximates FB to leading order and does not replace it.
}
\section{Alignment is assumed, not checked here}{

This is an engine-level entry point. Its arguments are a candidate family,
a specification, and a selected membership vector -- the identifier
configuration that produced them is not visible, so the alignment
conditions MR requires (selection ranking on the inferential coefficient
\eqn{\hat\beta(g)}, and a fixed candidate family) cannot be verified at
this level and are assumed to have been established upstream.

They are enforced by \code{.validate_mr_configuration()} at the three
configuration-visible entry points -- \code{\link[=forestsearch]{forestsearch()}} under
\code{mr_inference = TRUE}, and \code{\link[=forestsearch_bootstrap_dofuture]{forestsearch_bootstrap_dofuture()}} and
\code{\link[=forestsearch_Kfold]{forestsearch_Kfold()}} under \code{mr_in_replicates = TRUE}. Calling
\code{fs_mr_inference()} directly bypasses those guards: a family ranked on
DINA's native tau-hat, on GRF's doubly-robust score, or on a GRF
policy-tree objective will still produce numbers, but they do not de-bias
the reported effect.
}

\seealso{
\code{\link[=forestsearch]{forestsearch()}} for the \code{mr_inference} switch and the
vocabulary section; \code{\link[=mr_estimates_table]{mr_estimates_table()}} to render the result;
\code{\link[=forestsearch_bootstrap_dofuture]{forestsearch_bootstrap_dofuture()}} for the full bootstrap (FB) this
approximates; \code{\link[=fs_fdr_report]{fs_fdr_report()}} which sweeps \code{c_confirm} thresholds.
}
\keyword{internal}

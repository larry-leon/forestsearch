% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cox_ahr_cde_wrapper.R
\name{cox_ahr_cde_analysis}
\alias{cox_ahr_cde_analysis}
\title{Comprehensive Wrapper for Cox Spline Analysis with AHR and CDE Plotting}
\usage{
cox_ahr_cde_analysis(
  df,
  tte_name = "os_time",
  event_name = "os_event",
  treat_name = "treat",
  z_name = "biomarker",
  loghr_po_name = "loghr_po",
  theta1_name = "theta_1",
  theta0_name = "theta_0",
  spline_df = 3,
  alpha = 0.2,
  hr_threshold = 0.7,
  plot_style = c("combined", "separate", "grid"),
  plot_select = c("all", "profile_ahr"),
  save_plots = FALSE,
  output_dir = "plots",
  verbose = TRUE
)
}
\arguments{
\item{df}{Data frame containing survival data with potential outcomes.}

\item{tte_name}{Character string specifying time-to-event variable name.
Default: \code{"os_time"}.}

\item{event_name}{Character string specifying event indicator variable
name. Default: \code{"os_event"}.}

\item{treat_name}{Character string specifying treatment variable name.
Default: \code{"treat"}.}

\item{z_name}{Character string specifying continuous covariate/biomarker
name. Default: \code{"biomarker"}.}

\item{loghr_po_name}{Character string specifying potential outcome log HR
variable. Default: \code{"loghr_po"}.}

\item{theta1_name}{Optional: variable name for theta_1 (treated potential
outcome). Default: \code{"theta_1"}.}

\item{theta0_name}{Optional: variable name for theta_0 (control potential
outcome). Default: \code{"theta_0"}.}

\item{spline_df}{Integer degrees of freedom for spline fitting. Default: 3.}

\item{alpha}{Numeric significance level for confidence intervals.
Default: 0.20.}

\item{hr_threshold}{Numeric hazard ratio threshold for subgroup
identification, or \code{NULL} to suppress threshold-based elements.
When \code{NULL}, plots omit the HR threshold line, optimal cutpoint
line, and subgroup-dependent panels (Plots 5--6); subgroup statistics
report only the overall population. Default: 0.7.}

\item{plot_style}{Character: \code{"combined"}, \code{"separate"}, or
\code{"grid"} for plot layout. Default: \code{"combined"}.}

\item{plot_select}{Character controlling which panels to display:
\code{"all"} (default) shows the full grid layout; \code{"profile_ahr"}
shows only the treatment effect profile (top-left) and the AHR curve
for \eqn{z \geq} threshold (top-middle) side by side, with the AHR
panel's y-axis scaled to the data range.}

\item{save_plots}{Logical whether to save plots to file. Default: FALSE.}

\item{output_dir}{Character directory for saving plots. Default:
\code{"plots"}.}

\item{verbose}{Logical for diagnostic output. Default: TRUE.}
}
\value{
List of class \code{"cox_ahr_cde"} containing:
\describe{
\item{cox_fit}{Results from \code{\link{cox_cs_fit}} function.}
\item{ahr_results}{AHR calculations for different subgroup definitions.}
\item{cde_results}{CDE calculations if theta variables available.}
\item{optimal_cutpoint}{Optimal biomarker cutpoint, or \code{NULL} when
\code{hr_threshold} is \code{NULL}.}
\item{subgroup_stats}{Statistics for recommended and questionable
subgroups, or overall-only when \code{hr_threshold} is \code{NULL}.}
\item{data}{List with z_values, loghr_po, and subgroup assignments.}
}
}
\description{
This wrapper function combines Cox spline fitting with comprehensive
visualization of Average Hazard Ratios (AHRs) and Controlled Direct
Effects (CDEs) as described in the MRCT subgroups analysis documentation.
}
\examples{
\dontrun{
# With threshold - full subgroup analysis
results <- cox_ahr_cde_analysis(
  df = df_large, z_name = "z_bm",
  hr_threshold = 1.25, plot_style = "grid"
)

# Without threshold - pure AHR/CDE curves
results <- cox_ahr_cde_analysis(
  df = df_large, z_name = "z_bm",
  hr_threshold = NULL, plot_style = "grid"
)

# Compact two-panel without threshold
results <- cox_ahr_cde_analysis(
  df = df_large, z_name = "z_bm",
  hr_threshold = NULL,
  plot_select = "profile_ahr"
)
}

}

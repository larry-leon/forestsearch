% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subgroup_search.R
\name{fit_glm_for_subgroup}
\alias{fit_glm_for_subgroup}
\title{Fit GLM for Subgroup via Estimator Closure}
\usage{
fit_glm_for_subgroup(df_clean, id.x, estimator_fn)
}
\arguments{
\item{df_clean}{Data frame.  The analysis data, row-aligned with the
cleaned vectors.}

\item{id.x}{Integer vector.  1 = subject in this subgroup, 0 = not.}

\item{estimator_fn}{Closure from \code{make_effect_estimator()}.}
}
\value{
A list with components \code{hr}, \code{lower}, \code{upper}, \code{med0}, \code{med1},
or \code{NULL} on failure.
}
\description{
Subsets the analysis data frame to the candidate subgroup and calls the
pre-built estimator closure.  Returns a list compatible with
\code{\link[=create_result_row]{create_result_row()}} so the downstream pipeline is unchanged.
}
\details{
For binary outcomes with \code{effect_measure = "RD"}, the \code{hr} slot contains
the risk difference (not a hazard ratio) — the name is retained for
pipeline compatibility.  Similarly, \code{lower}/\code{upper} are Wald-type CI
bounds and \code{med0}/\code{med1} are \code{NA}.
}
\keyword{internal}

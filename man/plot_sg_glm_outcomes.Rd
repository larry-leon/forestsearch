% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sg_glm_outcomes.R
\name{plot_sg_glm_outcomes}
\alias{plot_sg_glm_outcomes}
\alias{print.fs_binary_plot}
\alias{plot.fs_binary_plot}
\title{Plot GLM Subgroup Outcomes}
\usage{
plot_sg_glm_outcomes(
  fs.est,
  fs_bc = NULL,
  outcome.name,
  treat.name,
  offset.name = NULL,
  effect_measure = NULL,
  outcome_type = NULL,
  adjust_covariates = NULL,
  sg0_name = "Questionable",
  sg1_name = "Recommend",
  E.name = "Treatment",
  C.name = "Control",
  bar_colors = c("steelblue", "coral"),
  show_effect = TRUE,
  show_bc = TRUE,
  show_itt = TRUE,
  conf.level = 0.95,
  title = NULL,
  subtitle = NULL,
  verbose = FALSE
)

\method{print}{fs_binary_plot}(x, ...)

\method{plot}{fs_binary_plot}(x, ...)
}
\arguments{
\item{fs.est}{A forestsearch object containing \code{df.est} with
\code{treat.recommend} column.}

\item{fs_bc}{Optional. Bootstrap results from
\code{\link{forestsearch_bootstrap_dofuture}} for bias-corrected
annotations.}

\item{outcome.name}{Character. Name of outcome column.}

\item{treat.name}{Character. Name of treatment column.}

\item{offset.name}{Character or \code{NULL}. Name of follow-up time
column for rate-based measures (IRR, IRD).}

\item{effect_measure}{Character or \code{NULL}. Effect measure.
Auto-detected from \code{fs.est$effect_measure} if \code{NULL}.}

\item{outcome_type}{Character or \code{NULL}. One of \code{"binary"},
\code{"continuous"}.  Auto-detected from \code{fs.est$outcome_type}
if \code{NULL}.}

\item{adjust_covariates}{Character vector or \code{NULL}. Baseline
covariates to adjust the plotted effect model for, matching the
adjustment used during search/estimation.  When \code{NULL}, resolved
from \code{fs.est$args_call_all$adjust_covariates} so the plot reflects
the same (adjusted or unadjusted) scale as the fit.  Columns must be
present in \code{fs.est$df.est}.}

\item{sg0_name}{Character. Label for H subgroup (treat.recommend == 0).
Default: \code{"Questionable"}.}

\item{sg1_name}{Character. Label for Hc subgroup (treat.recommend == 1).
Default: \code{"Recommend"}.}

\item{E.name}{Character. Label for experimental/treatment arm.
Default: \code{"Treatment"}.}

\item{C.name}{Character. Label for control arm.
Default: \code{"Control"}.}

\item{bar_colors}{Character vector of length 2. Colors for control and
treatment bars.  Default: \code{c("steelblue", "coral")}.}

\item{show_effect}{Logical. Annotate with effect estimate.  Default
\code{TRUE}.}

\item{show_bc}{Logical. Annotate with bias-corrected estimate when
\code{fs_bc} is provided.  Default \code{TRUE}.}

\item{show_itt}{Logical. Include an ITT (full population) panel.
Default \code{TRUE}.}

\item{conf.level}{Numeric. Confidence level for intervals.
Default \code{0.95}.}

\item{title}{Character or \code{NULL}. Plot title.}

\item{subtitle}{Character or \code{NULL}. Plot subtitle.}

\item{verbose}{Logical. Print diagnostic messages. Default \code{FALSE}.}

\item{x}{An fs_binary_plot object.}

\item{...}{Additional arguments (unused).}
}
\value{
A list of class \code{"fs_binary_plot"} containing:
\describe{
\item{plot}{A ggplot2 object.}
\item{data}{Data frame of per-arm summaries used for the plot.}
\item{effects}{Data frame of effect estimates per subgroup.}
\item{sg_definition}{Character. Subgroup definition label.}
\item{figure_note}{Character. Auto-generated figure note.}
}
}
\description{
Creates a publication-ready visualization of identified subgroup outcomes
for binary or continuous endpoints.  This is the GLM counterpart to
\code{\link{plot_sg_weighted_km}} for survival endpoints.
}
\details{
For binary outcomes, displays grouped bar charts of event rates by
treatment arm within each subgroup.  For continuous outcomes, displays
point-and-errorbar charts of means (+/- SE) by treatment arm.  Both
are annotated with the effect estimate and optional bias-corrected
estimate from the bootstrap.
}
\examples{
\dontrun{
fs <- forestsearch(df, outcome_type = "binary", effect_measure = "OR",
                   ...)
result <- plot_sg_glm_outcomes(fs, outcome.name = "status",
                                treat.name = "hormon")
print(result)
}

}

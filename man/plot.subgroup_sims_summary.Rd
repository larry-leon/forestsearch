% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_subgroup_sims.R
\name{plot.subgroup_sims_summary}
\alias{plot.subgroup_sims_summary}
\title{Forest plot of an extreme-subgroups summary}
\usage{
\method{plot}{subgroup_sims_summary}(
  x,
  metric = c("hr", "ub"),
  panel = c("single", "combo", "highrisk"),
  hr_true = x$hr_true,
  base_size = 14,
  point_size = 2.2,
  line_size = 0.75,
  widths = c(3.5, 5, 1.1, 1.2, 1.2, 1.1),
  clip_marker = "arrow",
  xlim = NULL,
  ticks_at = NULL,
  xlab = NULL,
  footnote = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{"subgroup_sims_summary"} object.}

\item{metric}{\code{"hr"} (HR point estimate) or \code{"ub"} (upper 95\% bound).}

\item{panel}{\code{"single"}, \code{"combo"}, or \code{"highrisk"}.}

\item{hr_true}{True hazard ratio for the reference/vertical line and
footnotes; defaults to the value stored on \code{x} and must be
non-\code{NULL}.}

\item{base_size, point_size, line_size}{Passed to \code{\link[=gg_forest]{gg_forest()}};
defaults match the vignettes' \verb{fp_*} values.}

\item{widths}{Column width vector passed to \code{\link[=gg_forest]{gg_forest()}}.}

\item{clip_marker}{Passed to \code{\link[=gg_forest]{gg_forest()}}; \code{"arrow"} marks whiskers
clipped at the axis limits.}

\item{xlim, ticks_at, xlab, footnote}{\code{NULL} (default) selects the
vignette value for the chosen \code{metric}/\code{panel}.}

\item{...}{Further arguments merged over the defaults and passed to
\code{\link[=gg_forest]{gg_forest()}}.}
}
\value{
The \code{\link[=gg_forest]{gg_forest()}} plot object.
}
\description{
Draws one of the vignettes' six forest panels from a
\code{\link[=summary.subgroup_sims]{summary.subgroup_sims()}} object via \code{\link[=gg_forest]{gg_forest()}}: the empirical-CI
distribution of the HR point estimate or of the upper 95\% bound
UB(HR), for the single-variable panel, the combination + random-
benchmark panel, or the high-risk panel (Pr(UB>=2) filter with the
ITT row anchored first).
}
\details{
Defaults reproduce the committed vignette figures exactly:
metric-specific axis limits, tick positions, reference/vertical line
styling, the four annotation columns (N plus tail probabilities and
the unconditional median), and the panel-specific footnotes. Any
default can be overridden through the named arguments or \code{...},
which is merged over the assembled argument list before the
\code{\link[=gg_forest]{gg_forest()}} call (so e.g. \code{ref_col = "black"} or
\code{widths = ...} in \code{...} win).

Summaries carrying effect metadata (the generic path of
\code{\link[=summary.subgroup_sims]{summary.subgroup_sims()}}, e.g. from \code{\link[=subgroup_glm]{subgroup_glm()}} fits) take a
scale-aware branch instead: identity-scale measures (MD) plot with
\code{xlog = FALSE} and the null line at 0, ratio-scale measures (OR)
with \code{xlog = TRUE} and the null line at 1, and both delegate
limits/ticks to \code{\link[=gg_forest]{gg_forest()}}'s data-driven defaults (the HR panel
constants can hide binary medians and remain available via
\verb{xlim=}/\verb{ticks_at=}); annotation
columns and footnotes are built from the resolved labels and
threshold strings (legacy literals preserved when a pair equals the
legacy pair), and columns for disabled (\code{NA}) thresholds are dropped
(the default \code{widths} shrink to match; an explicit \code{widths} is used
as given). \code{hr_true} is interpreted on the fitter's estimate scale
throughout. Legacy summaries produce byte-identical \code{\link[=gg_forest]{gg_forest()}}
calls.
}
\examples{
\dontrun{
S <- summary(sims_uniform, hr_true = 0.70)
plot(S, metric = "ub", panel = "combo")
}
}
\seealso{
\code{\link[=forest_height]{forest_height()}}, \code{\link[=summary.subgroup_sims]{summary.subgroup_sims()}}
}

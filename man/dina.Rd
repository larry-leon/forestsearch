% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dina_df.R
\name{dina}
\alias{dina}
\title{Estimate heterogeneous treatment effects via DINA (data-frame interface)}
\usage{
dina(
  df,
  outcome,
  treatment,
  covariates,
  family = c("gaussian", "binomial", "poisson", "cox"),
  status = NULL,
  ...
)
}
\arguments{
\item{df}{data frame (or tibble / \code{data.table}) containing the
outcome, treatment, and covariate columns.}

\item{outcome}{character(1); the name of the response column in
\code{df}.  For \code{family = "cox"} this is the event/censoring time
column.}

\item{treatment}{character(1); the name of the binary treatment
indicator column in \code{df}.  Values must be 0/1 or logical.}

\item{covariates}{character vector of length \verb{>= 1}; the names of
the covariate columns in \code{df}.  All must be numeric.}

\item{family}{one of \code{"gaussian"}, \code{"binomial"}, \code{"poisson"}, \code{"cox"}.}

\item{status}{character(1) or \code{NULL}; for \code{family = "cox"} only, the
name of the event indicator column in \code{df} (\code{1} = event, \code{0} =
censored).  Ignored for non-Cox families.}

\item{...}{further arguments forwarded to \code{\link[=dina_fit]{dina_fit()}}.}
}
\value{
An object of class \code{"dina"}, as returned by \code{\link[=dina_fit]{dina_fit()}}.
The \verb{$call} component is overwritten with the original \code{dina()}
call so that \code{print()} and \code{summary()} show the high-level
invocation rather than the internal matrix-based dispatch.
}
\description{
\code{dina()} is the high-level user-facing wrapper around \code{\link[=dina_fit]{dina_fit()}}.
It accepts a data frame plus column-name arguments, matching the
conventions used elsewhere in the \code{forestsearch} package, and
extracts the covariate matrix, response, and treatment indicator
before dispatching to \code{\link[=dina_fit]{dina_fit()}}.
}
\details{
All other arguments are forwarded to \code{\link[=dina_fit]{dina_fit()}} via \code{...},
including \code{propensity_method}, \code{baseline_method}, \code{cross_fitting},
\code{n_folds}, \code{cens_type}, \code{cens_params}, \code{n_grid}, \code{eps}, and \code{seed}.
See \code{\link[=dina_fit]{dina_fit()}} for their full documentation.

Covariate columns must be numeric (or integer).  Convert factors to
dummy variables (e.g., with \code{\link[stats:model.matrix]{stats::model.matrix()}}) before calling
if needed.  Rows with any \code{NA} value in the referenced columns are
rejected with an error; use \code{\link[stats:na.fail]{stats::na.omit()}} or impute beforehand.
}
\examples{
set.seed(1)
n <- 400
df_demo <- data.frame(
  y         = NA,
  w         = stats::rbinom(n, 1, 0.5),
  x1        = stats::runif(n, -1, 1),
  x2        = stats::runif(n, -1, 1),
  x3        = stats::runif(n, -1, 1)
)
tau_x      <- 0.5 + 0.8 * df_demo$x1 - 0.3 * df_demo$x2
df_demo$y  <- 1 + df_demo$x1 + df_demo$w * tau_x + stats::rnorm(n)

fit <- dina(
  df         = df_demo,
  outcome    = "y",
  treatment  = "w",
  covariates = c("x1", "x2", "x3"),
  family     = "gaussian",
  seed       = 1L
)
coef(fit)
confint(fit)

}
\seealso{
\code{\link[=dina_fit]{dina_fit()}} for the underlying matrix-based estimator;
\code{\link[=dina_bagged]{dina_bagged()}} for the bagged version with infinitesimal-jackknife
variance.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_to_guohe.R
\name{fs_export_maxeff_family}
\alias{fs_export_maxeff_family}
\title{Materialize the maxeff candidate family as 0/1 membership columns}
\usage{
fs_export_maxeff_family(fit, prefix = "sg_")
}
\arguments{
\item{fit}{A \code{forestsearch} object fitted with \code{sg_focus = "maxeff"}.}

\item{prefix}{Character prefix for the generated membership column names
(\code{sg_0001}, \code{sg_0002}, ... by default).}
}
\value{
A list with elements \code{data} (\code{fit$df.est} with one appended 0/1
membership column per candidate), \code{candidates} (the appended column
names), \code{selected} (the column name of the forestsearch-selected
candidate; the family table is sorted selected-first), and \code{family} (the
candidate family table \code{fit$grp.consistency$out_sg$result}).
}
\description{
Reconstructs, for every candidate in the deduplicated maxeff family kept on
a \code{forestsearch(sg_focus = "maxeff")} fit, its per-subject 0/1 membership on
the estimation frame \code{fit$df.est}. Each candidate's cut labels (e.g.
\code{"{er <= 0}"}, or a negated cut \code{"!{size <= 20}"}) are mapped back to the
aligned dummy columns of \code{fit$df.est} via \code{fit$confounders.evaluated} /
\code{fit$confounders.candidate}, and a candidate's membership is the
intersection (AND) over its cuts. The result is the materialized family
that \code{\link[=guohe_algorithm3]{guohe_algorithm3()}} consumes; \code{\link[=fs_to_guohe]{fs_to_guohe()}} calls this internally.
}
\examples{
set.seed(11)
n <- 200
df <- data.frame(id = seq_len(n), treat = rbinom(n, 1, 0.5),
                 age = round(rnorm(n, 55, 12)),
                 bm = factor(rbinom(n, 1, 0.5)))
harm <- df$age <= 50 & df$bm == "1"
tt <- rexp(n, 0.05 * exp(log(2.5) * df$treat * harm))
df$time <- pmin(tt, 60)
df$event <- as.integer(tt <= 60)
fit <- forestsearch(
  df.analysis = df,
  outcome.name = "time", event.name = "event",
  treat.name = "treat", id.name = "id",
  confounders.name = c("age", "bm"),
  sg_focus = "maxeff", use_grf = FALSE, use_lasso = FALSE,
  fs.splits = 50, n.min = 30, d0.min = 5, d1.min = 5,
  hr.threshold = 1.1, hr.consistency = 1.0,
  pconsistency.threshold = 0.5, maxk = 2,
  details = FALSE, plot.sg = FALSE,
  parallel_args = list(plan = "sequential", workers = 1L,
                       show_message = FALSE)
)
exp_fam <- fs_export_maxeff_family(fit)
head(exp_fam$candidates)
exp_fam$selected

}
\seealso{
\code{\link[=fs_to_guohe]{fs_to_guohe()}}, \code{\link[=fs_assert_membership]{fs_assert_membership()}}
}

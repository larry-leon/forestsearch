% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guohe_adaptive_r.R
\name{guohe_adaptive_r}
\alias{guohe_adaptive_r}
\title{Cross-validated choice of the shrinkage tuning parameter r}
\usage{
guohe_adaptive_r(
  data,
  outcome = c("survival", "binary", "continuous"),
  treatment,
  candidates,
  time = NULL,
  event = NULL,
  y = NULL,
  orient = -1,
  r_grid = c(0.03, 0.1, 0.2, 0.3, 0.4, 0.45),
  v = 5L,
  B = 200L,
  level = 0.05,
  seed = NULL,
  min_events = 5L,
  refit = TRUE,
  adjust_covariates = NULL,
  fast = NULL,
  parallel = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{One of \code{"survival"}, \code{"binary"}, \code{"continuous"}.}

\item{treatment}{Name of the 0/1 treatment column.}

\item{candidates}{Character vector of names of 0/1 membership columns forming
the enumerated family.}

\item{time, event}{Survival only: names of the time and event columns.}

\item{y}{Binary or continuous only: name of the outcome column.}

\item{orient}{Either \code{-1} or \code{+1}; sign orienting the effect of interest.}

\item{r_grid}{Numeric vector of candidate values in (0, 0.5).}

\item{v}{Number of cross-validation folds.}

\item{B}{Number of bootstrap resamples.}

\item{level}{One-sided level; the two-sided interval has coverage \code{1 - level}.}

\item{seed}{Optional integer for reproducible resampling.}

\item{min_events}{Minimum events (survival) or minimum per-arm count for a
candidate to be estimable.}

\item{refit}{Logical; if \code{TRUE}, also return a full-data fit at the selected
\code{r}.}

\item{adjust_covariates}{Optional character vector of covariate names for the
within-subgroup model, matching the argument of the same name in
\code{forestsearch()}'s \code{.estimate_or()} / \code{.make_lm_estimator()}. Default \code{NULL}
is the unadjusted model. Supplying it DISABLES the closed-form fast path,
because an adjusted coefficient is a partial regression coefficient, not a
2x2 log-odds-ratio or a difference of arm means.

NOTE: \code{forestsearch()} has a SECOND adjustment axis, \code{ps_adjust_method}
(\code{"iptw"} weighted fitting, \code{"dr_gcomp"} Bang-Robins G-computation). Those
modes are not implemented in this comparator, and the closed forms would be
invalid under them too -- IPTW fits a weighted logistic, and DR reports a
marginal log-OR from G-computed probabilities; neither equals the 2x2
statistic. If the comparator is extended to those modes, the \code{fast_ok} gate
must be extended with them.}

\item{fast}{Logical or \code{NULL}. Closed-form evaluation of the within-subgroup
effect, available only for UNADJUSTED binary and continuous outcomes. These
are EXACT, not approximations: with a single binary covariate the logistic
model is saturated, so its MLE is the 2x2 log-odds-ratio, and the Gaussian
coefficient is the difference of arm means. \code{NULL} (default) auto-enables
it whenever it is valid. \code{TRUE} errors if the configuration makes it
invalid (survival, or any \code{adjust_covariates}), rather than silently falling back.}

\item{parallel}{Logical; if \code{TRUE}, distribute the bootstrap model fitting
with \code{future.apply}. Requires a \code{future::plan()} to have been set by the
caller. Results are identical to \code{parallel = FALSE} for the same \code{seed},
because resample indices are drawn serially before any distribution.
Do NOT enable this inside an already-parallel outer loop.}
}
\value{
An object of class \code{"guohe_ar"}: the selected \code{r} (\code{r_hat}), the CV
objective over the grid (\code{objective}), the per-candidate objective at the
selected \code{r} (\code{per_candidate}), bookkeeping fields (\code{r_grid}, \code{v}, \code{B},
\code{n}, \code{n_candidates}, \code{fast}, \code{outcome}, \code{orient}, \code{level},
\code{adjust_covariates}), and, when \code{refit = TRUE}, the refitted full-data
\code{\link[=guohe_algorithm3]{guohe_algorithm3()}} result (\code{fit}).
}
\description{
Implements Algorithm 2 of Guo and He (2021), Section 2.5: a v-fold
cross-validated choice of the shrinkage tuning parameter \code{r} used by
\code{\link[=guohe_algorithm3]{guohe_algorithm3()}}. For each candidate \code{r} and each fold, the
bias-reduced estimator is computed on the training folds and compared, on
the held-out fold, against every candidate subgroup's effect estimate with
its sampling variance subtracted; the \code{r} minimising the resulting CV
objective is selected.
}
\details{
This is an independent implementation of the published method, validated by
reproducing the simulation study of Section 5 of Guo and He (2021).
}
\examples{
set.seed(1)
n <- 160
df <- data.frame(
  treat = rbinom(n, 1, 0.5),
  y     = rbinom(n, 1, 0.3),
  g1    = rbinom(n, 1, 0.5),
  g2    = rbinom(n, 1, 0.5),
  g3    = rbinom(n, 1, 0.5)
)
cv <- guohe_adaptive_r(
  df, outcome = "binary", treatment = "treat",
  candidates = c("g1", "g2", "g3"), y = "y",
  orient = +1, r_grid = c(0.05, 0.25), v = 2, B = 25, seed = 2
)
cv$r_hat

}
\references{
Guo, X. and He, X. (2021). Inference on Selected Subgroups in Clinical
Trials. \emph{Journal of the American Statistical Association},
\strong{116}(535), 1498--1506. \doi{10.1080/01621459.2020.1740096}
}

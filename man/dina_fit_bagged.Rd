% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dina_bagged.R
\name{dina_fit_bagged}
\alias{dina_fit_bagged}
\title{Bagged DINA with infinitesimal-jackknife variance}
\usage{
dina_fit_bagged(
  X,
  Y,
  W,
  family = c("gaussian", "binomial", "poisson", "cox"),
  propensity_method = "logistic",
  baseline_method = NULL,
  n_bags = 100L,
  cross_fitting_per_bag = TRUE,
  n_folds_per_bag = 2L,
  cens_type = NULL,
  cens_params = list(),
  n_grid = 500L,
  ij_finite_sample_correction = TRUE,
  project_psd = TRUE,
  eps = 1e-06,
  seed = NULL,
  parallel = c("none", "bags"),
  verbose = FALSE
)
}
\arguments{
\item{X}{a numeric covariate matrix or data frame with \code{n} rows and \code{d}
columns.  All columns are treated as numeric predictors.  Column names,
when present, are propagated to the coefficient names of the fitted
object.}

\item{Y}{the response.  For \code{family = "cox"}, a data frame (or matrix
coercible to one) with columns \code{time} (event/censoring time) and
\code{status} (1 = event, 0 = censored).  For all other families, a numeric
vector of length \code{n}.}

\item{W}{binary treatment indicator (0 = control, 1 = treatment), length
\code{n}.}

\item{family}{character; one of \code{"gaussian"}, \code{"binomial"}, \code{"poisson"},
\code{"cox"}.}

\item{propensity_method}{how to estimate the propensity score
\eqn{e(x) = P(W = 1 \mid X = x)}.  One of:
\itemize{
\item the string \code{"logistic"} (the default): a logistic GLM on \code{X};
\item a numeric scalar in \verb{(0, 1)}: a known constant propensity (e.g.,
\code{0.5} for a balanced randomized trial);
\item a function taking a covariate matrix and returning a numeric vector
of fitted propensities, allowing the user to plug in random forests,
boosting, etc.
}}

\item{baseline_method}{how to estimate the natural-parameter functions
\eqn{\eta_0(x)}, \eqn{\eta_1(x)} (or the marginal mean for the Gaussian
family).  One of:
\itemize{
\item \code{NULL} (the default): use \code{"glm"} for non-Cox families and \code{"cox"} for
the Cox family;
\item \code{"glm"}: a GLM with the natural link for the family;
\item \code{"cox"}: \code{\link[survival:coxph]{survival::coxph()}} (Cox family only);
\item a list with components \code{eta0} and \code{eta1}, each a function taking a
covariate matrix and returning a vector of linear predictors.  For
Gaussian, a list with a single component \code{nu} may be supplied
instead.
}}

\item{n_bags}{positive integer giving the number of bootstrap bags
(default \code{100L}).  For stable IJ variance estimates the literature
recommends \code{n_bags} on the order of the sample size \code{n}; smaller
values are fine for diagnostic use but produce noisier variance.}

\item{cross_fitting_per_bag}{logical; if \code{TRUE} (the default) each bag
uses \code{n_folds_per_bag}-fold cross-fitting internally, matching the
paper.  If \code{FALSE}, each bag uses a single in-bag fit (faster but
loses the Chernozhukov debiasing step).}

\item{n_folds_per_bag}{positive integer giving the number of
cross-fitting folds within each bag (default \code{2L}).  Ignored when
\code{cross_fitting_per_bag = FALSE}.}

\item{cens_type}{for \code{family = "cox"}, the censoring mechanism.  One of
\code{NULL} (no censoring), \code{"fixed"}, \code{"uniform"}, \code{"exponential"}, or
\code{"unknown"} (estimate the probability of \emph{not} being censored from \code{X}
by logistic regression).  Ignored for non-Cox families.}

\item{cens_params}{named list of censoring parameters.  Required entries
depend on \code{cens_type}:
\itemize{
\item \code{"fixed"}, \code{"uniform"}: \code{T0} (the administrative censoring time).
\item \code{"exponential"}: \code{rate_c} (the exponential rate).
\item \code{"unknown"}, \code{NULL}: ignored.
}}

\item{n_grid}{positive integer giving the number of time-grid points used
for numerical integration of the not-censoring probability for
\code{cens_type \%in\% c("uniform", "exponential")} (default \code{500}).}

\item{ij_finite_sample_correction}{logical; if \code{TRUE} (the default)
the IJ variance includes the Monte-Carlo correction
\eqn{(n / B) \widehat{V}_{boot}}.  If \code{FALSE} the raw IJ formula
is returned, which tends to overstate variance for small \code{n_bags}.}

\item{project_psd}{logical; if \code{TRUE} (the default) the corrected IJ
variance matrix is projected to its nearest positive
semi-definite matrix by clipping negative eigenvalues to zero.
Strongly recommended unless you want to inspect raw variance
values yourself.}

\item{eps}{small positive constant added to denominators for numerical
stability (default \code{1e-6}).}

\item{seed}{optional integer seed for the random cross-fitting fold
assignment.  When supplied, the global RNG state is restored on exit.}

\item{parallel}{character, one of \code{"none"} (default) or \code{"bags"}.
When \code{"bags"}, the per-bag loop is dispatched via
\code{foreach::foreach() \%dofuture\% { ... }}, using whatever
\code{future::plan()} is currently active.  Call e.g.
\code{future::plan(future::multisession, workers = N)} \emph{before} this
function to enable actual parallel execution; otherwise the
parallel branch will execute serially with a warning.}

\item{verbose}{logical; if \code{TRUE}, print a one-line progress message
every 10 bags.  Default \code{FALSE}.  Per-bag progress messages are
only emitted in \code{parallel = "none"} mode; in \code{parallel = "bags"}
mode a single start-of-loop summary is printed if \code{verbose = TRUE}.}
}
\value{
An object of class \code{c("dina_bagged", "dina")}, a list
containing all the components of a \code{"dina"} fit (so that the standard
S3 methods work via inheritance) plus:
\describe{
\item{vcov}{the corrected, PSD-projected IJ variance matrix
(or the raw IJ if \code{project_psd = FALSE}).}
\item{vcov_ij_raw}{the IJ variance without the finite-sample
correction.}
\item{vcov_ij_corrected}{the IJ variance with the finite-sample
correction, before PSD projection.}
\item{vcov_boot}{the empirical bootstrap covariance of the per-bag
coefficients, \eqn{\widehat{V}_{boot}}.}
\item{bag_coefficients}{\code{n_bags_used} by \code{(d + 1)} matrix of
per-bag coefficient vectors.}
\item{bag_inclusion}{\code{n} by \code{n_bags_used} integer matrix of bag
inclusion counts, \eqn{N_{bi}^*}.}
\item{n_bags, n_bags_used}{requested and successfully-fit bag
counts.}
\item{n_folds_per_bag, cross_fitting_per_bag}{bag-level
cross-fitting settings.}
}
}
\description{
\code{dina_fit_bagged()} fits the DINA estimator on \code{n_bags} bootstrap
replicates of the data and aggregates the resulting per-bag coefficient
vectors into a bagged estimator.  The variance is estimated by the
infinitesimal jackknife (IJ) of Wager, Hastie and Efron (2014), with
the finite-sample-corrected form
\deqn{\widehat{V}_{IJ-U} = \widehat{V}_{IJ} - (n / B) \widehat{V}_{boot},}
where \eqn{\widehat{V}_{boot}} is the empirical covariance of the
per-bag coefficients.  Negative eigenvalues introduced by the
Monte-Carlo correction (an artifact of small \code{n_bags}) are optionally
clipped to zero via PSD projection so that downstream \code{confint()} is
always well-defined.
}
\details{
The implementation calls \code{\link[=dina_fit]{dina_fit()}} once per bag, with cross-fitting
optionally enabled within each bag.  All other arguments
(\code{propensity_method}, \code{baseline_method}, \code{cens_type}, \code{cens_params},
\code{n_grid}, \code{eps}) are passed through unchanged.
}
\examples{
set.seed(1)
n <- 400
d <- 3
X <- matrix(stats::runif(n * d, -1, 1), n, d)
colnames(X) <- paste0("x", seq_len(d))
W <- stats::rbinom(n, 1, stats::plogis(0.5 * X[, 1]))
tau_x <- 0.5 + 0.8 * X[, 1] - 0.3 * X[, 2]
Y <- 1 + X[, 1] + W * tau_x + stats::rnorm(n)
fit <- dina_fit_bagged(X, Y, W, family = "gaussian",
                       n_bags = 30L, seed = 1L)
coef(fit)
sqrt(diag(vcov(fit)))

}
\references{
Wager, S., Hastie, T. and Efron, B. (2014).  Confidence intervals for
random forests: the jackknife and the infinitesimal jackknife.
\emph{Journal of Machine Learning Research} 15: 1625--1651.

Gao, Z. and Hastie, T. (2025).  Estimating heterogeneous treatment
effects for general responses.  \emph{Biometrics} 81(4): ujaf162.
\doi{10.1093/biomtc/ujaf162}.
}
\seealso{
\code{\link[=dina_fit]{dina_fit()}} for the single-pass DINA estimator with
sandwich variance.
}

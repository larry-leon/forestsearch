% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/find_k_inter_main.R
\name{validate_k_inter_effect}
\alias{validate_k_inter_effect}
\title{Validate k_inter Effect on Hazard Ratio Heterogeneity}
\usage{
validate_k_inter_effect(
  k_inter_values = c(-2, -1, 0, 1, 2, 3),
  data,
  continuous_vars,
  factor_vars,
  outcome_var,
  event_var,
  treatment_var,
  subgroup_vars,
  subgroup_cuts,
  k_treat = 1,
  n_super = 5000,
  verbose = TRUE
)
}
\arguments{
\item{k_inter_values}{Numeric vector of \code{k_inter} values to evaluate.
Default \code{c(-2, -1, 0, 1, 2, 3)}.}

\item{data}{A \code{data.frame} passed to \code{generate_aft_dgm_flex}.}

\item{continuous_vars}{Character vector of continuous variable names.}

\item{factor_vars}{Character vector of factor variable names.}

\item{outcome_var}{Character string naming the outcome (time) variable.}

\item{event_var}{Character string naming the event indicator variable.}

\item{treatment_var}{Character string naming the treatment variable.}

\item{subgroup_vars}{Character vector of subgroup-defining variable names.}

\item{subgroup_cuts}{Named list of cutpoint specifications.  See
\code{\link{generate_aft_dgm_flex}} for the full specification language.}

\item{k_treat}{Numeric treatment effect modifier. Default \code{1}.}

\item{n_super}{Integer super-population size. Default \code{5000}.}

\item{verbose}{Logical. If \code{TRUE} (default), prints a formatted table
and emits a PASS/CHECK verdict for \code{k_inter = 0}.}
}
\value{
A \code{data.frame} with columns \code{k_inter}, \code{hr_H},
\code{hr_Hc}, \code{AHR_H}, \code{AHR_Hc}, \code{ratio_cox},
\code{ratio_ahr}, one row per element of \code{k_inter_values}.
}
\description{
Sweeps a vector of \code{k_inter} values through
\code{\link{generate_aft_dgm_flex}} and reports the Cox-based and average
hazard ratios in the harm subgroup (H) and its complement (Hc) for each
value.  Primary use is confirming that \code{k_inter = 0} gives a H/Hc
ratio of approximately 1 (no heterogeneity) and that the chosen calibration
value produces the desired effect size.
}
\seealso{
\code{\link{find_k_inter_for_target_hr}},
\code{\link{generate_aft_dgm_flex}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_sg_distribution.R
\name{sgdist_fig_height}
\alias{sgdist_fig_height}
\title{Adaptive Figure Height for a Subgroup-Distribution Plot}
\usage{
sgdist_fig_height(
  n_bars,
  floor_in = 4,
  ceil_in = 10,
  per_bar = 0.35,
  base_in = 3
)
}
\arguments{
\item{n_bars}{Integer.  Number of bars in the plot, typically
retrieved via \code{attr(p, "n_bars")} on a plot returned by
\code{\link{plot_sg_distribution}()}.}

\item{floor_in}{Numeric.  Minimum height in inches.
Default: \code{4.0}.}

\item{ceil_in}{Numeric.  Maximum height in inches.
Default: \code{10.0}.}

\item{per_bar}{Numeric.  Height contribution per bar in inches.
Default: \code{0.35}.}

\item{base_in}{Numeric.  Base height added to \code{per_bar * n_bars}
before applying the floor / ceiling.  Default: \code{3.0}.}
}
\value{
Numeric scalar, a height in inches in \code{[floor_in,
  ceil_in]}.  Suitable for the \code{fig.height} chunk option.
}
\description{
Computes a reasonable knitr \code{fig.height} (in inches) for a
\code{\link{plot_sg_distribution}()} panel based on the number of
bars, keeping the visual quality consistent across scenarios with
very different numbers of distinct subgroup labels.
}
\examples{
\dontrun{
p <- plot_sg_distribution(results, placeholder_on_empty = TRUE)
h <- sgdist_fig_height(attr(p, "n_bars"))
# Use `h` as the fig.height chunk option.
}

}
\seealso{
\code{\link{plot_sg_distribution}}
}

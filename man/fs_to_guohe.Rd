% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_to_guohe.R
\name{fs_to_guohe}
\alias{fs_to_guohe}
\title{Run Guo and He Algorithm 3 on a forestsearch maxeff fit}
\usage{
fs_to_guohe(
  fit,
  time = "time_months",
  event = "status",
  treatment = "hormon",
  B = 2000L,
  r = 0.03,
  level = 0.05,
  seed = NULL,
  min_events = 5L,
  parallel = FALSE,
  verify = TRUE
)
}
\arguments{
\item{fit}{A \code{forestsearch} object fitted with \code{sg_focus = "maxeff"}.}

\item{time, event, treatment}{Names of the time, event, and 0/1 treatment
columns on \code{fit$df.est}. The defaults match the GBSG analyses
(\code{"time_months"}, \code{"status"}, \code{"hormon"}); set them to the names used in
the forestsearch call.}

\item{B}{Number of bootstrap resamples passed to \code{\link[=guohe_algorithm3]{guohe_algorithm3()}}.}

\item{r}{Shrinkage tuning parameter, strictly between 0 and 0.5; see
\code{\link[=guohe_algorithm3]{guohe_algorithm3()}} and \code{\link[=guohe_adaptive_r]{guohe_adaptive_r()}}.}

\item{level}{One-sided level; the two-sided interval has coverage
\code{1 - level}.}

\item{seed}{Optional integer for reproducible resampling.}

\item{min_events}{Minimum events for a candidate to be estimable.}

\item{parallel}{Logical; passed to \code{\link[=guohe_algorithm3]{guohe_algorithm3()}}. Requires a
\code{future::plan()} to have been set by the caller.}

\item{verify}{Logical; if \code{TRUE} (default), run \code{\link[=fs_assert_membership]{fs_assert_membership()}}
and stop on any mismatch between the reconstructed and the original
selected-subgroup membership.}
}
\value{
A list with elements \code{gh} (the full \code{"guohe_a3"} object), \code{export}
(the \code{\link[=fs_export_maxeff_family]{fs_export_maxeff_family()}} result), \code{selected} (the Guo and He
argmax candidate name), and the convenience scalars \code{naive_hr},
\code{debiased_hr}, and \code{bound_hr} (hazard-ratio scale).
}
\description{
Bridge from a \code{forestsearch(sg_focus = "maxeff")} fit to the Guo and He
(2021) Algorithm 3 de-biasing estimator \code{\link[=guohe_algorithm3]{guohe_algorithm3()}}. This is the
"post-hoc identified subgroups" application of Guo and He: forestsearch
enumerates a large candidate family and selects the argmax; Guo and He
supplies a de-biased effect and one-sided bound for the selected subgroup,
correcting the winner's-curse optimism of that selection. The candidate
family is materialized from the fit with \code{\link[=fs_export_maxeff_family]{fs_export_maxeff_family()}} and,
by default, verified against forestsearch's own selected-subgroup
membership with \code{\link[=fs_assert_membership]{fs_assert_membership()}} before any de-biasing runs.
}
\details{
The de-biasing engine is an independent implementation of the published
method, validated by reproducing the simulation study of Section 5 of Guo
and He (2021).

The Guo and He selection is \code{orient = +1} (the maximum-hazard-ratio, i.e.
harm, subgroup), so its argmax coincides with the forestsearch maxeff
selection. If the two nevertheless disagree (possible when candidates are
dropped as non-estimable), a warning is issued and the returned Guo and He
objects refer to the Guo and He argmax.
}
\examples{
set.seed(11)
n <- 200
df <- data.frame(id = seq_len(n), treat = rbinom(n, 1, 0.5),
                 age = round(rnorm(n, 55, 12)),
                 bm = factor(rbinom(n, 1, 0.5)))
harm <- df$age <= 50 & df$bm == "1"
tt <- rexp(n, 0.05 * exp(log(2.5) * df$treat * harm))
df$time <- pmin(tt, 60)
df$event <- as.integer(tt <= 60)
fit <- forestsearch(
  df.analysis = df,
  outcome.name = "time", event.name = "event",
  treat.name = "treat", id.name = "id",
  confounders.name = c("age", "bm"),
  sg_focus = "maxeff", use_grf = FALSE, use_lasso = FALSE,
  fs.splits = 50, n.min = 30, d0.min = 5, d1.min = 5,
  hr.threshold = 1.1, hr.consistency = 1.0,
  pconsistency.threshold = 0.5, maxk = 2,
  details = FALSE, plot.sg = FALSE,
  parallel_args = list(plan = "sequential", workers = 1L,
                       show_message = FALSE)
)
gh <- fs_to_guohe(fit, time = "time", event = "event",
                  treatment = "treat", B = 50, seed = 3)
gh$naive_hr
gh$debiased_hr
gh$bound_hr

}
\references{
Guo, X. and He, X. (2021). Inference on Selected Subgroups in Clinical
Trials. \emph{Journal of the American Statistical Association},
\strong{116}(535), 1498--1506. \doi{10.1080/01621459.2020.1740096}
}
\seealso{
\code{\link[=fs_export_maxeff_family]{fs_export_maxeff_family()}}, \code{\link[=fs_assert_membership]{fs_assert_membership()}},
\code{\link[=guohe_algorithm3]{guohe_algorithm3()}}, \code{\link[=guohe_adaptive_r]{guohe_adaptive_r()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_subgroup_sims.R
\name{subgroup_cox}
\alias{subgroup_cox}
\title{Cox per-subgroup fit function}
\usage{
subgroup_cox(formula)
}
\arguments{
\item{formula}{A model formula for \code{\link[survival:coxph]{survival::coxph()}}, evaluated
against each subgroup's data.}
}
\value{
A function of one argument (\code{data}) returning
\code{c(hr, ub)}; carries the formula in \code{attr(, "formula")}.
}
\description{
Returns a function \code{data -> c(HR, UB)} fitting \code{formula} by
\code{\link[survival:coxph]{survival::coxph()}} and extracting the first row of
\code{summary(fit)$conf.int}: the hazard-ratio point estimate
(\code{"exp(coef)"}) and the upper 95\% bound (\code{"upper .95"}). Estimation
failure or an empty coefficient table returns \code{c(NA, NA)}. This
reproduces the vignettes' \code{cox_sR()} exactly when called with
\code{survival::Surv(y_sim, event_sim) ~ treat_sim + survival::strata(grade)}.
}
\details{
Custom fitters may be passed to \code{\link[=run_subgroup_sims]{run_subgroup_sims()}} directly: any
function of a data frame returning a length-2 numeric
\verb{(estimate, upper bound)} is accepted. Fitters run on parallel
workers, so they should reference non-base functions by
\code{pkg::fun} or rely on packages listed in \code{future_packages}.
}
\examples{
fit <- subgroup_cox(survival::Surv(y_sim, event_sim) ~ treat_sim)
}

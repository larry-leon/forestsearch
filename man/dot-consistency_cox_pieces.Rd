% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consistency_resample.R
\name{.consistency_cox_pieces}
\alias{.consistency_cox_pieces}
\title{Per-subject Cox pieces for the consistency approximation}
\usage{
.consistency_cox_pieces(
  df,
  tte.name = "Y",
  event.name = "Event",
  treat.name = "Treat",
  cox_init = 0,
  adjust_covariates = NULL
)
}
\arguments{
\item{df}{data.frame/data.table with the survival columns (and any columns
referenced by \code{adjust_covariates}).}

\item{tte.name, event.name, treat.name}{Character; time, event, and (0/1)
treatment column names (defaults \code{"Y"}, \code{"Event"}, \code{"Treat"}).}

\item{cox_init}{Numeric; warm-start for the unadjusted Cox coefficient.}

\item{adjust_covariates}{Character vector or \code{NULL}; terms appended after
\code{treat.name}.}
}
\value{
List with \code{beta_hat}, \code{dfbeta}, \code{sigma_D}, \code{n}, \code{d}, \code{log_scale}
(TRUE), \code{measure} (\code{"HR"}); or \code{NULL} if degenerate.
}
\description{
Fits the treatment-only (or covariate-adjusted) Cox model once on a subgroup
and returns the treatment log hazard ratio, the per-subject treatment
\code{dfbeta} influence contributions, and the split-perturbation SD.
}
\keyword{internal}

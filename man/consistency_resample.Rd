% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consistency_resample.R
\name{consistency_resample}
\alias{consistency_resample}
\title{Resampling approximation to the splitting consistency rate}
\usage{
consistency_resample(
  df,
  hr.consistency = 1,
  consistency_threshold = NULL,
  comparison_threshold = NULL,
  outcome_type = c("survival", "binary", "continuous", "count"),
  effect_measure = NULL,
  method = c("both", "closed", "mc"),
  multiplier = c("rademacher", "normal", "poisson"),
  draws = 1000L,
  tte.name = "Y",
  event.name = "Event",
  treat.name = "Treat",
  outcome.name = "Y",
  offset.name = NULL,
  cox_init = 0,
  adjust_covariates = NULL,
  adverse_outcome = TRUE,
  seed = NULL
)
}
\arguments{
\item{df}{data.frame/data.table with the relevant outcome columns.}

\item{hr.consistency}{Numeric; survival HR consistency threshold (also the
default GLM threshold when \code{consistency_threshold} is \code{NULL}). Default 1.0.}

\item{consistency_threshold}{Numeric or \code{NULL}; GLM consistency threshold on
the natural scale (OR/RR/IRR, or RD/MD/IRD). \code{NULL} uses \code{hr.consistency}.}

\item{comparison_threshold}{Numeric or \code{NULL}; the consistency threshold
already resolved to the comparison scale \code{c} (\code{log(threshold)} for ratio
measures, identity for identity measures). When supplied it is used
directly and the internal transform is bypassed. This is the value
\code{\link[=forestsearch]{forestsearch()}} threads through for GLM outcomes (it resolves the scale
upstream), so callers avoid a double log-transform.}

\item{outcome_type}{Character; \code{"survival"} (default), \code{"binary"},
\code{"continuous"}, or \code{"count"}.}

\item{effect_measure}{Character or \code{NULL}; GLM effect measure (resolved to
the outcome-type default when \code{NULL}). Ignored for survival.}

\item{method}{Character; \code{"closed"}, \code{"mc"}, or \code{"both"} (default).}

\item{multiplier}{Character; \code{"rademacher"} (default), \code{"normal"},
\code{"poisson"}.}

\item{draws}{Integer; multiplier draws for the Monte Carlo path.}

\item{tte.name, event.name, treat.name}{Character; survival/treatment column
names.}

\item{outcome.name, offset.name}{Character; GLM outcome and (rate) offset
column names.}

\item{cox_init}{Numeric; Cox warm-start.}

\item{adjust_covariates}{Character vector or \code{NULL}; adjustment terms.}

\item{adverse_outcome}{Logical; GLM adverse-outcome convention (see
\code{\link[=.consistency_glm_pieces]{.consistency_glm_pieces()}}).}

\item{seed}{Integer or \code{NULL}; RNG seed for the Monte Carlo draws.}
}
\value{
List with \code{rate_closed}, \code{rate_mc}, \code{beta_hat}, \code{sigma_D}, \code{n}, \code{d},
\code{delta}, \code{measure}; all-\code{NA} rates (with available summaries) if the fit
fails or the configuration is unsupported.
}
\description{
Approximates the forestsearch consistency rate for a candidate subgroup
without repeated sample splits and refits. The subgroup is fit once; the
random-split pair of half-sample treatment effects is represented as
\verb{beta_hat +/- D}, a multiplier sum of the per-subject treatment \code{dfbeta}
contributions (see the file header). Handles the Cox (survival) path and the
GLM paths (binary OR/RR/RD, continuous MD, count IRR).
}
\details{
\code{multiplier = "rademacher"} (default) reproduces the 50/50 Bernoulli split of
\code{\link[=run_single_consistency_split]{run_single_consistency_split()}}; \code{"normal"} is the Lin (1997) Gaussian
multiplier and \code{"poisson"} a centred unit-Poisson (DBP) multiplier.
}
\examples{
\dontrun{
# survival (Cox)
consistency_resample(sub, hr.consistency = 1.0)
# binary odds ratio
consistency_resample(sub, outcome_type = "binary", effect_measure = "OR",
                     consistency_threshold = 1.0,
                     outcome.name = "Y", treat.name = "Treat")
# count incidence-rate ratio
consistency_resample(sub, outcome_type = "count", effect_measure = "IRR",
                     consistency_threshold = 1.0,
                     outcome.name = "events", offset.name = "fu_time")
}
}
\seealso{
\code{\link[=run_single_consistency_split]{run_single_consistency_split()}}, \code{\link[=consistency_resample_compare]{consistency_resample_compare()}}
}

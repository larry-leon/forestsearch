% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/betaHhat_truth.R
\name{fs_betaHhat_theta_dagger_check}
\alias{fs_betaHhat_theta_dagger_check}
\title{\code{theta-dagger} at the true subgroup flag, on the scoring frame}
\usage{
fs_betaHhat_theta_dagger_check(
  frame,
  outcome_type = c("survival", "binary", "continuous", "count"),
  harm.name = "flag_harm",
  outcome.name = "y_sim",
  event.name = "event_sim",
  treat.name = "treat_sim",
  effect_measure = NULL
)
}
\arguments{
\item{frame}{Data frame. The evaluation population, normally from
\code{\link[=fs_build_eval_frame]{fs_build_eval_frame()}}.}

\item{outcome_type}{Character. One of \code{"survival"}, \code{"binary"},
\code{"continuous"}, \code{"count"}.}

\item{harm.name}{Character. The true-subgroup flag column; \verb{== 1L} is in.}

\item{outcome.name, event.name, treat.name}{Character. Column names in
\code{frame}. \code{event.name} is survival only.}

\item{effect_measure}{Character. Required for \code{"continuous"} and
\code{"count"}; ignored for \code{"survival"} and \code{"binary"}, which keep their
fitted targets.}
}
\value{
A named numeric vector, \code{thetaDagger_H} and \code{thetaDagger_Hc}.
}
\description{
The marginal target at the DGM's own harm flag, computed on the same frame
\code{beta(Hhat)} is scored on. Use it as a sanity gate: it should reproduce the
DGM's own subgroup effects.
}
\details{
For \code{"continuous"} and \code{"count"} this is an \strong{exact identity}, not
agreement to Monte Carlo error -- the frame is the super-population and the
arithmetic is the same \code{\link[=compute_aor]{compute_aor()}} dispatch the DGM used. A tolerance
there would hide a real defect.

This is a thin dispatch onto the same per-family effect used for every
region; it introduces no arithmetic of its own.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_identification_summary.R
\name{fs_identification_structure}
\alias{fs_identification_structure}
\title{Identification structure for one set of replicates}
\usage{
fs_identification_structure(
  results,
  anchor,
  partner,
  proxy,
  confounders = NULL,
  sg_def_col = "sg_def",
  covs_col = "covs"
)
}
\arguments{
\item{results}{A per-replicate results data frame, a bundle carrying
\code{results}, or a path to an \code{.rds} holding either.}

\item{anchor}{Character. The covariate the search is expected to recover.}

\item{partner}{Character. The true second covariate of the planted region.}

\item{proxy}{Character. A correlated covariate that may substitute for
\code{partner}.}

\item{confounders}{Character vector. The analysis covariate pool, for the
involvement vector. Defaults to the three named roles.}

\item{sg_def_col, covs_col}{Character. Columns holding the realized rule and
its projected covariate names.}
}
\value{
A list with \code{det_rate}, \code{n_detected}, \code{n_total}, \code{involvement},
\code{structure} (a named share vector), and the four accuracy means.
}
\description{
Classifies every detected replicate's realized rule against a planted
\code{anchor} / \code{partner} / \code{proxy} triple, and returns covariate involvement,
the structure composition, and classification accuracy.
}
\section{The classification}{


Each detected replicate falls in exactly one of five mutually exclusive
categories, tested in this order: the anchor is absent (\verb{anchor missed});
the rule names the anchor and nothing else (\verb{anchor only}); the rule pairs
the anchor with the true \code{partner}; the rule pairs it with the \code{proxy}; or
the rule pairs it with something else. The order matters -- a rule naming
both the partner and the proxy counts as the true pairing.
}

\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dina_subgroup.R
\name{dina_subgroup}
\alias{dina_subgroup}
\title{Identify a harm subgroup from a DINA fit via univariate threshold search}
\usage{
dina_subgroup(
  fit,
  df,
  covariates,
  m_diff,
  n_min = 60L,
  n_min.frac = 0.1,
  direction = c("both", "left", "right"),
  max_depth = 2L,
  grid_probs = seq(0.1, 0.9, by = 0.1),
  sg_focus = "maxSG",
  selection_rule = "neighborhood",
  effect_neighborhood = 0.1,
  alpha = 0.05
)
}
\arguments{
\item{fit}{a fitted DINA object of class \code{"dina"} or
\code{"dina_bagged"}, as returned by \code{\link[=dina_fit]{dina_fit()}}, \code{\link[=dina_fit_bagged]{dina_fit_bagged()}},
\code{\link[=dina]{dina()}}, or \code{\link[=dina_bagged]{dina_bagged()}}.}

\item{df}{data frame containing the covariate columns referenced by
\code{covariates}.  Typically the same data frame used to fit \code{fit}.}

\item{covariates}{character vector of column names in \code{df} to search
over.  All must be numeric.}

\item{m_diff}{scalar harm threshold on the natural-parameter scale
of \code{fit$family}.  Subgroups are kept only if their mean tau-hat
meets or exceeds this value.

\strong{It is a candidacy floor, not a ranking key.}  It decides which
candidates enter the qualifying family; \code{sg_focus} then orders that
family.  Every focus ranks only over survivors of this floor, so
widening or tightening \code{m_diff} changes the selection by changing the
family, not by changing the sort.

\strong{Scale.}  The link (tau) scale of \code{fit$family}, which is not the scale
the effect is usually reported on: \code{log(HR)} for \code{cox}, \code{log(OR)} for
\code{binomial}, \code{log(IRR)} for \code{poisson}, and the raw mean difference for
\code{gaussian}.  A harm threshold of HR 1.25 is therefore
\code{m_diff = log(1.25)}, not \code{1.25}.

\strong{Under \code{\link[=forestsearch]{forestsearch()}} with \code{subgroup_method = "dina"} this argument is
derived, not passed through.}  It is computed as \code{log(hr.threshold)}
(identity for \code{gaussian}) from the resolved \code{effect.threshold}, and an
\code{m_diff} supplied in \code{dina_args} -- a \code{dina_frontier()} key -- is
\strong{ignored} on that path.  Set the floor with \code{effect.threshold} /
\code{hr.threshold} instead.

\strong{Under \code{dina_select_statistic = "effect"}} the floor still defines the
qualifying family on the tau-hat scale, while selection re-ranks that
family on the refit inferential effect.  The two scales therefore do
different jobs in one selection: \code{m_diff} shapes the family, the refit
effect orders it.  Since multiplier resampling de-biases over the
realized selection event, \code{m_diff} is what fixes the family MR de-biases
over -- changing it changes the inferential target, not just the winner.}

\item{n_min}{positive integer, or \code{NULL}; minimum subgroup size. Default
\code{60L}. Supplying a value (or omitting it) uses that fixed floor. Passing
\code{n_min = NULL} opts into a sample-size-adaptive floor
\code{max(60, ceiling(n_min.frac * n))}, where \code{n = nrow(df)} -- at least 60,
and at least \code{n_min.frac} of \code{n}. The resolved integer is stored on the
returned object (\verb{$n_min}), so \code{\link[=dina_subgroup_bootstrap]{dina_subgroup_bootstrap()}} and
\code{\link[=dina_subgroup_refit]{dina_subgroup_refit()}} inherit it.}

\item{n_min.frac}{numeric in (0, 1); fraction of \code{nrow(df)} used for the
adaptive \code{n_min} floor when \code{n_min = NULL}. Default \code{0.10}. Ignored when
\code{n_min} is supplied.
Default \code{60L}, matching the convention in GRF and \code{forestsearch()}.}

\item{direction}{one of \code{"both"} (default), \code{"left"}, or \code{"right"}.
For each covariate \code{x_j} controls whether to search subgroups of
the form \code{x_j <= q} (left), \code{x_j >= q} (right), or both.}

\item{max_depth}{integer, \code{1L} or \code{2L} (default \code{2L}, aligning with the
consistency search \code{maxk = 2} and GRF \code{grf_depth = 2}).  \code{1L} searches
single-covariate cuts only (the legacy behaviour).  \code{2L} additionally
searches AND-conjunctions of two distinct covariates and ranks them
jointly with the single-covariate cuts; see the \emph{Depth-2
conjunctions} section.}

\item{grid_probs}{numeric vector of probabilities in \verb{[0, 1]} giving
the per-covariate quantile grid used to generate depth-2 pair
thresholds.  Only consulted when \code{max_depth = 2L}.  Default interior
deciles \code{seq(0.1, 0.9, 0.1)}.  A finer grid recovers more of the
exhaustive pair space at higher cost; covariates with fewer unique
values than \code{length(grid_probs)} are searched at their unique values
instead.}

\item{sg_focus}{character; subgroup selection criterion applied to the
qualifying candidates.  One of \code{"maxSG"} (default), \code{"minSG"},
\code{"eff"}, \code{"effMaxSG"}, \code{"effMinSG"}, \code{"maxeff"}, \code{"maxeffCons"}.  The
canonical forms \code{"hr"}, \code{"hrMaxSG"}, \code{"hrMinSG"} (used internally by
\code{\link[=forestsearch]{forestsearch()}}) and the alias \code{"maxcons"} for \code{"eff"} are also
accepted.  See Description for the semantics of each.

\strong{Five spellings, one rule.}  On this path \code{"eff"}, \code{"hr"}, \code{"maxcons"},
\code{"maxeff"} and \code{"maxeffCons"} all resolve to the same ordering,
\code{order(-eff)}.  Two separate collapses produce that: \code{"eff"} and
\code{"maxcons"} are aliases of \code{"hr"} everywhere (see
\code{.normalize_sg_focus()}), and \code{"maxeff"} / \code{"maxeffCons"} are behavioural
synonyms of \code{"hr"} here specifically.  Use \code{\link[=fs_focus_tag]{fs_focus_tag()}} to label
output by the rule that ran; a hand-written tag reading \code{"maxeffCons"} on
this path names a rule that did not execute.  \code{\link[=forestsearch]{forestsearch()}} announces
the collapse at run time, gated on its \code{quiet} argument; \code{dina_subgroup()}
called directly does not, having no \code{quiet} of its own.

\code{"maxeff"} and \code{"maxeffCons"} are \strong{synonyms here}, both ranking the
qualifying candidates by effect (ties broken by insertion order) exactly
as \code{"eff"} / \code{"hr"} does.  The \code{Cons} suffix names the consistency floor,
and DINA has none -- it computes no \code{Pcons}, and no ordering key below
contains one -- so there is nothing for the qualifier to bind to.  This
is engine-specific: under \code{\link[=forestsearch]{forestsearch()}} with
\code{subgroup_method = "consistency"} the two are genuinely distinct, since
\code{"maxeffCons"} retains both the consistency and effect floors while
\code{"maxeff"} disables both.  The synonymy disables no floor -- candidates
are qualified before this ordering is applied -- and raises no condition,
by design.}

\item{selection_rule}{character; rule defining the candidate inclusion
band for \code{"effMaxSG"} / \code{"effMinSG"}.  One of \code{"neighborhood"}
(default; 1-D effect band), \code{"pareto"} (2-D non-dominated frontier
in (effect, N)), or \code{"both"} (intersection).  Must be
\code{"neighborhood"} for the other foci.  Forwarded unchanged to the
shared band computation, so semantics match \code{\link[=forestsearch]{forestsearch()}}.}

\item{effect_neighborhood}{numeric in \verb{[0, 1)}; relative tolerance for
the effect band.  A candidate is in the neighborhood iff its
(natural-scale) effect is at least
\code{(1 - effect_neighborhood) * max(effect)} over the qualifying set.
Default \code{0.10} (within 10\% of the strongest effect).  Consulted by
\code{selection_rule = "neighborhood"} and \code{"both"}; ignored by
\code{"pareto"} (and must be left at its default there) and for
\code{"maxSG"}, \code{"minSG"}, \code{"eff"}.}

\item{alpha}{confidence level for the Wald interval on the
subgroup-mean tau-hat.  Default \code{0.05}.}
}
\value{
An object of class \code{"dina_subgroup"}, a list with components:
\describe{
\item{found}{logical; \code{TRUE} if any candidate satisfied
\code{m_diff} and \code{n_min}, otherwise \code{FALSE}.  When \code{FALSE}, the
subgroup-specific components below are \code{NULL}.}
\item{covariate, direction, threshold}{the chosen cut(s).  Length
1 for a single-covariate subgroup; length 2 (one entry per
covariate) when \code{max_depth = 2L} selects a conjunction.}
\item{labels}{character vector of the chosen \verb{\{var op q\}} factor
label(s), in the form consumed by \code{\link[=get_dfpred]{get_dfpred()}} (AND-composed).
Length matches \code{covariate}.}
\item{label}{a single combined display string, e.g.
\code{"{nodes >= 10 & age >= 60}"}.}
\item{depth}{integer; number of covariates in the selected
subgroup (\code{1} or \code{2}).}
\item{n_subgroup}{integer size of the chosen subgroup.}
\item{mean_tau_hat}{scalar subgroup-mean tau-hat.}
\item{se_mean_tau_hat}{Wald standard error, computed as
\verb{sqrt(a_S^T vcov(fit) a_S)} (CONDITIONAL on the chosen
subgroup -- not selection-adjusted).}
\item{ci}{named length-2 vector \verb{(lower, upper)} giving the
Wald 1 - alpha CI.}
\item{mask}{logical vector of length \code{nrow(df)} marking which
rows are in the chosen subgroup.}
\item{m_diff, n_min, sg_focus, selection_rule,
effect_neighborhood, alpha, family, max_depth}{the inputs / fit
family, echoed for reproducibility.}
\item{n_total}{\code{nrow(df)}.}
\item{n_candidates_searched}{total number of \verb{(j, dir, q)}
triples evaluated.}
\item{n_candidates_qualifying}{number of those triples that met
both the size and \code{m_diff} constraints.}
\item{call}{the matched call.}
}
}
\description{
Given a fitted DINA object (\code{"dina"} or \code{"dina_bagged"}) and a data
frame containing the covariates the model was fit on, search over
(covariate, threshold, direction) candidates for subgroups whose mean
per-patient HTE estimate meets the harm threshold \code{m_diff}, then select
one according to \code{sg_focus} (default \code{"maxSG"}: the largest qualifying
subgroup).  This is the simplest bridge between DINA's continuous
estimator \eqn{\hat\tau(x) = \hat\beta_0 + x'\hat\beta} and
forestsearch's interpretable signature framework.
}
\details{
For each covariate \code{x_j} in \code{covariates}, the search visits the
sorted unique values of \code{df[[x_j]]} as candidate thresholds \code{q} and
for each threshold considers the left-tail subgroup of patients with
\eqn{x_{j,i} \le q} (and optionally also the right-tail subgroup
with \eqn{x_{j,i} \ge q}).  A candidate
subgroup must satisfy:
\itemize{
\item subgroup size \verb{|S| >= n_min}, AND
\item mean per-patient tau-hat over \code{S} \verb{>= m_diff}.
}

Among the qualifying candidates, \code{sg_focus} selects the returned
subgroup, mirroring \code{\link[=forestsearch]{forestsearch()}}'s selection vocabulary:
\itemize{
\item \code{"maxSG"} (default) -- largest qualifying subgroup (legacy
behaviour; size ties broken by the most extreme mean tau-hat).
\item \code{"minSG"} -- smallest qualifying subgroup.
\item \code{"eff"} -- most extreme effect, ignoring size.
\item \code{"effMaxSG"} -- largest subgroup in the candidate inclusion band
(see \code{selection_rule}).
\item \code{"effMinSG"} -- smallest subgroup in the inclusion band.
The \code{"eff*"} band foci reuse the inclusion-band logic shared with
\code{\link[=sort_subgroups]{sort_subgroups()}}; for ratio families (binomial, poisson, cox) the
band is applied on the natural (exponentiated) effect scale.
\code{selection_rule} defines the band, exactly as in \code{\link[=forestsearch]{forestsearch()}}:
\code{"neighborhood"} (1-D effect band within \code{effect_neighborhood} of the
maximum qualifying effect), \code{"pareto"} (the 2-D non-dominated frontier
in (effect, N) space), or \code{"both"} (their intersection).  DINA's
univariate (covariate, threshold, direction) candidates populate the
(effect, N) plane just as forestsearch's signatures do, so the
frontier is defined analogously.
}
}
\section{Depth-2 conjunctions}{

With \code{max_depth = 2} the candidate set is extended to AND-conjunctions
of two thresholds on \emph{distinct} covariates, e.g.
\code{nodes >= q1 & age >= q2}.  Because tau-hat is linear in X, the mean
over the intersection mask is scored exactly like a depth-1 cut, so
the \code{m_diff}, \code{n_min}, and \code{sg_focus} machinery is unchanged.  To stay
tractable, depth-2 pairs are generated over a per-covariate quantile
grid (\code{grid_probs}) rather than every unique value, while depth-1
singletons retain full resolution.  Singletons and pairs are ranked
\emph{jointly} under \code{sg_focus}, so a depth-2 conjunction is returned only
when it outranks every depth-1 candidate; otherwise the depth-1
selection is recovered exactly.  When the selected subgroup is a
conjunction, \code{covariate}, \code{direction}, and \code{threshold} are
length-2 vectors and \code{labels} holds the two \verb{\{var op q\}} factors in
the form consumed by \code{\link[=get_dfpred]{get_dfpred()}}.

The variance of the subgroup-mean tau-hat is computed as
\deqn{\widehat{\mathrm{Var}}(\bar{\hat\tau}_S) =
       a_S^\top \widehat{\mathrm{Var}}(\hat\beta) a_S,}
with \eqn{a_S = (1, \bar{x}_S^\top)} and
\eqn{\widehat{\mathrm{Var}}(\hat\beta) = \mathrm{vcov}(\mathtt{fit})}.
The variance source therefore tracks the fit class: sandwich for
\code{"dina"} and infinitesimal jackknife for \code{"dina_bagged"}.
}

\section{Inference caveat}{

The returned Wald confidence interval is \emph{conditional on the
selected (covariate, threshold) being treated as pre-specified}.
It does not adjust for the selection across the candidate set and
will undercover when the search space is large.  Use this CI for
descriptive reporting; for hypothesis testing against \code{m_diff},
a selection-adjusted interval via bootstrap of the full search
procedure is required.  The undercoverage is more pronounced with
\code{max_depth = 2L}, whose candidate space is substantially larger.
}

\examples{
set.seed(1)
n <- 400
df_demo <- data.frame(
  w  = stats::rbinom(n, 1, 0.5),
  x1 = stats::runif(n, -1, 1),
  x2 = stats::runif(n, -1, 1),
  x3 = stats::runif(n, -1, 1)
)
tau_x      <- 0.3 + 1.2 * df_demo$x1 - 0.4 * df_demo$x2
df_demo$y  <- 0.5 * df_demo$x1 + df_demo$w * tau_x + stats::rnorm(n)

fit <- dina(df_demo, outcome = "y", treatment = "w",
            covariates = c("x1", "x2", "x3"),
            family = "gaussian", seed = 1L)

sg <- dina_subgroup(fit, df_demo,
                    covariates = c("x1", "x2", "x3"),
                    m_diff = 0.5, n_min = 60L)
sg

# Tighter subgroup via the effect-neighborhood band:
dina_subgroup(fit, df_demo, covariates = c("x1", "x2", "x3"),
              m_diff = 0.5, n_min = 60L, sg_focus = "effMaxSG")

# Frontier-aware selection (2-D non-dominated set in (effect, N)):
dina_subgroup(fit, df_demo, covariates = c("x1", "x2", "x3"),
              m_diff = 0.5, n_min = 60L, sg_focus = "effMaxSG",
              selection_rule = "both")

# Depth-2: allow AND-conjunctions of two covariates.
dina_subgroup(fit, df_demo, covariates = c("x1", "x2", "x3"),
              m_diff = 0.5, n_min = 60L, max_depth = 2L)

}
\seealso{
\code{\link[=dina_fit]{dina_fit()}} / \code{\link[=dina]{dina()}} for the underlying estimator;
\code{\link[=dina_fit_bagged]{dina_fit_bagged()}} / \code{\link[=dina_bagged]{dina_bagged()}} for the bagged variant whose
IJ variance propagates through the subgroup-mean SE here;
\code{\link[=sort_subgroups]{sort_subgroups()}} and \code{\link[=forestsearch]{forestsearch()}} for the selection
vocabulary mirrored by \code{sg_focus}.
}

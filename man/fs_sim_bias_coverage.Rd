% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_bias_coverage.R
\name{fs_sim_bias_coverage}
\alias{fs_sim_bias_coverage}
\title{Bias and coverage summary for one simulation cell}
\usage{
fs_sim_bias_coverage(
  results,
  block = c("H", "Hc"),
  estimators = c("naive", "mr", "fld"),
  level = 0.95,
  target = "betaHhat",
  side = c("lower", "upper"),
  scale = c("log", "identity")
)
}
\arguments{
\item{results}{Data frame of per-replicate records from a combined bundle
(\code{readRDS(...)$results}).}

\item{block}{\code{"H"} (harm, default) or \code{"Hc"} (complement).  The \code{"fld"}
estimator is dropped with a message when the block's field columns are
absent (\verb{fld_Hc_*} exist only for bundles run with
\code{field_complement = TRUE}).}

\item{estimators}{Subset of \code{c("naive", "mr", "fld", "mr_w", "mr_wf")}
(default the first three).  \code{"mr_w"} / \code{"mr_wf"} are MR (IJ) with the
winner-only / winner-floor SE (\verb{mr_<block>_se_w} / \verb{_se_wf} and their
\verb{_lo_w/_hi_w} / \verb{_lo_wf/_hi_wf} bounds; TASK_complement_refinements_2026-09-06),
available only on bundles that recorded them.}

\item{level}{Coverage level (default 0.95).}

\item{target}{\code{"betaHhat"} (default; the per-replicate conditional target
\verb{betaHhat_<block>}), \code{"oracle"} (the per-replicate oracle estimate
\verb{or_<block>_est}), or a numeric scalar on the HR scale (e.g. a CDE or
marginal truth value from the bundle's \code{truth} element).}

\item{side}{\code{"lower"} (default; one-sided coverage of the lower bound,
the harm-claim orientation) or \code{"upper"} (one-sided coverage of the
upper bound, the benefit-claim orientation).  Two-sided quantities are
unaffected.}

\item{scale}{\code{"log"} (default) or \code{"identity"}.  The working scale of the
estimate columns: \code{"log"} for ratio measures (HR/OR/RR; bias, SD and the
normal-based one-sided bound \verb{exp(log est -/+ z * SE)} are computed on
the log scale, and non-positive estimates are dropped), \code{"identity"}
for difference measures (MD/RD; bias, SD and the bound \verb{est -/+ z * SE}
on the natural scale, no positivity guard).  Add-only
(TASK_continuous_field_mac_2026-09-07): the default reproduces the
previous output exactly.  Under \code{"identity"} the \code{bias_log} column
holds the identity-scale bias (the column name is kept so stacked
tables and \code{\link[=fs_plot_bias_coverage]{fs_plot_bias_coverage()}} read either scale unchanged).}
}
\value{
Data frame, one row per estimator: \code{estimator}, \code{n}, \code{bias_log},
\code{sd_emp}, \code{se_mean}, \code{b}, \code{r}, \code{cov1}, \code{cov1_wilson_lo},
\code{cov1_wilson_hi}, \code{cov2}, \code{cov2_wilson_lo}, \code{cov2_wilson_hi},
\code{cov1_ref}, \code{cov2_ref}.
}
\description{
From a combined bundle's per-replicate \code{results} (the recorder columns of
the \code{fb_mr_field} template family: \verb{nv_H_*}, \verb{mr_H_*}, \verb{fld_H_*},
\code{betaHhat_H}, \code{detected}, and their \code{Hc} twins), computes, per estimator
over the detected replicates: the retained bias on the log scale, the
empirical SD of the log estimate, the mean reported SE, their ratios
\code{b = bias_log / sd_emp} and \code{r = se_mean / sd_emp}, the observed one- and
two-sided coverage of the target with Wilson intervals, and the Gaussian
reference coverages at (b, r).
}
\details{
Conventions follow the committed template's diagnostics section: the
one-sided lower bound is the stored \verb{fld_<block>_lo1s} for the field row
and \verb{exp(log est - qnorm(level) * SE)} for naive and MR (IJ); two-sided
coverage uses each estimator's stored bounds; the field estimator is the
shrunk-field \code{est2} with the two-sided Lambda-quantile interval.  Under
\code{side = "upper"} (the complement's benefit-claim orientation,
TASK_mr_field_complement_2026-09-06) the one-sided coverage is
\verb{truth <= upper bound}, with the field's stored \verb{fld_<block>_up1s} and
\verb{exp(log est + qnorm(level) * SE)} for naive and MR (IJ), and the Gaussian
reference is \code{Phi(z_level * r + b)} -- a positive retained bias helps an
upper bound where it hurts a lower one.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consistency_resample.R
\name{.consistency_glm_pieces}
\alias{.consistency_glm_pieces}
\title{Per-subject GLM pieces for the consistency approximation}
\usage{
.consistency_glm_pieces(
  df,
  outcome_type,
  effect_measure = NULL,
  treat.name = "Treat",
  outcome.name = "Y",
  offset.name = NULL,
  adjust_covariates = NULL,
  adverse_outcome = TRUE
)
}
\arguments{
\item{df}{data.frame/data.table with \code{outcome.name}, \code{treat.name}, any
\code{adjust_covariates}, and \code{offset.name} (rate measures).}

\item{outcome_type}{Character; \code{"binary"}, \code{"continuous"}, or \code{"count"}.}

\item{effect_measure}{Character or \code{NULL}; resolved to the outcome-type
default when \code{NULL} (binary \code{"RD"}, continuous \code{"MD"}, count \code{"IRR"}).}

\item{treat.name, outcome.name, offset.name}{Character; column names.}

\item{adjust_covariates}{Character vector or \code{NULL}.}

\item{adverse_outcome}{Logical; if \code{FALSE}, binary \code{Y} is flipped to \code{1 - Y}
and continuous \code{Y} negated, matching the estimator so that a larger
coefficient consistently means treatment harm.}
}
\value{
List with \code{beta_hat}, \code{dfbeta}, \code{sigma_D}, \code{n}, \code{d}, \code{log_scale},
\code{measure}; or \code{NULL} if unsupported/degenerate/non-convergent.
}
\description{
Fits the same treatment-effect model the corresponding
\code{\link[=make_effect_estimator]{make_effect_estimator()}} closure fits (matching \code{effect_measure} and
\code{adverse_outcome} conventions), and returns the treatment coefficient, its
per-subject \code{dfbeta} influence contributions, and the split-perturbation SD.
}
\details{
Supported: \code{"OR"} (logistic), \code{"RR"} (log-binomial with Poisson fallback),
\code{"RD"} (identity-link binomial), \code{"MD"} (OLS), \code{"IRR"} (Poisson with
\code{offset(log(time))}). \code{"IRD"} and propensity-adjusted (\code{iptw}/\code{dr_gcomp})
effects are not single coefficients, so they return \code{NULL} (the caller falls
back to splitting).
}
\keyword{internal}

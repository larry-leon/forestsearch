% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dina_subgroup.R
\name{dina_frontier}
\alias{dina_frontier}
\title{Extract DINA per-covariate Pareto frontiers as forestsearch cuts}
\usage{
dina_frontier(
  fit,
  df,
  covariates,
  scope = c("wide", "harm"),
  m_diff = NULL,
  n_min = 60L,
  direction = c("both", "left", "right"),
  max_per_covariate = 3L,
  max_subgroups = 10L,
  digits = 3L
)
}
\arguments{
\item{fit}{a fitted DINA object (class \code{"dina"} or \code{"dina_bagged"}).}

\item{df}{data frame containing the covariate columns.}

\item{covariates}{character vector of numeric covariate columns to
search over.}

\item{scope}{one of \code{"wide"} (default) or \code{"harm"}.  \code{"wide"} casts a
broad net: every candidate with size \verb{>= n_min} is eligible, and the
per-covariate frontiers span the whole effect-size boundary
(mirroring GRF's wide-net role, leaving forestsearch's consistency
stage as the gatekeeper).  \code{"harm"} restricts the eligible set to
candidates with mean tau-hat \verb{>= m_diff} before taking the frontiers.}

\item{m_diff}{scalar harm floor on the link (natural-parameter) scale.
Required when \code{scope = "harm"}; ignored when \code{scope = "wide"}.
Default \code{NULL}.}

\item{n_min}{positive integer minimum subgroup size.  Default \code{60L}.}

\item{direction}{one of \code{"both"} (default), \code{"left"}, \code{"right"}.}

\item{max_per_covariate}{positive integer, or \code{Inf} for no limit: the
most cuts any single covariate may contribute (its top cuts by
effect).  Default \code{3L}.}

\item{max_subgroups}{positive integer, or \code{Inf} for no limit: the cap
on the total pooled cuts returned -- the DINA analog of
forestsearch's \code{max_subgroups_search}.  When the pool is larger it is
trimmed round-robin by within-covariate effect rank.  Default \code{10L}.}

\item{digits}{significant figures for rounding the emitted threshold
in \code{cut_expr}.  Default \code{3L}.}
}
\value{
A data frame (one row per cut, after the caps and
threshold-rounding deduplication) with columns:
\describe{
\item{covariate, direction, threshold}{the subgroup definition
(raw threshold).}
\item{n_subgroup}{subgroup size.}
\item{mean_tau_hat}{subgroup-mean tau-hat on the link scale.}
\item{effect}{the same on the natural scale (exponentiated for
ratio families binomial/poisson/cox), used for the frontiers and
the effect ranking.}
\item{cut_expr}{the canonical forestsearch cut expression.}
}
The result carries attributes \code{n_frontier} (total per-covariate
frontier size, deduped, before the caps), \code{n_qualifying}, and
\code{scope}.  Returns a 0-row data frame (same columns) when no candidate
qualifies.
}
\description{
Runs the same univariate (covariate, direction, threshold) search as
\code{\link[=dina_subgroup]{dina_subgroup()}}, but instead of selecting one subgroup it returns a
pool of \strong{per-covariate Pareto-non-dominated} cuts.  Each cut is
rendered as a forestsearch cut expression (\code{"x1 <= 0.5"}), so the pool
can be fed to \code{\link[=forestsearch]{forestsearch()}} as screening-stage candidates in the
same way GRF tree cuts are.  This is the DINA analog of GRF candidate
discovery.
}
\details{
\strong{Why per-covariate and not a single global frontier.}  The global
Pareto frontier in (effect, N) space collapses onto whichever single
covariate dominates the trade-off boundary, returning many
micro-stepped cuts on that one covariate.  That is faithful to the
definition but the opposite of what forestsearch needs: its value is
in \emph{composing} cuts drawn from several covariates.  So this function
computes a separate frontier for each covariate, then pools them,
mirroring how a GRF tree splits on multiple variables.

\strong{Two caps, each optional.}  Diversity and total burden are
controlled by two independent caps, either of which may be \code{Inf} to
disable it:
\itemize{
\item \code{max_per_covariate} bounds how many cuts any single covariate
contributes (its top cuts by effect), limiting within-covariate
redundancy.
\item \code{max_subgroups} bounds the total pool size across covariates,
the DINA analog of forestsearch's \code{max_subgroups_search}.
}
When the pool exceeds \code{max_subgroups}, the global trim is round-robin
on within-covariate effect rank -- every covariate's best cut first
(ordered by effect), then every covariate's second-best, and so on --
so a finite budget preserves cross-covariate spread instead of
refilling with one covariate's micro-steps.  Setting
\code{max_per_covariate = Inf} recovers a pure global budget; setting
\code{max_subgroups = Inf} recovers per-covariate-only limits.

\strong{Cut form.}  The expression is always the canonical \code{"<covariate> <= <threshold>"}.  forestsearch's factor machinery exposes both the
cut and its complement, so one expression covers both of DINA's
left/right directions -- the \code{direction} column is retained for
reference only.  Thresholds are rounded to \code{digits} significant
figures so they deduplicate cleanly against existing GRF / median cuts
and read well; raw values remain in \code{threshold}.

These cuts are \strong{candidates}, not forced selections: forestsearch
composes (up to \code{maxk}) and consistency-gatekeeps them, exactly as it
does GRF cuts.  To use them without disturbing any user-supplied
forced cuts, append rather than overwrite, e.g.
\code{forestsearch(..., conf_force = c(my_forced_cuts, fr$cut_expr))}.
}
\examples{
set.seed(1)
n <- 400
df_demo <- data.frame(
  w  = stats::rbinom(n, 1, 0.5),
  x1 = stats::runif(n, -1, 1),
  x2 = stats::runif(n, -1, 1),
  x3 = stats::runif(n, -1, 1)
)
tau_x      <- 0.3 + 1.2 * df_demo$x1 - 0.4 * df_demo$x2
df_demo$y  <- 0.5 * df_demo$x1 + df_demo$w * tau_x + stats::rnorm(n)

fit <- dina(df_demo, outcome = "y", treatment = "w",
            covariates = c("x1", "x2", "x3"),
            family = "gaussian", seed = 1L)

fr <- dina_frontier(fit, df_demo, covariates = c("x1", "x2", "x3"),
                    n_min = 60L, max_per_covariate = 3L,
                    max_subgroups = 10L)
fr
fr$cut_expr   # ready to append to forestsearch(conf_force = ...)

}
\seealso{
\code{\link[=dina_subgroup]{dina_subgroup()}} for the single-subgroup selector;
\code{\link[=forestsearch]{forestsearch()}} for the screening stage that consumes the cuts;
\code{\link[=compute_pareto_frontier]{compute_pareto_frontier()}} for the post-hoc frontier reporting on a
forestsearch result.
}

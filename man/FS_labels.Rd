% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subgroup_consistency_helpers.R
\name{FS_labels}
\alias{FS_labels}
\title{Convert Factor Code to Label}
\usage{
FS_labels(Qsg, confs_labels)
}
\arguments{
\item{Qsg}{Character. Factor code in format \code{"q<index>.<action>"} or
\code{"q<index>"}. For the full format, action 0 = NOT (complement),
action 1 = IN (member). Short format defaults to action 1.
Can also be a character vector for vectorized input.}

\item{confs_labels}{Character vector. Labels for each factor, indexed by
factor number.}
}
\value{
Character. Human-readable label wrapped in braces, e.g.,
\code{"{age <= 50}"} or \code{"!{age <= 50}"} for complement. Returns the
original code if no match is found.
}
\description{
Converts q-indexed codes to human-readable labels using the confs_labels
mapping. Supports both full format ("q1.1", "q3.0") and short format
("q1", "q3"). Handles vector input via recursion.
}
\examples{
\dontrun{
labels <- c("age <= 50", "tumor size <= 20", "nodes <= 3")
FS_labels("q1.1", labels)
FS_labels("q1.0", labels)
FS_labels("q2", labels)
FS_labels(c("q1", "q3"), labels)
}
}

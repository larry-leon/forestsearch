% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_forestplot.R
\name{save_forestplot}
\alias{save_forestplot}
\title{Save ForestSearch Forest Plot to File}
\usage{
save_forestplot(x, filename, width = 12, height = 10, dpi = 300, bg = "white")
}
\arguments{
\item{x}{An fs_forestplot object.}

\item{filename}{Character. Output filename. Extension determines format.}

\item{width}{Numeric. Plot width in inches. Default: 12.}

\item{height}{Numeric. Plot height in inches. Default: 10.}

\item{dpi}{Numeric. Resolution for raster formats. Default: 300.}

\item{bg}{Character. Background color. Default: "white".}
}
\value{
Invisibly returns the filename.
}
\description{
Saves a forest plot to a file (PDF, PNG, etc.) with explicit dimensions.
}
\examples{
\donttest{
# Setup: run ForestSearch on GBSG breast cancer data
df <- survival::gbsg
df$grade3 <- as.integer(df$grade == "3")
df$id <- seq_len(nrow(df))
fs <- forestsearch(df,
  confounders.name = c("age", "meno", "size", "grade3", "nodes", "pgr", "er"),
  outcome.name = "rfstime", event.name = "status", treat.name = "hormon",
  fs.splits = 100, max_subgroups_search = 5,
  use_twostage = TRUE, details = FALSE, plot.sg = FALSE)

result <- plot_subgroup_results_forestplot(
  fs_results = list(fs.est = fs),
  df_analysis = df,
  outcome.name = "rfstime", event.name = "status", treat.name = "hormon")

# Save to temporary file
save_forestplot(result, file.path(tempdir(), "forest_plot.pdf"))
}

}

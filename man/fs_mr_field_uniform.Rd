% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fs_mr_field_uniform.R
\name{fs_mr_field_uniform}
\alias{fs_mr_field_uniform}
\title{Uniform (kappa) calibration of the field interval}
\usage{
fs_mr_field_uniform(
  B = NULL,
  Sigma = NULL,
  w,
  sel,
  sigma_sel,
  p_hat,
  t_g = NULL,
  reselection = "maxeff",
  sz = NULL,
  effect_neighborhood = 0.1,
  selection_rule = "neighborhood",
  log_scale = TRUE,
  sdv = NULL,
  zcons_c = NULL,
  delta_grid = seq(0, 4, by = 0.5),
  mass = 0.99,
  M_cap = 40L,
  R_rep = 300L,
  R_out = 300L,
  R_in = 150L,
  alpha = 0.05,
  kappa_grid = seq(1, 3, by = 0.01),
  seed = NULL
)
}
\arguments{
\item{B}{Optional n x K influence matrix (the gate's \code{asm$B}); multiplier
perturbations are \code{crossprod(B, rnorm(n))}, covariance \code{t(B) \%*\% B}.}

\item{Sigma}{Optional K x K covariance, used when \code{B} is \code{NULL} (the
reference checks); perturbations via its Cholesky root.}

\item{w}{Numeric K: the gate's shrunk field (\code{beta_hat} with the winner's
entry replaced by the de-biased estimate), working scale.}

\item{sel}{Integer: the winner's index in \code{w}.}

\item{sigma_sel}{The winner's SE on the working scale (\code{sdv[sel]}).}

\item{p_hat}{Numeric K: re-selection frequencies from the gate's
multiplier pass (\code{tabulate(winner)/draws}).}

\item{t_g}{Numeric K admission floors, or \code{NULL} for no admission filter
(the gate's resolved \code{t_g}, restricted with the candidate set).}

\item{reselection, sz, effect_neighborhood, selection_rule, log_scale}{The
gate's selection-map configuration; \code{zcons_c}, when non-\code{NULL}, is the
consistency centre \code{c_cons} and \code{sdv} the per-candidate SEs so the
\code{maxcons} score can be reconstructed on the restricted family.}

\item{sdv}{Numeric K per-candidate SEs (needed only for \code{maxcons}).}

\item{zcons_c}{Consistency centre \code{c_cons}, or \code{NULL}.}

\item{delta_grid}{Winner-separation grid in SE units (H2 default 0-4 by 0.5).}

\item{mass, M_cap}{Mass-carrying reduction controls (H3 as adjusted at the
Gate 1 adjudication, 2026-09-05: cap 40, so the >= 0.99 mass target is
reachable on enumerated forestsearch families; original default was 12).}

\item{R_rep, R_out, R_in}{Monte Carlo sizes per delta (H4 defaults 300/300/150).}

\item{alpha}{Two-sided miscoverage target (0.05).}

\item{kappa_grid}{Candidate widening factors (default 1 to 3 by 0.01; the
grid was extended past 2 at the Gate 1 adjudication after ceiling hits).}

\item{seed}{Optional integer; drawn once at entry.}
}
\value{
List: \code{kappa} (kappa*), \code{kappa_mcse} (Monte Carlo SE of kappa*
from the binding profile's binomial error and the local slope of
min-delta C2 in kappa), \code{M}, \code{mass_covered}, \code{keep} (indices), \code{minC1},
\code{C1} (profile over \code{delta_grid}), \code{C2_k1} (profile at kappa = 1),
\code{C2_kstar} (profile at kappa*), \code{kappa_grid}, \code{C2_min} (min-over-delta
profile over \code{kappa_grid}), \code{n_kept} (trials with a winner per delta),
\code{delta_grid}, \code{timing_seconds}.
}
\description{
Computes, at analysis time and from the trial's own influence structure,
the smallest widening factor \code{kappa} on \code{kappa_grid} such that the two-sided
field interval \code{c + kappa * (q - c)} attains \code{1 - alpha} coverage
uniformly over a winner-profile protection family: hypothetical true
fields equal to the gate's shrunk field \code{w} with the winner's entry set to
\verb{max_\{g != winner\} w_g + delta * sigma_sel}, for \code{delta} on \code{delta_grid}
(in SE units of the winner).  Nothing is tuned to any simulation design.
}
\details{
The computation restricts to a mass-carrying candidate set (the smallest
\code{M <= M_cap} candidates by re-selection frequency whose cumulative share
of the re-selection mass is at least \code{mass}, the winner always included),
then per \code{delta} replays \code{R_rep} hypothetical trials with Gaussian
multipliers of covariance exactly \code{Sigma_hat} (via \code{crossprod(B, xi)}, or
a Cholesky root of a supplied \code{Sigma}), applies the gate's own selection
map (thresholded argmax under \code{reselection = "maxeff"}, vectorized; any
other rule via the per-draw \code{.fs_mr_select} path), drops trials with no
winner (conditioning on detection, as in the real analysis), and runs the
field procedure on each hypothetical trial to obtain the coverage
profiles C1(delta) (one-sided) and C2(delta; kappa) (two-sided, widened).

The guarantee, as documented for the gate: the one-sided bound is
uniformly valid; the two-sided interval widened by the returned \code{kappa}
is uniformly valid over the winner-profile family; the plain quantile
interval is approximate.
}
\keyword{internal}

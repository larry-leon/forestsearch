# Validate Input Parameters

Validate Input Parameters

## Usage

``` r
validate_inputs(
  data,
  model,
  cens_type,
  outcome_var,
  event_var,
  treatment_var,
  continuous_vars,
  factor_vars
)
```

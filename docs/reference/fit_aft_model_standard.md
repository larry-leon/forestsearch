# Fit Standard AFT Model (Non-Spline)

Fit Standard AFT Model (Non-Spline)

## Usage

``` r
fit_aft_model_standard(
  df_work,
  covariate_cols,
  interaction_term,
  k_treat,
  k_inter,
  verbose,
  set_var = NULL,
  beta_var = NULL
)
```

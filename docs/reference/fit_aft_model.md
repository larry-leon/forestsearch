# Fit AFT Model with Optional Spline Treatment Effect

Fit AFT Model with Optional Spline Treatment Effect

## Usage

``` r
fit_aft_model(
  df_work,
  interaction_term,
  k_treat,
  k_inter,
  verbose,
  spline_spec = NULL,
  set_var = NULL,
  beta_var = NULL
)
```

# TASK — `effMaxSG` band 0.20 at 31% prevalence: the band effect on the J = 10 grid, then the J = 20 grid with harm 1.5 at n = 1,000

Date: 2026-09-07. Author: chat (spec). Executor: Claude Code. Approver: Larry. Supersedes `TASK_p30sg_jcuts20_2026-09-07.md` (not to be run as written).
Predecessors: `TASK_p30_effMaxSG_2026-09-06.md` (campaign `p30sg`: `effMaxSG`, ε = 0.10, `er` J = 10, four cells), the band quote recorded in `REPORT_p30sg_stage0_2026-09-06.md` (HR scale, multiplicative, `hr ≥ (1 − ε)·max hr`, ε = `effect_neighborhood` = 0.10, one helper called by identifier and gate).

## Protocol

- First action: copy this file to `dev/tasks/` and commit. Do not push.
- Gates stop on failure; Gate 1 is the compute go/no-go (may be pre-authorized).
- No change under `R/`. Document-level, add-only template changes: `FS_S7_NBHD` (→ `effect_neighborhood`, default 0.10, forwarded to the gate as `nbhd` as now), `FS_S7_ER_JCUTS` (→ `conf.cont_jcuts = list(er = J)`, default 10), and `return_reselection = TRUE` in the gate call with `p̂(Ĥ)` and the top-3 recorded (changes no computed value). Knob-inert identity against `p30sg` on all non-p̂ columns.
- Committed bundles and documents read-only; new campaigns `p30sg_nb20` (arm A) and `p30sg_nb20_j20` (arm B).
- Standing convention: winner-only and winner-floor IJ variants excluded from every table, figure and report line.

## Purpose

The band width is the identifier's compromise between the effect maximizer and the largest subgroup (`maxSG` is ε → ∞ among screened candidates). At ε = 0.10 the band is a constant log(0.9) ≈ 0.105 log-HR below the maximizer, about a third of an SE at n = 500, and `effMaxSG` enlarged Ĥ by only a fifth. At ε = 0.20 it is log(0.8) ≈ 0.223, roughly three-quarters of an SE. Arm A measures the band effect alone on the identical replicates of `p30sg`; arm B applies the chosen band on the finer `er` grid (J = 20: the boundary er ≤ 59 between the 0.571 and 0.619 cuts, best representable sub-region ~95% of the truth, true cut still not a candidate) and adds harm 1.5 at n = 1,000 as the large-sample true-harm cell.

## Stage 0 — Discovery (no compute)

0a. Quote the template lines pinning `effect_neighborhood` and `fs_conf.cont_jcuts`, and the gate-call forwarding of `nbhd`; confirm the identifier and the gate read the same ε (the `.compute_inclusion_band()` helper on both sides). Report the J = 20 cut values on the GBSG `er` distribution and the position of er ≤ 59 among them.
0b. Quote the gate call and recorder lines for `return_reselection = TRUE` and the `p_hat_H` / top-3 columns.
0c. Cost anchors: p30sg per-replicate costs; expected family size at J = 20 (family builder, one replicate).
0d. `maxSG` (decision S-3): quote whether the gate has an aligned re-selection map for a pure size rule among screened candidates; if not, state so and leave it out — no R/ change in this task.

Gate 0: 0a–0c quoted; STOP if any knob is not document-level or the p̂ pass-through needs an R/ change.

Output: `REPORT_nb20_stage0_<date>.md`.

## Stage 1 — Knobs, identities, smoke, projection

1a. Add the three knobs (document-level).
1b. Identities: all knobs at default — 5 replicates of the p30sg HR 1.00 n500 cell at the p30sg seeds identical (≤ 1e-12) to the `p30sg` bundle on every non-p̂ column; p̂ finite, top-3 mass ≤ 1. ε = 0.20, J = 10 — 5 replicates: the band admits at least as many candidates as ε = 0.10 on every replicate (report the counts), |Ĥ| ≥ the p30sg value on every replicate, fields finite, γ in range, bound identities; on one replicate the observed Ĥ reproduced by the gate's re-selection map on the unperturbed effects. ε = 0.20, J = 20 — 5 replicates per cell: family size K reported, prevalence unchanged, fields finite, alignment check as above.
1c. Projection at 100 workers, measured under load, for both arms.

Gate 1: identities pass; projection reported. Compute go per S-5.

Output: `REPORT_nb20_stage1_<date>.md`.

## Stage 2 — Runs

Common settings: seeds `8316951 + sim_id`, `sim_id` 1–2,000 in two batches, `FS_S7_Z1Q=0.60`, `FS_S7_FOCUS=effMaxSG`, `FS_S7_NBHD=0.20`, `ci_method = "field"`, `include_complement = TRUE`, `field_complement = TRUE`, `ij_residual = "two_term"`, FB none, 100 workers, fail-fast per cell.

**Arm A — campaign `p30sg_nb20`, J = 10 (reduced, per Larry 2026-09-07):** HR 1.50 n500; HR 1.75 n500 — the two harm cells only, where the band effect matters; the null cells are not run in arm A.
**Arm B — campaign `p30sg_nb20_j20`, J = 20:** HR 1.00 n500; HR 1.50 n500; HR 1.75 n500; HR 1.50 n1000 (new); HR 1.00 n1000.

Arm A first (the cheaper, paired one), then arm B. Gate 2 per cell: completeness; same DGM draws as p30sg (`n_true`/`truth` identical, asserted); fields finite; invariants; γ in range; sens/spec/|Ĥ|/p̂ recorded.

## Stage 3 — Report

`REPORT_nb20_<date>.md` plus rendered documents.
1. **Identification, paired on the shared cells:** `p30sg` (ε 0.10, J 10) vs arm A (ε 0.20, J 10) on the two harm cells, and `p30sg` vs arm B (ε 0.20, J 20) on all shared cells (three-way where arm A exists): detection, mean |Ĥ|, |Ĥ|/|H| distribution, sensitivity, specificity, PPV, NPV, share larger/equal/smaller against the previous arm, β(Ĥ) and β(Ĥᶜ) against the planted values, naive optimism both blocks; the harm 1.5 n = 1,000 cell alone.
2. **Constructions** for both arms, both blocks, rows naive / oracle / MR (IJ two-term) / MR (field): bias in log-HR and SD units, empirical SD, mean SE, SE/SD, two-sided coverage with Wilson intervals, one-sided coverage on the exposed side, half-widths; bound-location tables; the joint table; the display for both blocks; the two summary-table formats of 2026-09-07 for Ĥ and Ĥᶜ.
3. **Regime diagnostics:** p̂(Ĥ) (mean, quartiles) and top-3 mass; band size (candidates admitted) per replicate; family size K; complement fits per replicate; corr(Λ*, Λ*ᶜ); for the complement SD(β̃ᶜ)/naive SE and λ-SDᶜ/naive SE — all beside p30sg's values.

Reading criteria (Larry's, not gates; by location and coverage): the band effect on sensitivity and |Ĥ|/|H| (arm A vs p30sg) and the grid effect at the chosen band (arm B vs arm A); the complement's SD(β̃ᶜ)/naive SE as Ĥ grows, with the field's complement upper coverage and the IJ two-term read against it; the harm-side field lower bound within 0.92–0.98 as the family and the region grow; harm 1.5 at n = 1,000 read against n = 500 on both blocks.

## Decisions (defaults in brackets)

- S-1 Cells: arm A reduced to HR 1.50 n500 and HR 1.75 n500; arm B as listed [per Larry].
- S-2 Band: ε = 0.20 [per Larry]; ε = 0.10 not re-run.
- S-3 `maxSG` arm: only if Stage 0 finds an aligned gate map, and then on the four J = 10 cells as arm C [default: no].
- S-4 Workers: 100 [default].
- S-5 Compute: pre-authorization with a wall ceiling (expected ~1 h for arm A, ~5 h for arm B; ceiling 7 h, hard timeout 9 h; arm B cells beyond the ceiling deferred, listed).

## Done means

Stage 3 report and rendered documents committed; Gate 2 records beside them; knobs landed with the knob-inert identity recorded; branch left unpushed for Larry.

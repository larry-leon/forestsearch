# TASK — Certification of the finalized survival constructions at the recommended configuration: the sample-size profile (campaign `cert20`) with the defaults flip (Part D) first

Date: 2026-09-08. Author: chat (spec). Executor: Claude Code (Linux, unattended overnight; Larry offline until the morning). Approver: Larry (F-1/F-2 decided 2026-09-08; kickoff paste = compute go). Reviewer: the Linux MR-field chat.
Finalized approach (F-3 closed 2026-09-08): field one-sided lower bound on β(Ĥ); field-s (R1) one-sided upper bound on β(Ĥᶜ); IJ two-term two-sided; Bonferroni joint pair; identifier effMaxSG, ε = 0.20 (0.10 documented as the conservative option), J = 10. Caveat on both blocks: stable-pick (high-p̂) under-correction; reported by stratum below, never hidden in averages.
Predecessors: `REPORT_field_studentize_e1_2026-09-08.md` (the two n = 500 harm cells of this configuration, campaign `e1stud`), `REPORT_nb20_2026-09-07.md` (arm B nulls at J 20, same DGM draws), `REPORT_harm_location_2026-09-08.md`, `REPORT_complement_location_2026-09-08.md`, `dev/notes/HANDOFF_mr_field_linux_2026-09-08.md`, `dev/notes/MEMO_conditional_curse_2026-09-08.md`.

## Protocol

- First action: archive stale variants in `~/Downloads` to `~/Downloads/cc_archive/`; copy this file to `dev/tasks/` and commit. Do not push. If the file is missing from `~/Downloads`, reconstruct it from the kickoff (which carries every parameter), commit it under this name with a reconstruction note, and proceed.
- Gates stop on failure per cell; a failed cell stops that cell, the next proceeds; the driver re-projects each cell from realized walls of the same n and defers, listing, any cell whose projected finish would cross the ceiling; every render is the committed template driven by `FS_S7_*` env only; `.refuse_if_tracked()` live; `devtools::install(dependencies = FALSE)` before any parallel run; never `load_all()`.
- Standing conventions: winner-only and winner-floor excluded; bounds by location; Wilson intervals; marginal and error SDs side by side; verify from source; records beside results; **no adoption or recommendation change in the record; report and wait.**

## Part D — Defaults become the recommendations (classified: changes behaviour; decided by Larry F-2)

D1. `R/fs_mr_inference.R`: `fs_mr_inference()` defaults `field_complement = TRUE`, `field_scale_complement = c("selected", "none")` (so `match.arg` yields `"selected"`), `return_reselection = TRUE`; `field_decompose` stays `FALSE`; **`ci_method` default untouched** (open decision, Larry: which two-sided interval is primary in user-facing output; the field block runs only under `ci_method = "field"` — quote the line). Roxygen updated to say these are the recommended constructions; `devtools::document()`.
D2. `R/forestsearch_main.R`: the `.g_mr(...)` fallbacks for the same three arguments mirror the new defaults (quote the lines before/after).
D3. Template: the env defaults `FS_S7_FIELD_COMPLEMENT`, `FS_S7_FIELD_SCALEC`, and the `return_reselection` setting default to the recommendations (unset = recommended); knob echo updated. `NEWS.md`: one bullet under the development header, worded "defaults are now the recommended constructions (field complement bound, studentized field-s, re-selection diagnostics); prior behaviour reachable by setting the arguments explicitly".
D4. Install; `deparse()` of installed bodies equals source.
**Gate D (5 replicates, ε 0.20 HR 1.50 n500 config, seeds 8316951 + sim_id, sim_id 1–5, tags `dflt_on`, `dflt_explicit`, `dflt_old`):**
- (i) render with the three knobs **unset** ≡ render with them **explicitly set to the recommendations** — `identical()` on every non-timing column and `truth`;
- (ii) the complement, field-s, joint_s, scale and p̂ columns of (i) `identical()` to the committed `e1stud` rows 1–5 (same machine);
- (iii) render with the knobs explicitly set to the **old** values (FALSE / none / FALSE) ≡ committed `p30sgnb20` rows 1–5 on all pre-existing columns — the old behaviour remains reachable.
On PASS: commit Part D with `REPORT_defaults_flip_2026-09-08.md` (diff summary, gate values). **On FAIL: revert the working tree to HEAD (`git checkout -- .` for the touched files), re-install from HEAD, record the failure and the differing columns in the report, and proceed to Part T** — the campaign sets every knob explicitly and does not depend on the defaults.

## Part T — Campaign `cert20` (compute pre-authorized by the kickoff)

All cells: `FS_S7_FOCUS=effMaxSG FS_S7_NBHD=0.20 FS_S7_Z1Q=0.60 FS_S7_FIELD_COMPLEMENT=TRUE FS_S7_FIELD_SCALEC=selected FS_S7_FIELD_DECOMP=TRUE FS_S7_IJ_RESIDUAL=two_term FS_S7_FB=none FS_S7_CAMPAIGN=cert20`, J = 10, `return_reselection = TRUE`, seeds `8316951 + sim_id`, sim_id 1–2000, two seed-disjoint batches of 1,000 then combine, 100 workers.

| # | Cell | Comparator on record | Purpose |
|---|---|---|---|
| 1 | HR 1.50 n1000 | none at this config (n1000 J20 nb20-B: same DGM draws → assert `n_true`/`truth` identical) | the complement's large-sample question |
| 2 | HR 1.75 n1000 | none | same |
| 3 | HR 1.50 n1500 | none (map1 h150 n1500 was 12.5%) | the IJ two-sided stress point, now at 31% |
| 4 | HR 1.75 n1500 | none | same |
| 5 | HR 1.00 n500 | nb20-B HR 1.00 n500 (J20): same draws, different family | the null: detection, one-sided rates at the null (level-dimension item), complement at the null |
| 6 | HR 1.00 n1000 | nb20-B HR 1.00 n1000 (J20): same draws | same |
| 7 | HR 1.00 n1500 | none | same; last, deferred first if the ceiling threatens |

Existing n = 500 harm cells (`e1stud` ε 0.20 HR 1.50 / 1.75) complete the profile; they are read from the committed bundles, not re-run.

**Stage 1 (per n):** smoke at 5 replicates for n = 1000 and n = 1500 (the n = 1500 configuration at 31% is new: confirm `harm_z1_quantile` produces prevalence ≈ 0.3065 and the planted region scales; report realized |H| and prevalence); every construction finite; invariants; projection per cell from the smoke timings at 100 workers. **Gate 1:** total projection ≤ **10 h** wall → go; hard timeout **12 h**; order as numbered; defer by re-projection.
**Gate 2 per cell:** completeness (2,000 rows; meta knobs as set; `n_workers`, `forestsearch_version`, and the flipped-defaults commit recorded), realized prevalence, same-draws assertions where a comparator exists, every quantity finite on detected replicates, interval invariants on harm, complement, `_s` and joint blocks, γ ∈ [0.025, 0.05], bound↔quantile identities ≤ 1e-12, p̂ validity, detection / sens / spec / PPV / NPV / |Ĥ| recorded.

**Stage 3 — the profile** (`summary_cert20.qmd`, transplanted from `summary_e1stud.qmd`; `REPORT_cert20_2026-09-08.md` beside the results with the Gate 2 record inside):
1. Standard tables (per-cell constructions; across cells) for all nine cells: rows naive / field / field-s / IJ two-term, both blocks; bias (log; marginal-SD and error-SD units), SDs, SE, r, SE/error-SD, one-sided on the exposed side [Wilson], two-sided.
2. **Coverage versus n** at HR 1.50 and 1.75: harm field lower, complement field-s upper (field beside), IJ two-sided, joint Bonferroni — one table and one figure each block.
3. **By p̂ tertile, every cell, both blocks:** the stable-pick caveat versus n — does the T3 shortfall shrink as n grows? Gaussian-implied beside observed.
4. Identification versus n: detection, |Ĥ|/|H| median and q90, sens / spec / PPV / NPV, β(Ĥ) and β(Ĥᶜ) against the planted values, naive optimism in both SD units, p̂ mean.
5. The null cells: detection rate; among detections, the harm lower bound's one-sided rate against β(Ĥ) and the field-s upper against β(Ĥᶜ); bounds by location.
6. **Acceptance criteria, evaluated and stated as findings (pre-registered):** harm field one-sided lower ≥ 0.94 with Wilson support at every harm cell; field-s complement one-sided upper ≥ 0.90 (n 500), ≥ 0.93 (n 1000), ≥ 0.94 (n 1500), Wilson-supported; IJ two-sided ≥ 0.93 everywhere; joint Bonferroni ≥ 0.93; null cells recorded, no criterion. Each criterion: met / not met / not run, per cell, with the number.

## Done means

Part D committed on PASS (or its failure recorded and the tree reverted); Stage 1 smokes and Gate 1 recorded; cells completed / deferred / dropped listed with walls; Gate 2 records; `summary_cert20` rendered; the report committed; branch left unpushed; one-paragraph closing summary giving the criteria table in one line per cell and the commit range. Out of scope: any `R/` change beyond Part D; any recommendation change; the `ci_method` default (open).

# TASK — Extending the 12.4% prevalence grid: is the two-sided decay HR-specific? (campaign `p12ext`)

Date: 2026-09-09. Author: chat (spec). Executor: Claude Code (Linux), unattended overnight (Larry offline). Approver: Larry (kickoff paste = compute go). Reviewer: the Linux MR-field chat.
Predecessors: `REPORT_tier2_2026-09-08.md` (12.4%, HR 1.75 at n = 500/1000/1500 and the null at n = 500), `REPORT_cert20_2026-09-08.md` (31% profile), `REPORT_fixedphat_ij2s_2026-09-09.md` (C-4: the decay is real at fixed p̂ band and is not compositional), `REVIEW_certification_2026-09-09.md`, `dev/notes/NOTE_survival_products_2026-09-09.md`.

**The question.** The IJ two-term **two-sided** interval on the harm block is the one uncertified item in the product set: 0.9611 → 0.9166 → 0.9129 at n = 500 → 1000 → 1500, at 12.4% prevalence — but **only ever measured at HR 1.75**, because that is the only effect size run at this prevalence. If the decay also appears at HR 1.50 it is a low-prevalence / large-n property and the documented caveat is general; if it does not, the caveat narrows to strong-effect settings. C-4 established the decay is real at fixed p̂ band and that composition runs the wrong way to explain it, so this is the natural next discriminator. **Secondary:** the 12.4% null exists only at n = 500 (detection 0.680), leaving the level dimension untested at larger n at low prevalence. **Free byproduct:** with `field_recovery = TRUE` this is the first campaign-scale record of the membership-agreement diagnostics.

## Protocol

- First action: archive stale variants in `~/Downloads` to `~/Downloads/cc_archive/` — **do not archive `HANDOFF_guohe_comparison_2026-09-09.md`** (current, belongs to another chat). Copy this file to `dev/tasks/` and commit. If missing from `~/Downloads`, reconstruct from the kickoff (which carries every cell and knob), commit under this name with a reconstruction note, and proceed.
- **Commit only; do not push.** Larry reviews before the work is treated as final. (GitHub Desktop on this machine pushes when driven from its UI; that is Larry's business and needs no comment in the record.)
- **No `R/` change. No template change.** Every knob explicit in the environment. `devtools::install(dependencies = FALSE)` before any parallel run if the installed package does not already match HEAD; never `load_all()`.
- Gates stop per cell; a failed cell stops that cell and the next proceeds; the driver re-projects each cell from realized walls of the same n and defers, listing, any cell that would cross the ceiling. `.refuse_if_tracked()` live. Leave the seven pre-existing untracked files alone.
- Standing conventions: winner-only and winner-floor excluded; bounds read **by location**, never as significance at the null; Wilson intervals; marginal and error SDs side by side; NPV beside sensitivity/specificity/PPV. **No recommendation change, no repair proposal; report and wait.**

## Stage 0

- Record HEAD, the installed version, and whether installed matches HEAD by `deparse()` of `fs_mr_inference` and `.fs_mr_field_complement`; install if not.
- Confirm from source that `field_recovery = TRUE` reaches the gate through the template's `mr_inference_args` (the knob added 2026-09-09) and name the recorder columns it fills. If the knob does not reach the gate, **run the campaign without it** and say so — the recovery columns are a byproduct, not the object of this task, and their absence must not stop the campaign.
- Quote the committed `tier2` cells' meta (focus `maxeffCons`, `FS_S7_Z1Q` unset, J = 10, seeds `8316951 + sim_id`) so the new cells are the same configuration at new HR and n.

## Stage 1 — Smoke and projection

5-replicate smoke at HR 1.50 n500 and at HR 1.50 n1500: every construction finite in all blocks; interval invariants on harm, complement, `_s`, joint and IJ; γ in range; **realized prevalence stated against the M1 default (≈ 0.124)**; ρᶜ and the recovery columns recorded if present. Time per replicate at 100 workers and project all five cells. **Gate 1: proceed if the total projection is ≤ 8 h wall; hard timeout 10 h.** Note for projection: contention at n = 1500 measured 3.1× the light-load per-replicate cost against 2.0× at n = 500 (`REPORT_cert20`), so project from a loaded reference, not a light one.

## Stage 2 — The five cells (campaign `p12ext`)

All cells: `FS_S7_FOCUS=maxeffCons` (**do not set `FS_S7_Z1Q`** — the M1 default 12.4% configuration, as `tier2` and `s7c`), `FS_S7_FIELD_COMPLEMENT=TRUE FS_S7_FIELD_SCALEC=selected FS_S7_FIELD_DECOMP=TRUE FS_S7_FIELD_RECOV=TRUE FS_S7_IJ_RESIDUAL=two_term FS_S7_FB=none FS_S7_CAMPAIGN=p12ext`, J = 10, `return_reselection = TRUE`, seeds `8316951 + sim_id`, sim_id 1–2000, two seed-disjoint batches of 1,000 then combine, 100 workers.

| # | Cell | Purpose |
|---|---|---|
| 1 | HR 1.50 n500 | the HR 1.50 anchor at this prevalence |
| 2 | HR 1.50 n1000 | **the discriminator** — is the two-sided decay present at HR 1.50? |
| 3 | HR 1.50 n1500 | the discriminator, continued |
| 4 | HR 1.00 n1000 | the null at larger n, low prevalence (level dimension) |
| 5 | HR 1.00 n1500 | same; **deferred first** if the ceiling threatens |

**Gate 2 per cell:** completeness (2,000 rows, sim_id 1–2000, no duplicates, no CONFIG-ERROR, meta knobs as set, `n_workers` and `forestsearch_version` recorded); realized prevalence (distributional criterion); every harm / complement / `_s` / joint / IJ / β(Ĥ) / β(Ĥᶜ) / p̂ / K / ρᶜ quantity finite on detected replicates; interval invariants on every block; γ ∈ [0.025, 0.05]; achieved joint probability ≥ 0.95 − 2/n_joint; bound↔quantile identities ≤ 1e-12; p̂ validity; recovery columns finite where present with `0 ≤ sens_H, ppv_H ≤ 1`; detection / sens / spec / PPV / NPV / |Ĥ| recorded.

## Stage 3 — The report

`summary_p12ext.qmd`, **transplanted from the committed `summary_tier2.qmd`** (globs, cell labels and comparator names only — no fresh authorship), rendered; `REPORT_p12ext_2026-09-09.md` beside the results with the Gate 2 record inside and every number verbatim:

1. **Standard tables** (per-cell constructions; across cells) for the five new cells **plus the four committed `tier2` cells** read as they stand: rows naive / field / field-s / IJ two-term, both blocks; bias (log; marginal-SD and error-SD units), SDs, SE, r, SE/error-SD, one-sided on the exposed side [Wilson], two-sided [Wilson].
2. **The discriminator table.** IJ two-term two-sided coverage on the harm block, at 12.4% prevalence, HR 1.50 against HR 1.75, at n = 500 / 1000 / 1500, with Wilson intervals and the 31% row beside them for contrast. State plainly whether the decay appears at HR 1.50, and if so whether its magnitude matches HR 1.75's.
3. **The mechanism, if the decay appears:** the harm-block bias, SE, bias/SE trajectory and the **two-sided miss split by side** (entirely above vs entirely below the target) against n, as `REPORT_fixedphat_ij2s` Part G did; and the same by fixed p̂ band ([0, 0.05), [0.05, 0.10), [0.10, 0.20), [0.20, 0.35), [0.35, 0.55), [0.55, 1.0]), flagging bands with n < 30. If the decay does **not** appear at HR 1.50, report the same quantities anyway — the contrast is the finding.
4. **The nulls:** detection rate at n = 1000 and 1500 against n = 500's 0.680; among detections, the harm lower bound's one-sided rate and the field-s upper's, bounds by location, and the share of detected null replicates whose harm lower bound reaches 1.00.
5. **One-sided products across the extended grid:** harm field lower and field-s complement upper at every cell, against the certified ranges (0.944–0.974 and 0.912–0.960); by p̂ tertile and by fixed p̂ band, both blocks.
6. **Recovery diagnostics at campaign scale** (if recorded): the distribution of `sens_H`, `ppv_H` and the containment quantiles across replicates, per cell; their correlation with p̂ and with the truth-referenced sensitivity. **This is the first campaign-scale record of these metrics** — describe, do not interpret beyond the record.
7. **No acceptance criteria are pre-registered for this campaign** — it is exploratory on the decay question. Report the certified ranges as reference lines only, and make no recommendation.

## Done means

Stage 0 record; Stage 1 smoke and Gate 1 projection; cells completed / deferred / dropped with walls; Gate 2 records; `summary_p12ext` rendered; the report committed; one-paragraph closing summary giving the discriminator answer in one line, the cells and walls, and the commit range. **Out of scope:** any `R/` or template change, any recommendation change, any repair proposal, the applications documents.

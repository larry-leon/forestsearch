# TASK v2 — Part A: FS at `effMaxSG` ε 0.20, the nine 12.4% cells

- **Date:** 2026-09-12
- **Machine:** Pop!_OS
- **Repo:** `larry-leon/forestsearch`
- **Branch:** `campaign/p12x20`, created by Larry from the `feature/glm-extension` tip
- **Campaign tag:** `p12x20`
- **Governing handoff:** `HANDOFF_partA_fs_effmaxsg_2026-09-12.md`
- **Supersedes** `TASK_p12x20_partA_2026-09-12.md` (v1), whose §2 cleanliness
  assertion counted untracked files and would have stopped on them. v2 changes
  that one assertion. Nothing else differs.

This is a re-parameterization of an existing committed runner. Two knobs change
against the `cert20` / `e1stud` invocation. Nothing is authored fresh.

---

## 0. Standing constraints

- **No `R/` change.** If anything appears to need one, STOP and report.
- CC never runs `git fetch`, `git pull` or `git push`. Larry pushes from GitHub Desktop.
- Every `git add` uses explicit named paths. Never `git add -A` or `git add .`.
- Pre-existing untracked files on this machine are never staged.
- Gates stop on failure, not to ask. A green gate proceeds without a round trip.
- **Do NOT regenerate `current_status.md`.** This branch uses `STATUS_p12x20.md`
  instead (§8). The usual closeout rule for that directory is suspended for the
  duration of this task; it resumes at the merge, which is a separate task.
- Report what is present. Do not assume a column is structurally NA because it
  was on another engine — FS computes `Pcons`, so `n_cons_qual`, `band_n` and
  `p_star` may be populated here.

## 1. First action

- Copy this file to `dev/tasks/TASK_p12x20_partA_2026-09-12_v2.md` and commit it
  with an explicit path.

## 2. Branch assertion

- Assert `git rev-parse --abbrev-ref HEAD` is `campaign/p12x20`. If not, STOP.
- Record `git rev-parse HEAD` as the **base SHA**. Report it.
- Assert `git status --porcelain --untracked-files=no` is empty before Stage 0 —
  i.e. no tracked modifications, so the install corresponds to a commit. If it is
  non-empty, STOP and report the entries.
- Untracked files (`??`) do **not** block. Report them for the record. Never
  stage, stash, clean or commit them.
- Merge nothing into this branch for the duration of the task.

## 3. Stage 0 — install, then verify the installed build

The previous campaign ran against a build predating the O-1 forwarding fix. The
fix is to reinstall unconditionally rather than to check for staleness: there is
no reliable way to recover the commit a local install came from.

- **0a.** Run `devtools::install()` from the repo root at the base SHA. Not
  `load_all()` — parallel workers see only the installed package.
- **0b.** Locate the O-1 forwarding commit in `git log` on this branch. Report
  its SHA and subject, and assert it is an ancestor of the base SHA
  (`git merge-base --is-ancestor`).
- **0c.** Identify a **named token** in the O-1 commit's diff — a function name
  and an argument or line unique to the fix. Report what you chose.
- **0d.** In a **fresh** R session, load the installed package and assert the
  token is present in the installed function body (`deparse()` the installed
  closure, not the source file). Report the assertion and its result.
- **0e.** Repeat 0d **inside a spawned parallel worker**, returning from the
  worker: `packageVersion("forestsearch")`, `.libPaths()[1]`, and the token
  check. This is the gate that matters — workers see only the installed library.
- **0f.** Assert the worker's `.libPaths()[1]` matches the install target.
- **0g.** Write `REPORT_p12x20_stage0_2026-09-12.md` with: base SHA, O-1 SHA and
  ancestry result, token chosen, results of 0d–0f, `packageVersion()`,
  `R.version.string`, hostname, `nproc`, total and available RAM. Commit it.

Any failure in 0a–0f: STOP and report.

## 4. Stage 1 — anchor read and cost probe (compute go/no-go)

Cost on this machine is **not** unmeasured. `cert20` and `e1stud` ran
`effMaxSG` ε 0.20 at 2,000 replicates on this machine, at 31% prevalence.
Anchor to them; probe only the prevalence delta.

- **1a. Anchor read.** From the committed `cert20` and `e1stud` bundle metas,
  read and report per cell: realized wall-clock, worker count, thread pinning,
  `forestsearch` version, and the values of every `FS_S7_*` knob — in particular
  `FS_S7_ER_JCUTS` and the four field knobs. Read from the metas, not from any
  report or summary prose.
- **1b. Knob transplant.** State the `cert20` / `e1stud` driver invocation
  verbatim, then state the Part A invocation as a diff against it. The only
  intended differences are `FS_S7_Z1Q` unset (12.4%) and the campaign tag.
  `FS_S7_FOCUS=effMaxSG` and `FS_S7_NBHD=0.20` should already match. **If any
  other knob differs, STOP and report** rather than reconciling it.
- **1c. Headroom.** Measure, do not copy: report `nproc`, available RAM, and
  peak RSS per worker observed during the probe. Propose a worker count and
  thread pinning for this machine with the measurement behind it.
- **1d. Probe.** Run two bracketing cells at **25 replicates each**, campaign tag
  `p12x20probe`, seeds from the standing convention (`8316951 + sim_id`,
  `sim_id` 1–25):
  - HR 1.50, n 500
  - HR 1.00, n 1500
- **1e. Report the distribution, not a mean.** For each probe cell give the full
  per-replicate wall-clock distribution: min, Q1, median, Q3, max, and the 25
  values. Project the nine cells from the **distribution** — state the projection
  rule used and give a range, not a point.
- **1f.** Note in the projection that `maxeffCons` early-stops and `effMaxSG`
  does not (`stop_threshold` is reset to `NULL` for band foci), so the
  `p12ext` / `tier2` 12.4% walls are a lower bound and are not the anchor.
- **1g.** Write `REPORT_p12x20_gate1_2026-09-12.md` with 1a–1f, and commit it.
  Probe payloads are scratch: do not commit them, and do not let the
  `p12x20probe` tag collide with the campaign tag.

**STOP at the end of Stage 1.** Stage 2 does not begin without Larry's explicit
go on the stated cost.

## 5. Stage 2 — the nine cells

Prevalence 12.4% (`FS_S7_Z1Q` unset), 2,000 replicates each, full MR,
`FS_S7_METHOD=consistency`, `FS_S7_FOCUS=effMaxSG`, `FS_S7_NBHD=0.20`,
`FS_S7_IJ_RESIDUAL=two_term`, `FS_S7_FB=none`, field knobs and `FS_S7_ER_JCUTS`
as read in 1a.

| Cell | HR | n |
|---|---|---|
| A1 | 1.50 | 500 |
| A2 | 1.50 | 1000 |
| A3 | 1.50 | 1500 |
| A4 | 1.75 | 500 |
| A5 | 1.75 | 1000 |
| A6 | 1.75 | 1500 |
| A7 | 1.00 | 500 |
| A8 | 1.00 | 1000 |
| A9 | 1.00 | 1500 |

- Each cell runs as **two seed-disjoint batches** of 1,000 (`sim_id` 1–1000 and
  1001–2000), seeds `8316951 + sim_id`, then combined.
- Assert on combine: 2,000 rows, no duplicate `sim_id`, and the two batches'
  `sim_id` sets are disjoint and their union is 1:2000.
- Run cells one at a time. Each cell's gates (§6) must pass before the next
  cell starts.

## 6. Per-cell gates

- **Gate 2 — the corrected identity**, via the committed `gate2.R`:
  `log(est2_s) + lam_mean_s == log(est2) + lam_mean`.
  Do **not** use the `fld_joint_bonf_*` vs `fld_joint_*` pair — those coincide
  only when γ sits at its 0.025 floor.
- **Same-draws, two directions.** Both are asserted per cell:
  - against the existing 12.4% FS bundles (`p12ext` / `tier2`) at the matched
    HR and n;
  - against `dinamr` and `grfmr` at 12.4% at the matched HR and n.
- In each direction report: `identical()` on `n_true` across all 2,000 rows;
  `all.equal()` on truth at tolerance 1e-8; and **beside it**, `identical()` on
  truth plus the maximum absolute and maximum relative discrepancy.
- Cross-machine and cross-build discrepancy within tolerance is expected and is
  not a defect. A mismatch is a finding about the DGM path — record it and STOP;
  it is not a cell failure to be retried.
- Report the gate counts per cell: checks run, checks passed, failures.

## 7. Artifacts and tracking

- Commit every file carrying payload or analysis content, with explicit paths.
  Nothing another repository would need stays in a session scratchpad.
- Payloads go in a dated directory carrying the `p12x20` tag.
- For each committed artifact report path, tracked status and size. Flag anything
  over 50 MB. Hard stop at 100 MB.
- Per-cell reports: `REPORT_p12x20_<cell>_2026-09-12.md`, committed beside the
  payloads — not in `dev/tasks/`.

## 8. Closeout

- Write `quarto/simulations/gbsg_020/STATUS_p12x20.md`: the campaign's cells and
  their status, payload paths, the base SHA, the Stage 0 install record, and the
  gate counts.
- **No pin post-condition on this file**, and `check_current_status.sh` is not
  run on this branch. The pin claim is made once, at the merge, in a separate
  task on `feature/glm-extension`.
- Regenerate `STATUS_p12x20.md` as the last action of this task and of any
  follow-up task on this branch.
- Report the commit range for Larry to push. Do not push.

## 9. Report format

- Bullet form, one item per bullet.
- Numbers verbatim. No interpretation, no reading of results, no acceptance
  criterion, no recommendation — this task produces computations and verification
  records only.

# TASK — Merge `campaign/p12x20` into `feature/glm-extension`

- **Date:** 2026-09-13
- **Machine:** Pop!_OS
- **Repo:** `larry-leon/forestsearch`
- **Campaign merged:** `p12x20` (Part A), complete at `94e539b6`, already pushed

Part A ran on a side branch so its closeouts would not collide with the Part B
measurement on `current_status.md`. This task brings it back and makes the pin
claim once, at the merged tip.

---

## 0. Standing constraints

- **No `R/` change.** If anything appears to need one, STOP and report.
- CC never runs `git fetch`, `git pull` or `git push`. Larry does those in
  GitHub Desktop.
- Every `git add` uses explicit named paths. Never `git add -A`.
- Never re-run or re-verify the campaign. The nine cells are committed and
  verified; this task touches no payload and re-runs no gate.

## 1. Before CC starts (Larry)

- In GitHub Desktop: check out `feature/glm-extension`, **pull**.
- Check out `campaign/p12x20`.
- CC asserts it is on `campaign/p12x20` with a clean tracked tree, and STOPs
  otherwise.

## 2. Merge direction

Resolve on the campaign branch, where a surprise is cheap.

- **2a.** Merge `feature/glm-extension` into `campaign/p12x20`.
- **2b.** Report every conflicted path. Expected surface:
  - `quarto/simulations/gbsg_020/current_status.md` — **do not hand-resolve.**
    Take either side to complete the merge; §5 regenerates it from the directory.
  - `dev/tasks/` — new files on both sides, no collision expected.
  - Payload directories — disjoint, no collision expected.
  - `R/` — no change on either side. **A conflict under `R/` is a STOP.**
- **2c.** Anything conflicting outside that list: STOP and report the paths and
  both sides. Do not resolve it.
- **2d.** After the merge resolves, check out `feature/glm-extension` and merge
  `campaign/p12x20` into it. This should be clean.

## 3. Logs

- Measure the total bytes of
  `quarto/simulations/gbsg_020/scripts_p12x20/logs/` and report it.
- **If under 25 MB:** commit the logs with explicit paths. This follows the
  directory README, which supersedes the Stage 2 addendum's instruction to leave
  them uncommitted. Record that supersession in `STATUS_p12x20.md`.
- **If 25 MB or over:** leave them uncommitted and report the figure. Do not
  delete them.

## 4. `STATUS_p12x20.md`

- Keep it. It stays the campaign-scoped record.
- Add one line at its head stating that it is campaign-scoped and makes **no pin
  claim**; the pin is asserted only in `current_status.md`.

## 5. `current_status.md` at the merged tip

- Regenerate it from the directory and from git — never from any prior status
  file, report or chat record.
- It must cover **all three campaigns**: `dinamr`, `grfmr` and `p12x20`.
- It must make the layout difference explicit and navigable: `p12x20` payloads
  and renders live in `p12x20_2026-09-12/`; `dinamr` and `grfmr` payloads and
  renders live in the `gbsg_020/` root. A reader arriving at a pin must be able
  to find all three without guessing.
- It must name `STATUS_p12x20.md` as the campaign-scoped record for `p12x20`.
- Do not retrofit the `dinamr` or `grfmr` layout. Describe what is there.

## 6. Post-conditions

- Run `check_current_status.sh`. Its stated pin must equal HEAD at commit time.
  This is the only place in the Part A workstream where that is asserted.
- `git status --porcelain --untracked-files=no` is empty.
- The nine `p12x20` combined payloads are reachable from the merged HEAD.
- `git diff --stat <pre-merge feature tip>..HEAD -- R/` is empty.
- Any failure: STOP and report. Do not push.

## 7. Report

- Bullet form, one item per bullet.
- Conflicted paths and how each was resolved.
- Log bytes and the resulting decision.
- `check_current_status.sh` result.
- The commit range for Larry to push, and which branches it covers.
- Numbers verbatim. No interpretation.

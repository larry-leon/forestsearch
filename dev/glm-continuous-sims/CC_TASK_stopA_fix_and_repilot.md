STOP A, delivered inline — from Larry. This message IS the task document. First action: save this entire message verbatim as dev/glm-continuous-sims/CC_TASK_stopA_fix_and_repilot.md (it gets committed in F1 below). Baseline tip 2d14b7a. Authority by repo path: CC_TASK_md_mr_harness.md (STOP A readouts, checks A/B/C), REPORT_blockers_1-3.md + _ADDENDUM.md (evidence: §D.1 guard, §D.4 consistency_method, §D.2 RNG), REPORT_overnight_funnel50.md (expectations, corrected q-column extraction). Closed decisions in HANDOFF_md_mr_harness_session.md stay closed.

TASK F — fix then re-pilot.

F1. Harness edits in quarto/simulations/actg175/continuous/mr_coverage_sweep_md_harm.qmd — exactly these five, nothing else; the diff against the provenance copy at 169847d must show only these changes plus the saved task document:
a. Remove the front-end overrides: at :125 delete the use_lasso <- TRUE; use_grf <- TRUE assignments (keep use_twostage, is_rct) and delete use_lasso = use_lasso, use_grf = use_grf from the call (~:282). Verified against the actual formals at R/forestsearch_main.R:1096-1100: use_lasso, use_grf, use_dina all default FALSE, so deletion lands on FALSE. Do not add use_dina.
b. Add consistency_method = "resample" to the forestsearch() call (formal default resolves "split" via match.arg — addendum §D.4).
c. Stop swallowing diagnostics: replace error = function(e) NULL (:290) with a handler storing conditionMessage(e) in a new err_msg field and a status distinguishing CONFIG-ERROR from NO-DETECTION; capture MR-skip announcements (quiet = FALSE with output captured, or harvest the .mr_skip condition). A configuration failure must never appear as detected = 0 or mr_ok = 0.
d. Pin RNG: RNGkind("L'Ecuyer-CMRG") immediately before set.seed(seed_for(i)) in the per-replicate function (§D.2: CMRG reproduces n_true = 341).
e. Guard the empty-rule path in CHECK A and CHECK C: zero detections prints the count and skips the identity computation instead of erroring the render (overnight O2.4).
Non-changes: no conf.cont (the preanti 20/50 representability ceiling stays on the record), no threshold/cal_target_md/n/seed-scheme change, no R/ change.
Commit (one commit): fix(harness): MR-compatible defaults, resample consistency, diagnostics hardening — body citing addendum §D.1/§D.4 and overnight O2.4, and including the saved task document. Push.

F2. Re-pilot: the pilot cell — same seed table, same 50 replicates, mr_inference = TRUE, mr_draws = 5000, all else as committed in F1. PID-watch if parallel (never pgrep -f a string the watcher's own command line contains). Record per-replicate t2_secs and total wall (flags-off search was ~162 s/replicate sequential; resample + MR overhead is measured, not assumed).

F3. STOP A report — dev/glm-continuous-sims/REPORT_stopA_md_harm_pilot.md, every number verbatim: (1) detected/50 and mr_ok/50, with any err_msg/skip messages quoted verbatim where mr_ok < detected; (2) the per-cell readouts of CC_TASK_md_mr_harness.md via the corrected q-column extraction; (3) bias and coverage of the MR-corrected estimate against EXACT β(Ĥ) per selected rule via fs_build_eval_frame() / fs_betaHhat_theta_dagger_check(), θ† reference columns kept strictly distinct; (4) naive vs MR-corrected summaries beside the O1 anchors (naive mean ≈ 100.83, excess over Q ≈ 59.42 — same seeds, exact comparison); (5) checks A/B/C outcomes with the new guards; (6) per-replicate wall clock incl. MR and a PROPOSED STOP B grid schedule (n in {500,1000,2000,4000}, s = 1000) — STOP B is NOT started. Commit report + run scripts, push, STOP. Transport, not approval. If F2 errors or MR skips anywhere, F3 is still written with the verbatim messages; do not iterate fixes. Pre-registered context, not acceptance criteria: detection ≈ 50/50; Q floor-clearance ≈ 36/50; naive optimism ≈ 2.5x; MR judged on coverage vs exact β(Ĥ).

TASK U — in parallel while F2 computes; must not touch the harness or interleave with F1's commit. U1: in the roxygen sources generating man/fpr_calibration.Rd and man/sg_tables.Rd, replace ᶜ (U+1D9C) with a LaTeX-safe form (\eqn{\hat{H}^c}{H-hat^c} or the files' existing \eqn convention) and ─ (U+2500) with ASCII; repo roxygen rules (markdown = TRUE, literal % < > &, no hand-escaping); devtools::document(); only those two .Rd files may change. U2: append two lines to CLAUDE.md — certification surface is bare R CMD check --as-cran (rcmdcheck); devtools::check() is the dev loop (--no-manual). U3: ONE rcmdcheck::rcmdcheck(args = "--as-cran", error_on = "never"); paste the final status line verbatim into the STOP A report appendix; expected 0 errors / 0 warnings with at most the environmental tidy NOTE; anything else reported verbatim, not chased. One commit for U, push.

AFTER the STOP A report is pushed: read ~/Downloads/CC_TASK_consistency_method_default.md and execute it — precondition then met — with this substitution to its Rationale and commit body: drop every claim about main/CRAN/release status; rest the rationale solely on branch facts (resample is the intended method; split is the retained, costly fallback — fs.splits x 2 refits, R/forestsearch_main.R:1433-1436; resample covers Cox, logistic, log-binomial/Poisson, identity — R/consistency_resample.R). Its escape clause stands: if STOP A showed resample itself misbehaving, do not flip — write the one-paragraph note instead. Then stop.

Order: F1 commit -> F2 launched -> U during F2 -> F3 after F2 -> single STOP at F3's push -> the gated consistency_method task. No STOP B, no grid, no DGM change, no R/ edits in Task F, no files touched beyond those named.
